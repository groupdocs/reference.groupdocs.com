---
title: FromArgb
second_title: Référence de l'API GroupDocs.Watermark pour .NET
description: Crée unColorgroupdocs.watermark.watermarks/color structure à partir dune valeur ARGB 32 bits.
type: docs
weight: 1430
url: /fr/net/groupdocs.watermark.watermarks/color/fromargb/
---
## FromArgb(int) {#fromargb}

Crée un[`Color`](../../color) structure à partir d'une valeur ARGB 32 bits.

```csharp
public static Color FromArgb(int argb)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| argb | Int32 | Une valeur spécifiant la valeur ARGB 32 bits. |

### Return_Value

Le[`Color`](../../color) structure créée par cette méthode.

### Voir également

* struct [Color](../../color)
* espace de noms [GroupDocs.Watermark.Watermarks](../../color)
* Assemblée [GroupDocs.Watermark](../../../)

---

## FromArgb(int, Color) {#fromargb_1}

Crée un[`Color`](../../color) structure du spécifié[`Color`](../../color) structure, mais avec la nouvelle valeur alpha spécifiée.

```csharp
public static Color FromArgb(int alpha, Color baseColor)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| alpha | Int32 | La valeur alpha du nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |
| baseColor | Color | Le[`Color`](../../color) à partir de laquelle créer le nouveau[`Color`](../../color). |

### Return_Value

Le[`Color`](../../color) que cette méthode crée.

### Exceptions

| exception | condition |
| --- | --- |
| ArgumentOutOfRangeException | Alpha est inférieur à 0 ou supérieur à 255. |

### Remarques

Bien que cette méthode permette de transmettre une valeur 32 bits pour la valeur alpha, la valeur est limitée à 8 bits.

### Voir également

* struct [Color](../../color)
* espace de noms [GroupDocs.Watermark.Watermarks](../../color)
* Assemblée [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int) {#fromargb_2}

Crée un[`Color`](../../color)structure à partir des valeurs de couleur 8 bits spécifiées (rouge, vert et bleu) et la valeur alpha est implicitement 255 (entièrement opaque).

```csharp
public static Color FromArgb(int red, int green, int blue)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| red | Int32 | La valeur du composant rouge pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |
| green | Int32 | La valeur du composant vert pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |
| blue | Int32 | La valeur du composant bleu pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |

### Return_Value

Le[`Color`](../../color) que cette méthode crée.

### Exceptions

| exception | condition |
| --- | --- |
| ArgumentOutOfRangeException | Rouge, vert ou bleu est inférieur à 0 ou supérieur à 255. |

### Remarques

Bien que cette méthode permette de transmettre une valeur de 32 bits pour chaque composant de couleur, la valeur de chaque composant est limitée à 8 bits.

### Voir également

* struct [Color](../../color)
* espace de noms [GroupDocs.Watermark.Watermarks](../../color)
* Assemblée [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int, int) {#fromargb_3}

Crée un[`Color`](../../color) structure à partir des quatre valeurs du composant ARGB (alpha, rouge, vert et bleu).

```csharp
public static Color FromArgb(int alpha, int red, int green, int blue)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| alpha | Int32 | La valeur du composant alpha pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |
| red | Int32 | La valeur du composant rouge pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |
| green | Int32 | La valeur du composant vert pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |
| blue | Int32 | La valeur du composant bleu pour le nouveau[`Color`](../../color). Les valeurs valides sont comprises entre 0 et 255. |

### Return_Value

Le[`Color`](../../color) que cette méthode crée.

### Exceptions

| exception | condition |
| --- | --- |
| ArgumentOutOfRangeException | Rouge, vert ou bleu est inférieur à 0 ou supérieur à 255. |

### Remarques

Bien que cette méthode permette de transmettre une valeur de 32 bits pour chaque composant, la valeur de chaque composant est limitée à 8 bits.

### Voir également

* struct [Color](../../color)
* espace de noms [GroupDocs.Watermark.Watermarks](../../color)
* Assemblée [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
