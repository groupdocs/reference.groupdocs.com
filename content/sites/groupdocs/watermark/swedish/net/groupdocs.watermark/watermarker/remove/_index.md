---
title: Remove
second_title: GroupDocs.Watermark for .NET API-referens
description: Tar bort vattenstämpel från dokumentet.
type: docs
weight: 90
url: /sv/net/groupdocs.watermark/watermarker/remove/
---
## Remove(PossibleWatermark) {#remove}

Tar bort vattenstämpel från dokumentet.

```csharp
public void Remove(PossibleWatermark possibleWatermark)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| possibleWatermark | PossibleWatermark | Vattenstämpeln att ta bort. |

### Anmärkningar

Läs mer om att ta bort vattenstämplar: [Tar bort hittade vattenstämplar](https://docs.groupdocs.com/display/watermarknet/Removing+found+watermarks) .

### Exempel

Hitta och ta bort den första möjliga vattenstämpeln som innehåller viss text eller bild från ett document av vilken typ som helst som stöds.

```csharp
using (Watermarker watermarker = new Watermarker(@"D:\input.doc"))
{
    ImageSearchCriteria imageSearchCriteria = new ImageDctHashSearchCriteria(@"D:\logo.png");
    Regex regex = new Regex(@"^Company\sName$", RegexOptions.IgnoreCase);
    TextSearchCriteria textSearchCriteria = new TextSearchCriteria(regex);
    PossibleWatermarkCollection watermarks = watermarker.Search(textSearchCriteria.Or(imageSearchCriteria));
    if (watermarks.Count > 0)
    {
        watermarker.Remove(watermarks[0]);
    }

    watermarker.Save(@"D:\output.doc");
}
```

### Se även

* class [PossibleWatermark](../../../groupdocs.watermark.search/possiblewatermark)
* class [Watermarker](../../watermarker)
* namnutrymme [GroupDocs.Watermark](../../watermarker)
* hopsättning [GroupDocs.Watermark](../../../)

---

## Remove(PossibleWatermarkCollection) {#remove_1}

Tar bort alla vattenstämplar i samlingen från dokumentet.

```csharp
public void Remove(PossibleWatermarkCollection possibleWatermarks)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| possibleWatermarks | PossibleWatermarkCollection | Samlingen av vattenstämplar att ta bort. |

### Anmärkningar

Läs mer om att ta bort vattenstämplar [Tar bort hittade vattenstämplar](https://docs.groupdocs.com/display/watermarknet/Removing+found+watermarks) .

### Exempel

Hitta och ta bort alla möjliga vattenstämplar som innehåller viss text eller bild från ett document av vilken typ som helst som stöds.

```csharp
using (Watermarker watermarker = new Watermarker(@"D:\input.doc"))
{
    ImageSearchCriteria imageSearchCriteria = new ImageDctHashSearchCriteria(@"D:\logo.png");
    Regex regex = new Regex(@"^Company\sName$", RegexOptions.IgnoreCase);
    TextSearchCriteria textSearchCriteria = new TextSearchCriteria(regex);
    PossibleWatermarkCollection watermarks = watermarker.Search(textSearchCriteria.Or(imageSearchCriteria));
    watermarker.Remove(watermarks);
    watermarker.Save(@"D:\output.doc");
}
```

### Se även

* class [PossibleWatermarkCollection](../../../groupdocs.watermark.search/possiblewatermarkcollection)
* class [Watermarker](../../watermarker)
* namnutrymme [GroupDocs.Watermark](../../watermarker)
* hopsättning [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
