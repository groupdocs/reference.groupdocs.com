---
title: FromArgb
second_title: GroupDocs.Watermark für .NET-API-Referenz
description: Erstellt einColorgroupdocs.watermark.watermarks/color Struktur aus einem 32BitARGBWert.
type: docs
weight: 1430
url: /de/net/groupdocs.watermark.watermarks/color/fromargb/
---
## FromArgb(int) {#fromargb}

Erstellt ein[`Color`](../../color) Struktur aus einem 32-Bit-ARGB-Wert.

```csharp
public static Color FromArgb(int argb)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| argb | Int32 | Ein Wert, der den 32-Bit-ARGB-Wert angibt. |

### Rückgabewert

Der[`Color`](../../color) Struktur, die diese Methode erzeugt.

### Siehe auch

* struct [Color](../../color)
* namensraum [GroupDocs.Watermark.Watermarks](../../color)
* Montage [GroupDocs.Watermark](../../../)

---

## FromArgb(int, Color) {#fromargb_1}

Erstellt ein[`Color`](../../color) Struktur aus der angegebenen[`Color`](../../color) Struktur, aber mit dem neu spezifizierten Alpha-Wert.

```csharp
public static Color FromArgb(int alpha, Color baseColor)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| alpha | Int32 | Der Alpha-Wert für das Neue[`Color`](../../color). Gültige Werte sind 0 bis 255. |
| baseColor | Color | Der[`Color`](../../color) aus der das Neue entsteht[`Color`](../../color). |

### Rückgabewert

Der[`Color`](../../color) die diese Methode erstellt.

### Ausnahmen

| Ausnahme | Bedingung |
| --- | --- |
| ArgumentOutOfRangeException | Alpha ist kleiner als 0 oder größer als 255. |

### Bemerkungen

Obwohl diese Methode die Übergabe eines 32-Bit-Werts für den Alphawert zulässt, ist der Wert auf 8 Bit begrenzt.

### Siehe auch

* struct [Color](../../color)
* namensraum [GroupDocs.Watermark.Watermarks](../../color)
* Montage [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int) {#fromargb_2}

Erstellt ein[`Color`](../../color)Struktur aus den angegebenen 8-Bit-Farbwerten (rot, grün und blau) und der Alphawert ist implizit 255 (vollständig deckend).

```csharp
public static Color FromArgb(int red, int green, int blue)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| red | Int32 | Der rote Komponentenwert für das Neue[`Color`](../../color). Gültige Werte sind 0 bis 255. |
| green | Int32 | Der grüne Komponentenwert für den Neuen[`Color`](../../color). Gültige Werte sind 0 bis 255. |
| blue | Int32 | Der blaue Komponentenwert für das Neue[`Color`](../../color). Gültige Werte sind 0 bis 255. |

### Rückgabewert

Der[`Color`](../../color) die diese Methode erstellt.

### Ausnahmen

| Ausnahme | Bedingung |
| --- | --- |
| ArgumentOutOfRangeException | Rot, Grün oder Blau ist kleiner als 0 oder größer als 255. |

### Bemerkungen

Obwohl diese Methode die Übergabe eines 32-Bit-Werts für jede Farbkomponente zulässt, ist der Wert jeder Komponente auf 8 Bit begrenzt.

### Siehe auch

* struct [Color](../../color)
* namensraum [GroupDocs.Watermark.Watermarks](../../color)
* Montage [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int, int) {#fromargb_3}

Erstellt ein[`Color`](../../color) Struktur aus den vier Werten der ARGB-Komponenten (Alpha, Rot, Grün und Blau).

```csharp
public static Color FromArgb(int alpha, int red, int green, int blue)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| alpha | Int32 | Der Alpha-Komponentenwert für das Neue[`Color`](../../color). Gültige Werte sind 0 bis 255. |
| red | Int32 | Der rote Komponentenwert für das Neue[`Color`](../../color). Gültige Werte sind 0 bis 255. |
| green | Int32 | Der grüne Komponentenwert für den Neuen[`Color`](../../color). Gültige Werte sind 0 bis 255. |
| blue | Int32 | Der blaue Komponentenwert für das Neue[`Color`](../../color). Gültige Werte sind 0 bis 255. |

### Rückgabewert

Der[`Color`](../../color) die diese Methode erstellt.

### Ausnahmen

| Ausnahme | Bedingung |
| --- | --- |
| ArgumentOutOfRangeException | Rot, Grün oder Blau ist kleiner als 0 oder größer als 255. |

### Bemerkungen

Obwohl diese Methode die Übergabe eines 32-Bit-Werts für jede Komponente ermöglicht, ist der Wert jeder Komponente auf 8 Bit begrenzt.

### Siehe auch

* struct [Color](../../color)
* namensraum [GroupDocs.Watermark.Watermarks](../../color)
* Montage [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
