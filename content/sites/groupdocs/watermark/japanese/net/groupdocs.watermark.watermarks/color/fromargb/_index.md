---
title: FromArgb
second_title: GroupDocs.Watermark for .NET API リファレンス
description: を作成しますColorgroupdocs.watermark.watermarks/color32 ビット ARGB 値からの構造体.
type: docs
weight: 1430
url: /ja/net/groupdocs.watermark.watermarks/color/fromargb/
---
## FromArgb(int) {#fromargb}

を作成します[`Color`](../../color)32 ビット ARGB 値からの構造体.

```csharp
public static Color FromArgb(int argb)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| argb | Int32 | 32 ビット ARGB 値を指定する値。 |

### 戻り値

の[`Color`](../../color)このメソッドが作成する構造。

### 関連項目

* struct [Color](../../color)
* 名前空間 [GroupDocs.Watermark.Watermarks](../../color)
* 組み立て [GroupDocs.Watermark](../../../)

---

## FromArgb(int, Color) {#fromargb_1}

を作成します[`Color`](../../color)指定された構造[`Color`](../../color)構造体、 ですが、新しく指定されたアルファ値があります。

```csharp
public static Color FromArgb(int alpha, Color baseColor)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| alpha | Int32 | 新しいのアルファ値[`Color`](../../color).有効な値は 0 ～ 255 です。 |
| baseColor | Color | の[`Color`](../../color)そこから新しいものを作成する[`Color`](../../color). |

### 戻り値

の[`Color`](../../color)このメソッドが作成するもの。

### 例外

| 例外 | 調子 |
| --- | --- |
| ArgumentOutOfRangeException | アルファが 0 未満または 255 より大きい。 |

### 備考

この方法ではアルファ値として 32 ビットの値を渡すことができますが、値は 8 ビットに制限されています。

### 関連項目

* struct [Color](../../color)
* 名前空間 [GroupDocs.Watermark.Watermarks](../../color)
* 組み立て [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int) {#fromargb_2}

を作成します[`Color`](../../color)指定された 8 ビットのカラー値 (赤、緑、青) からの構造体と アルファ値は暗黙的に 255 (完全に不透明) です.

```csharp
public static Color FromArgb(int red, int green, int blue)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| red | Int32 | 新しいコンポーネントの赤のコンポーネント値[`Color`](../../color).有効な値は 0 ～ 255 です。 |
| green | Int32 | 新しいのグリーン コンポーネント値[`Color`](../../color).有効な値は 0 ～ 255 です。 |
| blue | Int32 | 新しいの青のコンポーネント値[`Color`](../../color).有効な値は 0 ～ 255 です。 |

### 戻り値

の[`Color`](../../color)このメソッドが作成するもの。

### 例外

| 例外 | 調子 |
| --- | --- |
| ArgumentOutOfRangeException | 赤、緑、または青が 0 未満または 255 より大きい。 |

### 備考

この方法では各色成分に 32 ビット値を渡すことができますが、 各成分の値は 8 ビットに制限されています。

### 関連項目

* struct [Color](../../color)
* 名前空間 [GroupDocs.Watermark.Watermarks](../../color)
* 組み立て [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int, int) {#fromargb_3}

を作成します[`Color`](../../color) 4 つの ARGB コンポーネント (アルファ、赤、緑、青) 値からの構造。

```csharp
public static Color FromArgb(int alpha, int red, int green, int blue)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| alpha | Int32 | 新しいのアルファ成分値[`Color`](../../color).有効な値は 0 ～ 255 です。 |
| red | Int32 | 新しいコンポーネントの赤のコンポーネント値[`Color`](../../color).有効な値は 0 ～ 255 です。 |
| green | Int32 | 新しいのグリーン コンポーネント値[`Color`](../../color).有効な値は 0 ～ 255 です。 |
| blue | Int32 | 新しいの青のコンポーネント値[`Color`](../../color).有効な値は 0 ～ 255 です。 |

### 戻り値

の[`Color`](../../color)このメソッドが作成するもの。

### 例外

| 例外 | 調子 |
| --- | --- |
| ArgumentOutOfRangeException | 赤、緑、または青が 0 未満または 255 より大きい。 |

### 備考

この方法では各コンポーネントに 32 ビットの値を渡すことができますが、 各コンポーネントの値は 8 ビットに制限されています。

### 関連項目

* struct [Color](../../color)
* 名前空間 [GroupDocs.Watermark.Watermarks](../../color)
* 組み立て [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
