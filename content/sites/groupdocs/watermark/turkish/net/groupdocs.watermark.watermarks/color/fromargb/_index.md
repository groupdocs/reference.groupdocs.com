---
title: FromArgb
second_title: .NET API Başvurusu için GroupDocs.Watermark
description: oluştururColorgroupdocs.watermark.watermarks/color 32 bit ARGB değerinden yapı.
type: docs
weight: 1430
url: /tr/net/groupdocs.watermark.watermarks/color/fromargb/
---
## FromArgb(int) {#fromargb}

oluşturur[`Color`](../../color) 32 bit ARGB değerinden yapı.

```csharp
public static Color FromArgb(int argb)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| argb | Int32 | 32 bit ARGB değerini belirten bir değer. |

### Geri dönüş değeri

bu[`Color`](../../color) Bu yöntemin oluşturduğu yapı.

### Ayrıca bakınız

* struct [Color](../../color)
* ad alanı [GroupDocs.Watermark.Watermarks](../../color)
* toplantı [GroupDocs.Watermark](../../../)

---

## FromArgb(int, Color) {#fromargb_1}

oluşturur[`Color`](../../color) belirtilen yapı[`Color`](../../color) yapı, ancak belirtilen yeni alfa değeriyle.

```csharp
public static Color FromArgb(int alpha, Color baseColor)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| alpha | Int32 | Yeni için alfa değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |
| baseColor | Color | bu[`Color`](../../color) yenisini yaratmak için[`Color`](../../color). |

### Geri dönüş değeri

bu[`Color`](../../color) bu yöntemin oluşturduğu.

### istisnalar

| istisna | şart |
| --- | --- |
| ArgumentOutOfRangeException | Alfa, 0'dan küçük veya 255'ten büyüktür. |

### Notlar

Bu yöntem, alfa değeri için 32 bitlik bir değerin geçmesine izin verse de, değer 8 bit ile sınırlıdır.

### Ayrıca bakınız

* struct [Color](../../color)
* ad alanı [GroupDocs.Watermark.Watermarks](../../color)
* toplantı [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int) {#fromargb_2}

oluşturur[`Color`](../../color)yapı, belirtilen 8 bitlik renk değerlerinden (kırmızı, yeşil ve mavi) ve alfa değeri dolaylı olarak 255'tir (tamamen opak).

```csharp
public static Color FromArgb(int red, int green, int blue)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| red | Int32 | Yeni için kırmızı bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |
| green | Int32 | Yeni için yeşil bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |
| blue | Int32 | Yeni için mavi bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |

### Geri dönüş değeri

bu[`Color`](../../color) bu yöntemin oluşturduğu.

### istisnalar

| istisna | şart |
| --- | --- |
| ArgumentOutOfRangeException | Kırmızı, yeşil veya mavi, 0'dan küçük veya 255'ten büyüktür. |

### Notlar

Bu yöntem her renk bileşeni için 32 bitlik bir değerin iletilmesine izin verse de, her bileşenin değeri 8 bit ile sınırlıdır.

### Ayrıca bakınız

* struct [Color](../../color)
* ad alanı [GroupDocs.Watermark.Watermarks](../../color)
* toplantı [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int, int) {#fromargb_3}

oluşturur[`Color`](../../color) dört ARGB bileşeni (alfa, kırmızı, yeşil ve mavi) değerlerinden yapı.

```csharp
public static Color FromArgb(int alpha, int red, int green, int blue)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| alpha | Int32 | Yeni için alfa bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |
| red | Int32 | Yeni için kırmızı bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |
| green | Int32 | Yeni için yeşil bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |
| blue | Int32 | Yeni için mavi bileşen değeri[`Color`](../../color). Geçerli değerler 0 ile 255 arasındadır. |

### Geri dönüş değeri

bu[`Color`](../../color) bu yöntemin oluşturduğu.

### istisnalar

| istisna | şart |
| --- | --- |
| ArgumentOutOfRangeException | Kırmızı, yeşil veya mavi, 0'dan küçük veya 255'ten büyüktür. |

### Notlar

Bu yöntem her bileşen için 32 bitlik bir değerin iletilmesine izin verse de, her bileşenin değeri 8 bit ile sınırlıdır.

### Ayrıca bakınız

* struct [Color](../../color)
* ad alanı [GroupDocs.Watermark.Watermarks](../../color)
* toplantı [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
