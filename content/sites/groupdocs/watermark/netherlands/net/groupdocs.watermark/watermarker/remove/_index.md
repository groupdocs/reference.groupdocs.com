---
title: Remove
second_title: GroupDocs.Watermark voor .NET API-referentie
description: Verwijdert watermerk uit het document.
type: docs
weight: 90
url: /nl/net/groupdocs.watermark/watermarker/remove/
---
## Remove(PossibleWatermark) {#remove}

Verwijdert watermerk uit het document.

```csharp
public void Remove(PossibleWatermark possibleWatermark)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| possibleWatermark | PossibleWatermark | Het watermerk dat moet worden verwijderd. |

### Opmerkingen

Meer informatie over het verwijderen van watermerken: [Gevonden watermerken verwijderen](https://docs.groupdocs.com/display/watermarknet/Removing+found+watermarks) .

### Voorbeelden

Zoek en verwijder het eerst mogelijke watermerk met bepaalde tekst of afbeelding uit een document van elk ondersteund type.

```csharp
using (Watermarker watermarker = new Watermarker(@"D:\input.doc"))
{
    ImageSearchCriteria imageSearchCriteria = new ImageDctHashSearchCriteria(@"D:\logo.png");
    Regex regex = new Regex(@"^Company\sName$", RegexOptions.IgnoreCase);
    TextSearchCriteria textSearchCriteria = new TextSearchCriteria(regex);
    PossibleWatermarkCollection watermarks = watermarker.Search(textSearchCriteria.Or(imageSearchCriteria));
    if (watermarks.Count > 0)
    {
        watermarker.Remove(watermarks[0]);
    }

    watermarker.Save(@"D:\output.doc");
}
```

### Zie ook

* class [PossibleWatermark](../../../groupdocs.watermark.search/possiblewatermark)
* class [Watermarker](../../watermarker)
* naamruimte [GroupDocs.Watermark](../../watermarker)
* montage [GroupDocs.Watermark](../../../)

---

## Remove(PossibleWatermarkCollection) {#remove_1}

Verwijdert alle watermerken in de verzameling uit het document.

```csharp
public void Remove(PossibleWatermarkCollection possibleWatermarks)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| possibleWatermarks | PossibleWatermarkCollection | De verzameling watermerken om te verwijderen. |

### Opmerkingen

Meer informatie over het verwijderen van watermerken [Gevonden watermerken verwijderen](https://docs.groupdocs.com/display/watermarknet/Removing+found+watermarks) .

### Voorbeelden

Zoek en verwijder alle mogelijke watermerken met bepaalde tekst of afbeelding uit een document van elk ondersteund type.

```csharp
using (Watermarker watermarker = new Watermarker(@"D:\input.doc"))
{
    ImageSearchCriteria imageSearchCriteria = new ImageDctHashSearchCriteria(@"D:\logo.png");
    Regex regex = new Regex(@"^Company\sName$", RegexOptions.IgnoreCase);
    TextSearchCriteria textSearchCriteria = new TextSearchCriteria(regex);
    PossibleWatermarkCollection watermarks = watermarker.Search(textSearchCriteria.Or(imageSearchCriteria));
    watermarker.Remove(watermarks);
    watermarker.Save(@"D:\output.doc");
}
```

### Zie ook

* class [PossibleWatermarkCollection](../../../groupdocs.watermark.search/possiblewatermarkcollection)
* class [Watermarker](../../watermarker)
* naamruimte [GroupDocs.Watermark](../../watermarker)
* montage [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
