---
title: Remove
second_title: GroupDocs.Watermark για Αναφορά API .NET
description: Αφαιρεί το υδατογράφημα από το έγγραφο.
type: docs
weight: 90
url: /el/net/groupdocs.watermark/watermarker/remove/
---
## Remove(PossibleWatermark) {#remove}

Αφαιρεί το υδατογράφημα από το έγγραφο.

```csharp
public void Remove(PossibleWatermark possibleWatermark)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| possibleWatermark | PossibleWatermark | Το υδατογράφημα προς αφαίρεση. |

### Παρατηρήσεις

Μάθετε περισσότερα σχετικά με την αφαίρεση υδατογραφημάτων: [Αφαίρεση υδατογραφημάτων που βρέθηκαν](https://docs.groupdocs.com/display/watermarknet/Removing+found+watermarks) .

### Παραδείγματα

Βρείτε και αφαιρέστε το πρώτο πιθανό υδατογράφημα που περιέχει συγκεκριμένο κείμενο ή εικόνα από ένα έγγραφο οποιουδήποτε υποστηριζόμενου τύπου.

```csharp
using (Watermarker watermarker = new Watermarker(@"D:\input.doc"))
{
    ImageSearchCriteria imageSearchCriteria = new ImageDctHashSearchCriteria(@"D:\logo.png");
    Regex regex = new Regex(@"^Company\sName$", RegexOptions.IgnoreCase);
    TextSearchCriteria textSearchCriteria = new TextSearchCriteria(regex);
    PossibleWatermarkCollection watermarks = watermarker.Search(textSearchCriteria.Or(imageSearchCriteria));
    if (watermarks.Count > 0)
    {
        watermarker.Remove(watermarks[0]);
    }

    watermarker.Save(@"D:\output.doc");
}
```

### Δείτε επίσης

* class [PossibleWatermark](../../../groupdocs.watermark.search/possiblewatermark)
* class [Watermarker](../../watermarker)
* χώρος ονομάτων [GroupDocs.Watermark](../../watermarker)
* συνέλευση [GroupDocs.Watermark](../../../)

---

## Remove(PossibleWatermarkCollection) {#remove_1}

Αφαιρεί όλα τα υδατογραφήματα της συλλογής από το έγγραφο.

```csharp
public void Remove(PossibleWatermarkCollection possibleWatermarks)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| possibleWatermarks | PossibleWatermarkCollection | Η συλλογή υδατογραφημάτων για αφαίρεση. |

### Παρατηρήσεις

Μάθετε περισσότερα σχετικά με την κατάργηση υδατογραφημάτων [Αφαίρεση υδατογραφημάτων που βρέθηκαν](https://docs.groupdocs.com/display/watermarknet/Removing+found+watermarks) .

### Παραδείγματα

Βρείτε και αφαιρέστε όλα τα πιθανά υδατογραφήματα που περιέχουν συγκεκριμένο κείμενο ή εικόνα από ένα έγγραφο οποιουδήποτε υποστηριζόμενου τύπου.

```csharp
using (Watermarker watermarker = new Watermarker(@"D:\input.doc"))
{
    ImageSearchCriteria imageSearchCriteria = new ImageDctHashSearchCriteria(@"D:\logo.png");
    Regex regex = new Regex(@"^Company\sName$", RegexOptions.IgnoreCase);
    TextSearchCriteria textSearchCriteria = new TextSearchCriteria(regex);
    PossibleWatermarkCollection watermarks = watermarker.Search(textSearchCriteria.Or(imageSearchCriteria));
    watermarker.Remove(watermarks);
    watermarker.Save(@"D:\output.doc");
}
```

### Δείτε επίσης

* class [PossibleWatermarkCollection](../../../groupdocs.watermark.search/possiblewatermarkcollection)
* class [Watermarker](../../watermarker)
* χώρος ονομάτων [GroupDocs.Watermark](../../watermarker)
* συνέλευση [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
