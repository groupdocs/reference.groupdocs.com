---
title: FromArgb
second_title: Riferimento API GroupDocs.Watermark per .NET
description: Crea unColorgroupdocs.watermark.watermarks/color struttura da un valore ARGB a 32 bit.
type: docs
weight: 1430
url: /it/net/groupdocs.watermark.watermarks/color/fromargb/
---
## FromArgb(int) {#fromargb}

Crea un[`Color`](../../color) struttura da un valore ARGB a 32 bit.

```csharp
public static Color FromArgb(int argb)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| argb | Int32 | Un valore che specifica il valore ARGB a 32 bit. |

### Valore di ritorno

Il[`Color`](../../color) struttura creata da questo metodo.

### Guarda anche

* struct [Color](../../color)
* spazio dei nomi [GroupDocs.Watermark.Watermarks](../../color)
* assemblea [GroupDocs.Watermark](../../../)

---

## FromArgb(int, Color) {#fromargb_1}

Crea un[`Color`](../../color) struttura dal specificato[`Color`](../../color) struttura, ma con il nuovo valore alfa specificato.

```csharp
public static Color FromArgb(int alpha, Color baseColor)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| alpha | Int32 | Il valore alfa per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |
| baseColor | Color | Il[`Color`](../../color) da cui creare il nuovo[`Color`](../../color). |

### Valore di ritorno

Il[`Color`](../../color) che questo metodo crea.

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentOutOfRangeException | Alfa è minore di 0 o maggiore di 255. |

### Osservazioni

Sebbene questo metodo consenta di passare un valore a 32 bit per il valore alfa, il valore è limitato a 8 bit.

### Guarda anche

* struct [Color](../../color)
* spazio dei nomi [GroupDocs.Watermark.Watermarks](../../color)
* assemblea [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int) {#fromargb_2}

Crea un[`Color`](../../color)struttura dai valori di colore a 8 bit specificati (rosso, verde e blu) e il valore alfa è implicitamente 255 (completamente opaco).

```csharp
public static Color FromArgb(int red, int green, int blue)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| red | Int32 | Il valore del componente rosso per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |
| green | Int32 | Il valore della componente verde per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |
| blue | Int32 | Il valore del componente blu per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |

### Valore di ritorno

Il[`Color`](../../color) che questo metodo crea.

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentOutOfRangeException | Rosso, verde o blu è minore di 0 o maggiore di 255. |

### Osservazioni

Sebbene questo metodo consenta di passare un valore a 32 bit per ciascun componente colore, il valore di ciascun componente è limitato a 8 bit.

### Guarda anche

* struct [Color](../../color)
* spazio dei nomi [GroupDocs.Watermark.Watermarks](../../color)
* assemblea [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int, int) {#fromargb_3}

Crea un[`Color`](../../color) struttura dai valori dei quattro componenti ARGB (alfa, rosso, verde e blu).

```csharp
public static Color FromArgb(int alpha, int red, int green, int blue)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| alpha | Int32 | Il valore del componente alfa per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |
| red | Int32 | Il valore del componente rosso per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |
| green | Int32 | Il valore della componente verde per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |
| blue | Int32 | Il valore del componente blu per il nuovo[`Color`](../../color). I valori validi sono da 0 a 255. |

### Valore di ritorno

Il[`Color`](../../color) che questo metodo crea.

### Eccezioni

| eccezione | condizione |
| --- | --- |
| ArgumentOutOfRangeException | Rosso, verde o blu è minore di 0 o maggiore di 255. |

### Osservazioni

Sebbene questo metodo consenta di passare un valore a 32 bit per ciascun componente, il valore di ciascun componente è limitato a 8 bit.

### Guarda anche

* struct [Color](../../color)
* spazio dei nomi [GroupDocs.Watermark.Watermarks](../../color)
* assemblea [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
