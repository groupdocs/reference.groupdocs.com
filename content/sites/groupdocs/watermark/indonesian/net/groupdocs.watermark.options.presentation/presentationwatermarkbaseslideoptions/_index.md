---
title: PresentationWatermarkBaseSlideOptions
second_title: GroupDocs.Watermark untuk Referensi .NET API
description: Kelas dasar untuk menambahkan opsi tanda air ke dokumen Presentasi.
type: docs
weight: 1990
url: /id/net/groupdocs.watermark.options.presentation/presentationwatermarkbaseslideoptions/
---
## PresentationWatermarkBaseSlideOptions class

Kelas dasar untuk menambahkan opsi tanda air ke dokumen Presentasi.

```csharp
public abstract class PresentationWatermarkBaseSlideOptions : PresentationWatermarkOptions
```

## Properti

| Nama | Keterangan |
| --- | --- |
| [AlternativeText](../../groupdocs.watermark.options.presentation/presentationwatermarkbaseslideoptions/alternativetext) { get; set; } | Mendapatkan atau menyetel teks deskriptif (alternatif) yang akan dikaitkan dengan bentuk. |
| [Effects](../../groupdocs.watermark.options.presentation/presentationwatermarkbaseslideoptions/effects) { get; set; } | Mendapat atau menetapkan nilai[`PresentationImageEffects`](../presentationimageeffects) or [`PresentationTextEffects`](../presentationtexteffects) untuk efek yang harus diterapkan pada watermark. |
| [IsLocked](../../groupdocs.watermark.options.presentation/presentationwatermarkbaseslideoptions/islocked) { get; set; } | Mendapatkan atau menetapkan nilai yang menunjukkan apakah pengeditan bentuk di PowerPoint dilarang. |
| [Name](../../groupdocs.watermark.options.presentation/presentationwatermarkbaseslideoptions/name) { get; set; } | Mendapatkan atau menyetel nama menjadi bentuk. |
| [ProtectWithUnreadableCharacters](../../groupdocs.watermark.options.presentation/presentationwatermarkbaseslideoptions/protectwithunreadablecharacters) { get; set; } | Mendapat atau menetapkan nilai yang menunjukkan apakah karakter tanda air teks dicampur dengan karakter yang tidak dapat dibaca. |

### Perkataan

**Belajarlah lagi:**

* [Tambahkan tanda air ke dokumen presentasi](https://docs.groupdocs.com/display/watermarknet/Add+watermarks+to+presentation+documents)

### Contoh

Tambahkan watermark ke slide layanan yang berbeda dari presentasi Power Point.

```csharp
PresentationLoadOptions loadOptions = new PresentationLoadOptions();
using (Watermarker watermarker = new Watermarker(@"D:\test.pptx", loadOptions))
{
    TextWatermark watermark = new TextWatermark("Test watermark", new Font("Arial", 8));

    // Tambahkan tanda air ke semua slide master
    PresentationWatermarkMasterSlideOptions masterSlideOptions = new PresentationWatermarkMasterSlideOptions();
    masterSlideOptions.MasterSlideIndex = -1; // bawaan
    watermarker.Add(watermark, masterSlideOptions);

    // Tambahkan tanda air ke semua slide tata letak
    PresentationWatermarkLayoutSlideOptions layoutSlideOptions = new PresentationWatermarkLayoutSlideOptions();
    layoutSlideOptions.LayoutSlideIndex = -1; // bawaan
    watermarker.Add(watermark, layoutSlideOptions);

    // Tambahkan tanda air ke semua slide catatan
    PresentationWatermarkNoteSlideOptions noteSlideOptions = new PresentationWatermarkNoteSlideOptions();
    noteSlideOptions.SlideIndex = -1; // bawaan
    watermarker.Add(watermark, noteSlideOptions);

    // Tambahkan tanda air ke master handout
    PresentationWatermarkMasterHandoutSlideOptions masterHandoutSlideOptions = new PresentationWatermarkMasterHandoutSlideOptions();
    watermarker.Add(watermark, masterHandoutSlideOptions);

    // Tambahkan tanda air ke master catatan
    PresentationWatermarkMasterNotesSlideOptions masterNotesSlideOptions = new PresentationWatermarkMasterNotesSlideOptions();
    watermarker.Add(watermark, masterNotesSlideOptions);

    watermarker.Save();
}
```

### Lihat juga

* class [PresentationWatermarkOptions](../presentationwatermarkoptions)
* ruang nama [GroupDocs.Watermark.Options.Presentation](../../groupdocs.watermark.options.presentation)
* perakitan [GroupDocs.Watermark](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
