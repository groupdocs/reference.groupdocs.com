---
title: FromArgb
second_title: GroupDocs.Watermark untuk Referensi .NET API
description: Membuat aColorgroupdocs.watermark.watermarks/color struktur dari nilai ARGB 32bit.
type: docs
weight: 1430
url: /id/net/groupdocs.watermark.watermarks/color/fromargb/
---
## FromArgb(int) {#fromargb}

Membuat a[`Color`](../../color) struktur dari nilai ARGB 32-bit.

```csharp
public static Color FromArgb(int argb)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| argb | Int32 | Nilai yang menentukan nilai ARGB 32-bit. |

### Nilai Pengembalian

Itu[`Color`](../../color) struktur yang diciptakan metode ini.

### Lihat juga

* struct [Color](../../color)
* ruang nama [GroupDocs.Watermark.Watermarks](../../color)
* perakitan [GroupDocs.Watermark](../../../)

---

## FromArgb(int, Color) {#fromargb_1}

Membuat a[`Color`](../../color) struktur dari yang ditentukan[`Color`](../../color) struktur, tetapi dengan nilai alfa baru yang ditentukan.

```csharp
public static Color FromArgb(int alpha, Color baseColor)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| alpha | Int32 | Nilai alfa untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |
| baseColor | Color | Itu[`Color`](../../color) dari mana untuk membuat yang baru[`Color`](../../color). |

### Nilai Pengembalian

Itu[`Color`](../../color) yang dibuat oleh metode ini.

### Pengecualian

| pengecualian | kondisi |
| --- | --- |
| ArgumentOutOfRangeException | Alpha kurang dari 0 atau lebih besar dari 255. |

### Perkataan

Meskipun metode ini memungkinkan nilai 32-bit dilewatkan untuk nilai alfa, nilainya dibatasi hingga 8 bit.

### Lihat juga

* struct [Color](../../color)
* ruang nama [GroupDocs.Watermark.Watermarks](../../color)
* perakitan [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int) {#fromargb_2}

Membuat a[`Color`](../../color)struktur dari nilai warna 8-bit yang ditentukan (merah, hijau, dan biru) dan nilai alfa secara implisit 255 (sepenuhnya buram).

```csharp
public static Color FromArgb(int red, int green, int blue)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| red | Int32 | Nilai komponen merah untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |
| green | Int32 | Nilai komponen hijau untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |
| blue | Int32 | Nilai komponen biru untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |

### Nilai Pengembalian

Itu[`Color`](../../color) yang dibuat oleh metode ini.

### Pengecualian

| pengecualian | kondisi |
| --- | --- |
| ArgumentOutOfRangeException | Merah, hijau, atau biru kurang dari 0 atau lebih besar dari 255. |

### Perkataan

Meskipun metode ini memungkinkan nilai 32-bit dilewatkan untuk setiap komponen warna, nilai setiap komponen dibatasi hingga 8 bit.

### Lihat juga

* struct [Color](../../color)
* ruang nama [GroupDocs.Watermark.Watermarks](../../color)
* perakitan [GroupDocs.Watermark](../../../)

---

## FromArgb(int, int, int, int) {#fromargb_3}

Membuat a[`Color`](../../color) struktur dari empat nilai komponen ARGB (alfa, merah, hijau, dan biru).

```csharp
public static Color FromArgb(int alpha, int red, int green, int blue)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| alpha | Int32 | Nilai komponen alfa untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |
| red | Int32 | Nilai komponen merah untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |
| green | Int32 | Nilai komponen hijau untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |
| blue | Int32 | Nilai komponen biru untuk yang baru[`Color`](../../color). Nilai yang valid adalah 0 hingga 255. |

### Nilai Pengembalian

Itu[`Color`](../../color) yang dibuat oleh metode ini.

### Pengecualian

| pengecualian | kondisi |
| --- | --- |
| ArgumentOutOfRangeException | Merah, hijau, atau biru kurang dari 0 atau lebih besar dari 255. |

### Perkataan

Meskipun metode ini memungkinkan nilai 32-bit dilewatkan untuk setiap komponen, nilai setiap komponen dibatasi hingga 8 bit.

### Lihat juga

* struct [Color](../../color)
* ruang nama [GroupDocs.Watermark.Watermarks](../../color)
* perakitan [GroupDocs.Watermark](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Watermark.dll -->
