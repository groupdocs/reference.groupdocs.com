---
title: PreviewOptions
second_title: .NET API 참조용 GroupDocs.Redaction
description: 미리 보기 생성을 위한 요구 사항 및 스트림 대리자를 설정하는 옵션을 제공합니다.
type: docs
weight: 330
url: /ko/net/groupdocs.redaction.options/previewoptions/
---
## PreviewOptions class

미리 보기 생성을 위한 요구 사항 및 스트림 대리자를 설정하는 옵션을 제공합니다.

```csharp
public class PreviewOptions
```

## 생성자

| 이름 | 설명 |
| --- | --- |
| [PreviewOptions](previewoptions#constructor)(CreatePageStream) | PreviewOptions 클래스의 새 인스턴스를 초기화하여 출력 스트림을 닫습니다. |
| [PreviewOptions](previewoptions#constructor_1)(CreatePageStream, ReleasePageStream) | PreviewOptions 클래스의 새 인스턴스를 초기화하여 추가 사용을 위해 출력 스트림이 클라이언트에 반환되도록 합니다. |

## 속성

| 이름 | 설명 |
| --- | --- |
| [CreatePageStream](../../groupdocs.redaction.options/previewoptions/createpagestream) { get; set; } | 페이지 스트림 생성 대리자의 인스턴스를 가져오거나 설정합니다. |
| [Height](../../groupdocs.redaction.options/previewoptions/height) { get; set; } | 페이지 미리보기 높이를 가져오거나 설정합니다. |
| [PageNumbers](../../groupdocs.redaction.options/previewoptions/pagenumbers) { get; set; } | 미리 보기를 생성할 페이지 번호 배열을 가져오거나 설정합니다. |
| [PreviewFormat](../../groupdocs.redaction.options/previewoptions/previewformat) { get; set; } | 미리보기 이미지 형식을 가져오거나 설정합니다. |
| [ReleasePageStream](../../groupdocs.redaction.options/previewoptions/releasepagestream) { get; set; } | 페이지 미리 보기 완료 대리자의 인스턴스를 가져오거나 설정합니다. |
| [Width](../../groupdocs.redaction.options/previewoptions/width) { get; set; } | 페이지 미리보기 너비를 가져오거나 설정합니다. |

### 예

다음 예는 다음을 사용하여 문서 미리보기를 가져오는 방법을 보여줍니다.[`PreviewOptions`](../previewoptions) 그리고[`CreatePageStream`](./createpagestream) 대리자.

다음 예는 다음을 사용하여 문서 미리보기를 가져오는 방법을 보여줍니다.[`PreviewOptions`](../previewoptions) 그리고 두 대의원.

```csharp
    CreatePageStream createDelegate = delegate (int pageNumber)
    {
        var pagePath = System.IO.Path.Combine(@"C:\Temp", string.Format("page_{0}.png", pageNumber));
        return System.IO.File.Create(pagePath);
    };
    var previewOptions = new PreviewOptions(createDelegate);
    previewOptions.PreviewFormat = PreviewOptions.PreviewFormats.PNG;
    previewOptions.Height = 640;
    previewOptions.Width = 480;
    previewOptions.PageNumbers = new int[] { 1 };
    using (var redactor = new Redactor("C:\Temp\SourceFile.pdf"))
    {
        redactor.GeneratePreview(previewOptions);
    }
```

```csharp
    CreatePageStream createDelegate = delegate (int pageNumber)
    {
        var pagePath = System.IO.Path.Combine(@"C:\Temp", string.Format("page_{0}.png", pageNumber));
        return System.IO.File.Create(pagePath);
    };
    ReleasePageStream releaseDelegate = delegate (int pageNumber, System.IO.Stream pageStream)
    {
        // 페이지 미리보기를 포함하는 Stream으로 무엇이든 수행
        pageStream.Close();
    };
    var previewOptions = new PreviewOptions(createDelegate, releaseDelegate);
    previewOptions.PreviewFormat = PreviewOptions.PreviewFormats.PNG;
    previewOptions.Height = 640;
    previewOptions.Width = 480;
    previewOptions.PageNumbers = new int[] { 1 };
    using (var redactor = new Redactor("C:\Temp\SourceFile.pdf"))
    {
        redactor.GeneratePreview(previewOptions);
    }
```

### 또한보십시오

* 네임스페이스 [GroupDocs.Redaction.Options](../../groupdocs.redaction.options)
* 집회 [GroupDocs.Redaction](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Redaction.dll -->
