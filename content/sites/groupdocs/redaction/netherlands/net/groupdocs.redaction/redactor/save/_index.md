---
title: Save
second_title: GroupDocs.Redaction voor .NET API-referentie
description: Slaat het document op in een bestand met de volgende opties AddSuffix  true RasterizeToPDF  true.
type: docs
weight: 60
url: /nl/net/groupdocs.redaction/redactor/save/
---
## Save() {#save}

Slaat het document op in een bestand met de volgende opties: AddSuffix = true, RasterizeToPDF = true.

```csharp
public string Save()
```

### Winstwaarde

Pad naar geredigeerd document

### Zie ook

* class [Redactor](../../redactor)
* naamruimte [GroupDocs.Redaction](../../redactor)
* montage [GroupDocs.Redaction](../../../)

---

## Save(SaveOptions) {#save_1}

Slaat het document op in een bestand.

```csharp
public string Save(SaveOptions saveOptions)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| saveOptions | SaveOptions | Opties om een achtervoegsel toe te voegen of te rasteren |

### Winstwaarde

Pad naar geredigeerd document

### Voorbeelden

Het volgende voorbeeld laat zien hoe u een document opslaat met SaveOptions.

```csharp
    using (Redactor redactor = new Redactor(@"C:\sample.pdf"))
    {
       // Documentredactie komt hier
       // ...
    
       // Sla het document op met standaardopties (pagina's converteren naar afbeeldingen, opslaan als PDF)
       redactor.Save();
    
       // Sla het document op in het originele formaat en overschrijf het originele bestand
       redactor.Save(new SaveOptions() { AddSuffix = false, RasterizeToPDF = false });
    
       // Sla het document op in het bestand "*_Redacted.*" in de originele indeling
       redactor.Save(new SaveOptions() { AddSuffix = true, RasterizeToPDF = false });
    
       // Sla het document op in "*_AnyText.*" (bijv. tijdstempel in plaats van "AnyText") in de bestandsnaam zonder rastering
       redactor.Save(new SaveOptions(false, "AnyText"));
    }    
```

### Zie ook

* class [SaveOptions](../../../groupdocs.redaction.options/saveoptions)
* class [Redactor](../../redactor)
* naamruimte [GroupDocs.Redaction](../../redactor)
* montage [GroupDocs.Redaction](../../../)

---

## Save(Stream, RasterizationOptions) {#save_2}

Slaat het document op in een stream, inclusief aangepaste locatie.

```csharp
public void Save(Stream document, RasterizationOptions rasterizationOptions)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| document | Stream | Doelstroom |
| rasterizationOptions | RasterizationOptions | Opties om al dan niet te rasteren en om pagina's op te geven voor rastering |

### Voorbeelden

Het volgende voorbeeld laat zien hoe u opties instelt voor het rasterisatieproces.

```csharp
    using (var redactor = new Redactor("SomePresentation.pptx"))
    {
        // verwerk gevoelige gegevens op de eerste dia 
    
        var rasterizationOptions = new RasterizationOptions();
        rasterizationOptions.PageIndex = 0;
        rasterizationOptions.PageCount = 1;
        rasterizationOptions.Compliance = PdfComplianceLevel.PdfA1a;
        using (var stream = File.Open(Path.Combine(@"C:\Temp", "PresentationFirstSlide.pdf")))
        {
            redactor.Save(stream, rasterizationOptions);
        }
    }      
```

### Zie ook

* class [RasterizationOptions](../../../groupdocs.redaction.options/rasterizationoptions)
* class [Redactor](../../redactor)
* naamruimte [GroupDocs.Redaction](../../redactor)
* montage [GroupDocs.Redaction](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Redaction.dll -->
