---
title: CreatePageStream
second_title: Référence de l'API GroupDocs.Redaction pour .NET
description: Représente la méthode qui renvoie un flux pour écrire les données daperçu de la page.
type: docs
weight: 290
url: /fr/net/groupdocs.redaction.options/createpagestream/
---
## CreatePageStream delegate

Représente la méthode qui renvoie un flux pour écrire les données d'aperçu de la page.

```csharp
public delegate Stream CreatePageStream(int pageNumber);
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| pageNumber | Int32 | Numéro de page d'une page pour générer la vignette |

### Return_Value

Diffuser pour écrire un aperçu de la page

### Exemples

L'exemple suivant montre comment obtenir un aperçu de document à l'aide de[`PreviewOptions`](../previewoptions) et[`CreatePageStream`](../createpagestream) déléguer.

```csharp
    CreatePageStream createDelegate = delegate (int pageNumber)
    {
        var pagePath = System.IO.Path.Combine(@"C:\Temp", string.Format("page_{0}.png", pageNumber));
        return System.IO.File.Create(pagePath);
    };
    var previewOptions = new PreviewOptions(createDelegate);
    previewOptions.PreviewFormat = PreviewOptions.PreviewFormats.PNG;
    previewOptions.Height = 640;
    previewOptions.Width = 480;
    previewOptions.PageNumbers = new int[] { 1 };
    using (var redactor = new Redactor("C:\Temp\SourceFile.pdf"))
    {
        redactor.GeneratePreview(previewOptions);
    }
```

### Voir également

* espace de noms [GroupDocs.Redaction.Options](../../groupdocs.redaction.options)
* Assemblée [GroupDocs.Redaction](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Redaction.dll -->
