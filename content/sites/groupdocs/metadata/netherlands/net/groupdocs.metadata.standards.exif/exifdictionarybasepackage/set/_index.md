---
title: Set
second_title: GroupDocs.Metadata voor .NET API-referentie
description: Voegt de opgegeven tag toe of vervangt deze.
type: docs
weight: 40
url: /nl/net/groupdocs.metadata.standards.exif/exifdictionarybasepackage/set/
---
## ExifDictionaryBasePackage.Set method

Voegt de opgegeven tag toe of vervangt deze.

```csharp
public void Set(TiffTag tag)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| tag | TiffTag | De tag die moet worden ingesteld. |

### Voorbeelden

Dit codevoorbeeld laat zien hoe u een aangepaste tag toevoegt aan een EXIF-pakket.

```csharp
using (Metadata metadata = new Metadata(Constants.TiffWithExif))
{
    IExif root = metadata.GetRootPackage() as IExif;
    if (root != null)
    {
        // Stel het EXIF-pakket in als het ontbreekt
        if (root.ExifPackage == null)
        {
            root.ExifPackage = new ExifPackage();
        }

        // Voeg een bekende eigenschap toe
        root.ExifPackage.Set(new TiffAsciiTag(TiffTagID.Artist, "test artist"));

        // Voeg een volledig aangepaste eigenschap toe (die niet wordt beschreven in de EXIF-specificatie).
        // Houd er rekening mee dat de gekozen ID kan kruisen met de ID's die door sommige tools van derden worden gebruikt.
        root.ExifPackage.Set(new TiffAsciiTag((TiffTagID)65523, "custom"));

        metadata.Save(Constants.OutputTiff);
    }
}
```

### Zie ook

* class [TiffTag](../../../groupdocs.metadata.formats.image/tifftag)
* class [ExifDictionaryBasePackage](../../exifdictionarybasepackage)
* naamruimte [GroupDocs.Metadata.Standards.Exif](../../exifdictionarybasepackage)
* montage [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
