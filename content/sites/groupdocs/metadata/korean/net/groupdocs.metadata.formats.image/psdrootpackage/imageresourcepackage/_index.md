---
title: ImageResourcePackage
second_title: .NET API 참조용 GroupDocs.Metadata
description: Photoshop 이미지 리소스 메타데이터 패키지를 가져옵니다. 이미지 리소스 블록은 Photoshop 기본 파일 형식의 기본 구성 단위입니다.
type: docs
weight: 20
url: /ko/net/groupdocs.metadata.formats.image/psdrootpackage/imageresourcepackage/
---
## PsdRootPackage.ImageResourcePackage property

Photoshop 이미지 리소스 메타데이터 패키지를 가져옵니다. 이미지 리소스 블록은 Photoshop 기본 파일 형식의 기본 구성 단위입니다.

```csharp
public ImageResourcePackage ImageResourcePackage { get; }
```

### 자산 가치

이미지 리소스 메타데이터 패키지.

### 비고

**더 알아보기**

* [PSD 이미지의 메타데이터 작업](https://docs.groupdocs.com/display/metadatanet/Working+with+metadata+in+PSD+images)

### 예

아래 코드 샘플은 PSD 이미지에서 이미지 리소스 블록(Photoshop 파일 형식의 빌딩 블록)을 추출하는 방법을 보여줍니다.

```csharp
using (Metadata metadata = new Metadata(Constants.PsdWithIrb))
{
    var root = metadata.GetRootPackage<PsdRootPackage>();

    if (root.ImageResourcePackage != null)
    {
        foreach (var block in root.ImageResourcePackage.ToList())
        {
            Console.WriteLine(block.Signature);
            Console.WriteLine(block.ID);
            Console.WriteLine(block.Name);
            Console.WriteLine(block.Data);
        }
    }
}
```

### 또한보십시오

* class [ImageResourcePackage](../../imageresourcepackage)
* class [PsdRootPackage](../../psdrootpackage)
* 네임스페이스 [GroupDocs.Metadata.Formats.Image](../../psdrootpackage)
* 집회 [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
