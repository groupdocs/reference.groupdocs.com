---
title: IIptc
second_title: GroupDocs.Metadata για Αναφορά API .NET
description: Αντιπροσωπεύει βασικές λειτουργίες που προορίζονται να λειτουργήσουν με μεταδεδομένα IPTC. Βρείτε περισσότερες πληροφορίες στοhttp//en.wikipedia.org/wiki/International_Press_Telecommunications_Councilhttp//en.wikipedia.org/wiki/International_Press_Telecommunications_Council .
type: docs
weight: 2870
url: /el/net/groupdocs.metadata.standards.iptc/iiptc/
---
## IIptc interface

Αντιπροσωπεύει βασικές λειτουργίες που προορίζονται να λειτουργήσουν με μεταδεδομένα IPTC. Βρείτε περισσότερες πληροφορίες στο[http://en.wikipedia.org/wiki/International_Press_Telecommunications_Council](http://en.wikipedia.org/wiki/International_Press_Telecommunications_Council) .

```csharp
public interface IIptc
```

## Ιδιότητες

| Ονομα | Περιγραφή |
| --- | --- |
| [IptcPackage](../../groupdocs.metadata.standards.iptc/iiptc/iptcpackage) { get; set; } | Λαμβάνει ή ορίζει το πακέτο μεταδεδομένων IPTC που σχετίζεται με το αρχείο. |

### Παρατηρήσεις

**Μάθε περισσότερα**

* [Εργασία με μεταδεδομένα IPTC IIM](https://docs.groupdocs.com/display/metadatanet/Working+with+IPTC+IIM+metadata)

### Παραδείγματα

Αυτό το παράδειγμα δείχνει πώς να διαβάζετε τις βασικές ιδιότητες μεταδεδομένων IPTC.

```csharp
using (Metadata metadata = new Metadata(Constants.JpegWithIptc))
{
    IIptc root = metadata.GetRootPackage() as IIptc;
    if (root != null && root.IptcPackage != null)
    {
        if (root.IptcPackage.EnvelopeRecord != null)
        {
            Console.WriteLine(root.IptcPackage.EnvelopeRecord.DateSent);
            Console.WriteLine(root.IptcPackage.EnvelopeRecord.Destination);
            Console.WriteLine(root.IptcPackage.EnvelopeRecord.FileFormat);
            Console.WriteLine(root.IptcPackage.EnvelopeRecord.FileFormatVersion);

            //...
        }

        if (root.IptcPackage.ApplicationRecord != null)
        {
            Console.WriteLine(root.IptcPackage.ApplicationRecord.Headline);
            Console.WriteLine(root.IptcPackage.ApplicationRecord.ByLine);
            Console.WriteLine(root.IptcPackage.ApplicationRecord.ByLineTitle);
            Console.WriteLine(root.IptcPackage.ApplicationRecord.CaptionAbstract);
            Console.WriteLine(root.IptcPackage.ApplicationRecord.City);
            Console.WriteLine(root.IptcPackage.ApplicationRecord.DateCreated);
            Console.WriteLine(root.IptcPackage.ApplicationRecord.ReleaseDate);

            //...
        }
    }
}
```

### Δείτε επίσης

* χώρος ονομάτων [GroupDocs.Metadata.Standards.Iptc](../../groupdocs.metadata.standards.iptc)
* συνέλευση [GroupDocs.Metadata](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
