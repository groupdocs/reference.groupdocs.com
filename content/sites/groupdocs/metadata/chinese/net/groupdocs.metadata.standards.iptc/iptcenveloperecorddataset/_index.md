---
title: IptcEnvelopeRecordDataSet
second_title: GroupDocs.Metadata for .NET API 参考
description: 定义 IPTC 信封记录数据集编号
type: docs
weight: 2920
url: /zh/net/groupdocs.metadata.standards.iptc/iptcenveloperecorddataset/
---
## IptcEnvelopeRecordDataSet enumeration

定义 IPTC 信封记录数据集编号。

```csharp
public enum IptcEnvelopeRecordDataSet
```

### 价值观

| 姓名 | 价值 | 描述 |
| --- | --- | --- |
| ModelVersion | `0` | 标识信息版本的二进制数  交换模型，第 I 部分，由供应商使用。版本号由 IPTC 和 NAA 分配。  此记录的版本号为四 (4). |
| Destination | `5` | 可选、可重复、最多 1024 个八位字节，由顺序连续的图形字符组成。  这个数据集是为了满足一些需要在适当的 OSI 层之上路由信息的供应商。 |
| FileFormat | `20` | 文件格式. |
| FileFormatVersion | `22` | 强制性的，不可重复的，两个八位字节。  表示 1:20. 中指定的文件格式的特定版本的二进制数 文件格式列表，包括版本交叉引用，包含在附录 A. |
| ServiceIdentifier | `30` | 强制，不可重复。最多 10 个八位字节，由图形字符组成。  标识供应商和产品。 |
| EnvelopeNumber | `40` | 强制，不可重复，八个八位字节，由数字字符组成。  这些字符构成一个数字，该数字对于 1:70 中指定的日期 和 1:30. 中指定的服务标识符是唯一的 如果相同的信封号码出现在相同的日期和 以及相同的服务标识符，则记录 2-9 必须与原始记录相同 。  这不是一个连续的 serial 号码接收检查。 |
| ProductID | `50` | 可选，可重复。最多 32 个八位字节，由图形字符组成。  允许提供商识别其整体服务的子集。  用于提供选择、路由或以其他方式处理数据的接收组织数据。 |
| EnvelopePriority | `60` | 可选，不可重复。一个八位字节，由一个数字字符组成。  指定信封处理优先级而非编辑紧急程度（请参阅 2:10，紧急程度）。 '1' 表示最紧急， '5' 表示正常紧急， 和 '8' 表示最不紧急。 数字“9”表示用户定义的优先级。 数字“0”保留供将来使用。 |
| DateSent | `70` | 强制，不可重复。八个八位字节，由数字字符组成。  使用 ISO 8601 中定义的格式 CCYYMMDD（世纪、年、月、日）来表示服务发送材料的年、月和日。 |
| TimeSent | `80` | 使用 HHMMSS±HHMM 格式，其中 HHMMSS 指 本地小时、分钟和秒，HHMM 指提前 (+) 或晚 (-) 通用协调时间的小时和 分钟，如 ISO 8601 中所述。这是服务发送 the 的时间材料. |
| CodedCharacterSet | `90` | 可选，不可重复，最多 32 个八位字节，由一个或 个控制函数组成，用于通知、调用或 编码字符集的指定。控制功能遵循 ISO 2022 标准，可能由转义控制 字符和一个或多个图形字符组成。有关详细信息 ，请参阅附录 C，IPTC-NAA 代码库。 |
| Uno | `100` | 无效（永久标识符）。 |
| ArmIdentifier | `120` | 数据集标识抽象关系方法 (ARM)，它在 ARM 的发起者向 IPTC 和 NAA 注册的文档中描述 。 |
| ArmVersion | `122` | 表示数据集 1:120. 中指定的特定 ARM 版本的二进制数 |

### 也可以看看

* 命名空间 [GroupDocs.Metadata.Standards.Iptc](../../groupdocs.metadata.standards.iptc)
* 部件 [GroupDocs.Metadata](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
