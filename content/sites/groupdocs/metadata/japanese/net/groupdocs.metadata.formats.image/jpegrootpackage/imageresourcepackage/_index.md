---
title: ImageResourcePackage
second_title: GroupDocs.Metadata for .NET API リファレンス
description: Photoshop 画像リソース メタデータ パッケージを取得します 画像リソース ブロックはPhotoshop ネイティブ ファイル形式の基本的な構成単位です
type: docs
weight: 20
url: /ja/net/groupdocs.metadata.formats.image/jpegrootpackage/imageresourcepackage/
---
## JpegRootPackage.ImageResourcePackage property

Photoshop 画像リソース メタデータ パッケージを取得します。 画像リソース ブロックは、Photoshop ネイティブ ファイル形式の基本的な構成単位です。

```csharp
public ImageResourcePackage ImageResourcePackage { get; }
```

### プロパティ値

画像リソース メタデータ パッケージ.

### 備考

**もっと詳しく知る**

* [JPEG 画像のメタデータの操作](https://docs.groupdocs.com/display/metadatanet/Working+with+metadata+in+JPEG+images)

### 例

以下のコード サンプルは、JPEG 画像から画像リソース ブロック (Photoshop ファイル形式の構成要素) を抽出する方法を示しています。

```csharp
using (Metadata metadata = new Metadata(Constants.JpegWithIrb))
{
    var root = metadata.GetRootPackage<JpegRootPackage>();

    if (root.ImageResourcePackage != null)
    {
        foreach (var block in root.ImageResourcePackage.ToList())
        {
            Console.WriteLine(block.Signature);
            Console.WriteLine(block.ID);
            Console.WriteLine(block.Name);
            Console.WriteLine(block.Data);
        }
    }
}
```

### 関連項目

* class [ImageResourcePackage](../../imageresourcepackage)
* class [JpegRootPackage](../../jpegrootpackage)
* 名前空間 [GroupDocs.Metadata.Formats.Image](../../jpegrootpackage)
* 組み立て [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
