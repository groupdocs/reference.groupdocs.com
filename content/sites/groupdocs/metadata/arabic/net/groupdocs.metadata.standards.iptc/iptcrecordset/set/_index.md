---
title: Set
second_title: GroupDocs.Metadata لمرجع .NET API
description: إضافة أو تحديث البيانات المحددة في السجل المناسب.
type: docs
weight: 80
url: /ar/net/groupdocs.metadata.standards.iptc/iptcrecordset/set/
---
## IptcRecordSet.Set method

إضافة أو تحديث البيانات المحددة في السجل المناسب.

```csharp
public void Set(IptcDataSet dataSet)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| dataSet | IptcDataSet | مجموعة بيانات IPTC المراد إضافتها / تحديثها. |

### أمثلة

يوضح هذا المثال كيفية إضافة أو تحديث مجموعات بيانات IPTC المخصصة في ملف.

```csharp
using (Metadata metadata = new Metadata(Constants.PsdWithIptc))
{
    IIptc root = metadata.GetRootPackage() as IIptc;
    if (root != null)
    {
        // قم بتعيين حزمة IPTC إذا كانت مفقودة
        if (root.IptcPackage == null)
        {
            root.IptcPackage = new IptcRecordSet();
        }

        // إضافة خاصية معرفة باستخدام DataSet API
        root.IptcPackage.Set(new IptcDataSet((byte) IptcRecordType.ApplicationRecord, (byte) IptcApplicationRecordDataSet.BylineTitle, "test code sample"));

        // أضف مجموعة بيانات IPTC مخصصة
        root.IptcPackage.Set(new IptcDataSet(255, 255, new byte[] { 1, 2, 3 }));

        metadata.Save(Constants.OutputPsd);
    }
}
```

### أنظر أيضا

* class [IptcDataSet](../../iptcdataset)
* class [IptcRecordSet](../../iptcrecordset)
* مساحة الاسم [GroupDocs.Metadata.Standards.Iptc](../../iptcrecordset)
* المجسم [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
