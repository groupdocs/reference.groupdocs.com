---
title: Sanitize
second_title: Riferimento API GroupDocs.Metadata per .NET
description: Rimuove le proprietà dei metadati scrivibili da tutti i pacchetti rilevati o da interi pacchetti se possibile. Loperazione è ricorsiva quindi interessa anche tutti i pacchetti annidati.
type: docs
weight: 100
url: /it/net/groupdocs.metadata/metadata/sanitize/
---
## Metadata.Sanitize method

Rimuove le proprietà dei metadati scrivibili da tutti i pacchetti rilevati o da interi pacchetti, se possibile. L'operazione è ricorsiva quindi interessa anche tutti i pacchetti annidati.

```csharp
public int Sanitize()
```

### Valore di ritorno

Il numero di proprietà interessate.

### Osservazioni

**Saperne di più**

* [Metadati puliti](https://docs.groupdocs.com/display/metadatanet/Clean+metadata)

### Esempi

Questo esempio mostra come rimuovere tutti i pacchetti/proprietà di metadati rilevati da un file.

```csharp
using (Metadata metadata = new Metadata(Constants.InputPdf))
{
    // Rimuovi i pacchetti di metadati rilevati
    var affected = metadata.Sanitize();
    Console.WriteLine("Properties removed: {0}", affected);

    metadata.Save(Constants.OutputPdf);
}
```

### Guarda anche

* class [Metadata](../../metadata)
* spazio dei nomi [GroupDocs.Metadata](../../metadata)
* assemblea [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
