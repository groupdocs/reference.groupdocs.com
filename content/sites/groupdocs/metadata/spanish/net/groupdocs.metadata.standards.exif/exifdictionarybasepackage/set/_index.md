---
title: Set
second_title: Referencia de API de GroupDocs.Metadata para .NET
description: Agrega o reemplaza la etiqueta especificada.
type: docs
weight: 40
url: /es/net/groupdocs.metadata.standards.exif/exifdictionarybasepackage/set/
---
## ExifDictionaryBasePackage.Set method

Agrega o reemplaza la etiqueta especificada.

```csharp
public void Set(TiffTag tag)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| tag | TiffTag | La etiqueta a configurar. |

### Ejemplos

Este ejemplo de código demuestra cómo agregar una etiqueta personalizada a un paquete EXIF.

```csharp
using (Metadata metadata = new Metadata(Constants.TiffWithExif))
{
    IExif root = metadata.GetRootPackage() as IExif;
    if (root != null)
    {
        // Establecer el paquete EXIF si falta
        if (root.ExifPackage == null)
        {
            root.ExifPackage = new ExifPackage();
        }

        // Agregar una propiedad conocida
        root.ExifPackage.Set(new TiffAsciiTag(TiffTagID.Artist, "test artist"));

        // Agregue una propiedad totalmente personalizada (que no se describe en la especificación EXIF).
        // Tenga en cuenta que la identificación elegida puede cruzarse con las identificaciones utilizadas por algunas herramientas de terceros.
        root.ExifPackage.Set(new TiffAsciiTag((TiffTagID)65523, "custom"));

        metadata.Save(Constants.OutputTiff);
    }
}
```

### Ver también

* class [TiffTag](../../../groupdocs.metadata.formats.image/tifftag)
* class [ExifDictionaryBasePackage](../../exifdictionarybasepackage)
* espacio de nombres [GroupDocs.Metadata.Standards.Exif](../../exifdictionarybasepackage)
* asamblea [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
