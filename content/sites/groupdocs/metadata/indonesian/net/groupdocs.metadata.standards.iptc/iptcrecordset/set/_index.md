---
title: Set
second_title: GroupDocs.Metadata untuk Referensi .NET API
description: Menambahkan atau memperbarui kumpulan data yang ditentukan dalam catatan yang sesuai.
type: docs
weight: 80
url: /id/net/groupdocs.metadata.standards.iptc/iptcrecordset/set/
---
## IptcRecordSet.Set method

Menambahkan atau memperbarui kumpulan data yang ditentukan dalam catatan yang sesuai.

```csharp
public void Set(IptcDataSet dataSet)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| dataSet | IptcDataSet | Set data IPTC untuk ditambahkan/diperbarui. |

### Contoh

Contoh ini menunjukkan cara menambah atau memperbarui kumpulan data IPTC khusus dalam sebuah file.

```csharp
using (Metadata metadata = new Metadata(Constants.PsdWithIptc))
{
    IIptc root = metadata.GetRootPackage() as IIptc;
    if (root != null)
    {
        // Tetapkan paket IPTC jika tidak ada
        if (root.IptcPackage == null)
        {
            root.IptcPackage = new IptcRecordSet();
        }

        // Tambahkan properti yang diketahui menggunakan DataSet API
        root.IptcPackage.Set(new IptcDataSet((byte) IptcRecordType.ApplicationRecord, (byte) IptcApplicationRecordDataSet.BylineTitle, "test code sample"));

        // Tambahkan Kumpulan Data IPTC khusus
        root.IptcPackage.Set(new IptcDataSet(255, 255, new byte[] { 1, 2, 3 }));

        metadata.Save(Constants.OutputPsd);
    }
}
```

### Lihat juga

* class [IptcDataSet](../../iptcdataset)
* class [IptcRecordSet](../../iptcrecordset)
* ruang nama [GroupDocs.Metadata.Standards.Iptc](../../iptcrecordset)
* perakitan [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
