---
title: Set
second_title: .NET API संदर्भ के लिए GroupDocs.Metadata
description: उपयुक्त रकर्ड में नर्दष्ट डेटसेट क जड़त य अपडेट करत है
type: docs
weight: 80
url: /hi/net/groupdocs.metadata.standards.iptc/iptcrecordset/set/
---
## IptcRecordSet.Set method

उपयुक्त रिकॉर्ड में निर्दिष्ट डेटासेट को जोड़ता या अपडेट करता है।

```csharp
public void Set(IptcDataSet dataSet)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| dataSet | IptcDataSet | जोड़ने/अपडेट करने के लिए IPTC डेटासेट। |

### उदाहरण

यह उदाहरण दिखाता है कि किसी फ़ाइल में कस्टम IPTC डेटासेट कैसे जोड़ें या अपडेट करें।

```csharp
using (Metadata metadata = new Metadata(Constants.PsdWithIptc))
{
    IIptc root = metadata.GetRootPackage() as IIptc;
    if (root != null)
    {
        // IPTC पैकेज सेट करें यदि यह गायब है
        if (root.IptcPackage == null)
        {
            root.IptcPackage = new IptcRecordSet();
        }

        // डेटासेट एपीआई का उपयोग करके एक ज्ञात संपत्ति जोड़ें
        root.IptcPackage.Set(new IptcDataSet((byte) IptcRecordType.ApplicationRecord, (byte) IptcApplicationRecordDataSet.BylineTitle, "test code sample"));

        // एक कस्टम IPTC डेटासेट जोड़ें
        root.IptcPackage.Set(new IptcDataSet(255, 255, new byte[] { 1, 2, 3 }));

        metadata.Save(Constants.OutputPsd);
    }
}
```

### यह सभी देखें

* class [IptcDataSet](../../iptcdataset)
* class [IptcRecordSet](../../iptcrecordset)
* नाम स्थान [GroupDocs.Metadata.Standards.Iptc](../../iptcrecordset)
* सभा [GroupDocs.Metadata](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Metadata.dll -->
