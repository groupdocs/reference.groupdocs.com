---
title: StatusChanged
second_title: GroupDocs.Search efter .NET API Reference
description: Uppstår när indexstatus ändras.
type: docs
weight: 80
url: /sv/net/groupdocs.search.events/eventhub/statuschanged/
---
## EventHub.StatusChanged event

Uppstår när indexstatus ändras.

```csharp
public event EventHandler<BaseIndexEventArgs> StatusChanged;
```

### Exempel

Exemplet visar hur man använder händelsen.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";

// Skapa ett index
Index index = new Index(indexFolder);

// Prenumererar på evenemanget
index.Events.StatusChanged += (sender, args) =>
{
    if (args.Status != IndexStatus.InProgress)
    {
        // Ett meddelande om att operationen är slutförd bör finnas här
    }
};

// Inställning av flaggan för asynkron indexering
IndexingOptions options = new IndexingOptions();
options.IsAsync = true;

// Asynkron indexering av dokument från den angivna mappen
// Metoden avslutas innan operationen är klar
index.Add(documentsFolder, options);
```

### Se även

* class [BaseIndexEventArgs](../../baseindexeventargs)
* class [EventHub](../../eventhub)
* namnutrymme [GroupDocs.Search.Events](../../eventhub)
* hopsättning [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
