---
title: Create
second_title: GroupDocs.Search for .NET API 参考
description: 在内存中创建一个新索引
type: docs
weight: 50
url: /zh/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

在内存中创建一个新索引。

```csharp
public Index Create()
```

### 返回值

创建的索引。

### 例子

示例演示如何通过索引存储库在内存中创建索引。

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### 也可以看看

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* 命名空间 [GroupDocs.Search](../../indexrepository)
* 部件 [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

在内存中创建一个新索引。

```csharp
public Index Create(IndexSettings settings)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| settings | IndexSettings | 索引设置。 |

### 返回值

创建的索引。

### 例子

示例演示如何通过索引存储库在内存中创建索引。

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // 在索引期间禁用停用词

Index index = indexRepository.Create(settings);
```

### 也可以看看

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* 命名空间 [GroupDocs.Search](../../indexrepository)
* 部件 [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

在磁盘上创建一个新索引。 索引文件夹将在创建索引之前被清理。

```csharp
public Index Create(string indexFolder)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| indexFolder | String | 索引文件夹。 |

### 返回值

创建的索引。

### 例子

该示例演示了如何通过索引存储库在磁盘上创建索引。

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### 也可以看看

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* 命名空间 [GroupDocs.Search](../../indexrepository)
* 部件 [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

在磁盘上创建一个新索引。 索引文件夹将在创建索引之前被清理。

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| indexFolder | String | 索引文件夹。 |
| settings | IndexSettings | 索引设置。 |

### 返回值

创建的索引。

### 例子

该示例演示了如何通过索引存储库在磁盘上创建索引。

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // 在索引期间禁用停用词

Index index = indexRepository.Create(indexFolder, settings);
```

### 也可以看看

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* 命名空间 [GroupDocs.Search](../../indexrepository)
* 部件 [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
