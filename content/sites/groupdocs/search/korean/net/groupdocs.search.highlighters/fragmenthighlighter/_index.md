---
title: FragmentHighlighter
second_title: GroupDocs..NET API 참조 검색
description: 텍스트 조각으로 검색 결과를 강조 표시하는 검색 결과 강조 표시를 나타냅니다.
type: docs
weight: 640
url: /ko/net/groupdocs.search.highlighters/fragmenthighlighter/
---
## FragmentHighlighter class

텍스트 조각으로 검색 결과를 강조 표시하는 검색 결과 강조 표시를 나타냅니다.

```csharp
public class FragmentHighlighter : Highlighter
```

## 생성자

| 이름 | 설명 |
| --- | --- |
| [FragmentHighlighter](fragmenthighlighter)(OutputFormat) | 의 새 인스턴스를 초기화합니다.[`FragmentHighlighter`](../fragmenthighlighter) 클래스. |

## 속성

| 이름 | 설명 |
| --- | --- |
| [OutputFormat](../../groupdocs.search.common/resultbuilderfactory/outputformat) { get; } | 출력 형식을 가져옵니다. |

## 행동 양식

| 이름 | 설명 |
| --- | --- |
| [GetResult](../../groupdocs.search.highlighters/fragmenthighlighter/getresult)() | 결과 조각 컨테이너의 배열을 가져옵니다. |

### 비고

**더 알아보기**

* [검색 결과 강조](https://docs.groupdocs.com/display/searchnet/Highlighting+search+results)

### 예

이 예제는 클래스의 일반적인 사용법을 보여줍니다.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";

// 인덱스 생성
Index index = new Index(indexFolder);

// 지정된 폴더에서 문서 인덱싱
index.Add(documentsFolder);

// '아인슈타인'이라는 단어 검색
SearchResult result = index.Search("Einstein");

// 하이라이트 옵션 할당
HighlightOptions options = new HighlightOptions();
options.TermsBefore = 5;
options.TermsAfter = 5;
options.TermsTotal = 15;

// 문서의 텍스트에서 찾은 단어 강조 표시
FoundDocument document = result.GetFoundDocument(0);
FragmentHighlighter highlighter = new FragmentHighlighter(OutputFormat.Html);
index.Highlight(document, highlighter, options);

// 결과 얻기
FragmentContainer[] fragmentContainers = highlighter.GetResult();
for (int i = 0; i < fragmentContainers.Length; i++)
{
    FragmentContainer container = fragmentContainers[i];
    string[] fragments = container.GetFragments();
    if (fragments.Length > 0)
    {
        Console.WriteLine(container.FieldName);
        Console.WriteLine();
        for (int j = 0; j < fragments.Length; j++)
        {
            // HTML 마크업을 콘솔에 출력
            Console.WriteLine(fragments[j]);
            Console.WriteLine();
        }
    }
}
```

### 또한보십시오

* class [Highlighter](../highlighter)
* 네임스페이스 [GroupDocs.Search.Highlighters](../../groupdocs.search.highlighters)
* 집회 [GroupDocs.Search](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
