---
title: Create
second_title: GroupDocs..NET API 참조 검색
description: 메모리에 새 인덱스를 만듭니다.
type: docs
weight: 50
url: /ko/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

메모리에 새 인덱스를 만듭니다.

```csharp
public Index Create()
```

### 반환 값

생성된 인덱스입니다.

### 예

이 예제는 인덱스 저장소를 통해 메모리에 인덱스를 생성하는 방법을 보여줍니다.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### 또한보십시오

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* 네임스페이스 [GroupDocs.Search](../../indexrepository)
* 집회 [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

메모리에 새 인덱스를 만듭니다.

```csharp
public Index Create(IndexSettings settings)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| settings | IndexSettings | 인덱스 설정입니다. |

### 반환 값

생성된 인덱스입니다.

### 예

이 예제는 인덱스 저장소를 통해 메모리에 인덱스를 생성하는 방법을 보여줍니다.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // 인덱싱 중 불용어 사용 비활성화

Index index = indexRepository.Create(settings);
```

### 또한보십시오

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* 네임스페이스 [GroupDocs.Search](../../indexrepository)
* 집회 [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

디스크에 새 인덱스를 생성합니다. 인덱스 생성 전에 인덱스 폴더를 정리합니다.

```csharp
public Index Create(string indexFolder)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| indexFolder | String | 색인 폴더. |

### 반환 값

생성된 인덱스입니다.

### 예

이 예제는 인덱스 저장소를 통해 디스크에 인덱스를 생성하는 방법을 보여줍니다.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### 또한보십시오

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* 네임스페이스 [GroupDocs.Search](../../indexrepository)
* 집회 [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

디스크에 새 인덱스를 생성합니다. 인덱스 생성 전에 인덱스 폴더를 정리합니다.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| indexFolder | String | 색인 폴더. |
| settings | IndexSettings | 인덱스 설정입니다. |

### 반환 값

생성된 인덱스입니다.

### 예

이 예제는 인덱스 저장소를 통해 디스크에 인덱스를 생성하는 방법을 보여줍니다.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // 인덱싱 중 불용어 사용 비활성화

Index index = indexRepository.Create(indexFolder, settings);
```

### 또한보십시오

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* 네임스페이스 [GroupDocs.Search](../../indexrepository)
* 집회 [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
