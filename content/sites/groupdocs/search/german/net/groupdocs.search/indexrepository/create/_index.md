---
title: Create
second_title: GroupDocs.Suche nach .NET-API-Referenz
description: Erstellt einen neuen Index im Speicher.
type: docs
weight: 50
url: /de/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Erstellt einen neuen Index im Speicher.

```csharp
public Index Create()
```

### Rückgabewert

Der erstellte Index.

### Beispiele

Das Beispiel zeigt, wie ein Index im Speicher über das Index-Repository erstellt wird.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Siehe auch

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* namensraum [GroupDocs.Search](../../indexrepository)
* Montage [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Erstellt einen neuen Index im Speicher.

```csharp
public Index Create(IndexSettings settings)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| settings | IndexSettings | Die Indexeinstellungen. |

### Rückgabewert

Der erstellte Index.

### Beispiele

Das Beispiel zeigt, wie ein Index im Speicher über das Index-Repository erstellt wird.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Verwendung von Stoppwörtern während der Indizierung deaktivieren

Index index = indexRepository.Create(settings);
```

### Siehe auch

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* namensraum [GroupDocs.Search](../../indexrepository)
* Montage [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Erstellt einen neuen Index auf der Festplatte. Der Indexordner wird vor der Indexerstellung bereinigt.

```csharp
public Index Create(string indexFolder)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| indexFolder | String | Der Indexordner. |

### Rückgabewert

Der erstellte Index.

### Beispiele

Das Beispiel zeigt, wie ein Index auf der Festplatte über das Index-Repository erstellt wird.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Siehe auch

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* namensraum [GroupDocs.Search](../../indexrepository)
* Montage [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Erstellt einen neuen Index auf der Festplatte. Der Indexordner wird vor der Indexerstellung bereinigt.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| indexFolder | String | Der Indexordner. |
| settings | IndexSettings | Die Indexeinstellungen. |

### Rückgabewert

Der erstellte Index.

### Beispiele

Das Beispiel zeigt, wie ein Index auf der Festplatte über das Index-Repository erstellt wird.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Verwendung von Stoppwörtern während der Indizierung deaktivieren

Index index = indexRepository.Create(indexFolder, settings);
```

### Siehe auch

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* namensraum [GroupDocs.Search](../../indexrepository)
* Montage [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
