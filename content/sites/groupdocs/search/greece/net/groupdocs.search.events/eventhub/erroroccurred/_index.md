---
title: ErrorOccurred
second_title: GroupDocs.Αναζήτηση για αναφορά API .NET
description: Εμφανίζεται όταν συμβαίνει ένα σφάλμα κατά τη διάρκεια μιας λειτουργίας ευρετηρίου.
type: docs
weight: 10
url: /el/net/groupdocs.search.events/eventhub/erroroccurred/
---
## EventHub.ErrorOccurred event

Εμφανίζεται όταν συμβαίνει ένα σφάλμα κατά τη διάρκεια μιας λειτουργίας ευρετηρίου.

```csharp
public event EventHandler<IndexErrorEventArgs> ErrorOccurred;
```

### Παραδείγματα

Το παράδειγμα δείχνει πώς να χρησιμοποιήσετε το συμβάν.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";
string query = "Einstein";

// Δημιουργία ευρετηρίου
Index index = new Index(indexFolder);

// Εγγραφή στην εκδήλωση
index.Events.ErrorOccurred += (sender, args) =>
{
    Console.WriteLine(args.Message);
};

// Δημιουργία ευρετηρίου εγγράφων από τον καθορισμένο φάκελο
index.Add(documentsFolder);

// Αναζήτηση στο ευρετήριο
SearchResult result = index.Search(query);
```

### Δείτε επίσης

* class [IndexErrorEventArgs](../../indexerroreventargs)
* class [EventHub](../../eventhub)
* χώρος ονομάτων [GroupDocs.Search.Events](../../eventhub)
* συνέλευση [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
