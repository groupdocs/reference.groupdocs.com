---
title: HtmlFragmentHighlighter
second_title: GroupDocs.Αναζήτηση για αναφορά API .NET
description: Αντιπροσωπεύει ένα εργαλείο επισήμανσης αποτελεσμάτων αναζήτησης που επισημαίνει τα αποτελέσματα αναζήτησης σε τμήματα κειμένου με μορφοποίηση HTML.
type: docs
weight: 660
url: /el/net/groupdocs.search.highlighters/htmlfragmenthighlighter/
---
## HtmlFragmentHighlighter class

Αντιπροσωπεύει ένα εργαλείο επισήμανσης αποτελεσμάτων αναζήτησης που επισημαίνει τα αποτελέσματα αναζήτησης σε τμήματα κειμένου με μορφοποίηση HTML.

```csharp
[Obsolete("Please, use the FragmentHighlighter class instead.")]
public class HtmlFragmentHighlighter : Highlighter
```

## Ιδιότητες

| Ονομα | Περιγραφή |
| --- | --- |
| [OutputFormat](../../groupdocs.search.common/resultbuilderfactory/outputformat) { get; } | Λαμβάνει τη μορφή εξόδου. |

### Παρατηρήσεις

**Μάθε περισσότερα**

* [Επισήμανση αποτελεσμάτων αναζήτησης](https://docs.groupdocs.com/display/searchnet/Highlighting+search+results)

### Παραδείγματα

Το παράδειγμα δείχνει μια τυπική χρήση της κλάσης.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";

// Δημιουργία ευρετηρίου
Index index = new Index(indexFolder);

// Δημιουργία ευρετηρίου εγγράφων από τον καθορισμένο φάκελο
index.Add(documentsFolder);

// Αναζήτηση για τη λέξη "Αϊνστάιν"
SearchResult result = index.Search("Einstein");

// Εκχώρηση επιλογών επισήμανσης
HighlightOptions options = new HighlightOptions();
options.TermsBefore = 5;
options.TermsAfter = 5;
options.TermsTotal = 15;

// Επισήμανση λέξεων που βρέθηκαν στο κείμενο ενός εγγράφου
FoundDocument document = result.GetFoundDocument(0);
HtmlFragmentHighlighter highlighter = new HtmlFragmentHighlighter();
index.Highlight(document, highlighter, options);

// Λήψη του αποτελέσματος
FragmentContainer[] fragmentContainers = highlighter.GetResult();
for (int i = 0; i < fragmentContainers.Length; i++)
{
    FragmentContainer container = fragmentContainers[i];
    string[] fragments = container.GetFragments();
    if (fragments.Length > 0)
    {
        Console.WriteLine(container.FieldName);
        Console.WriteLine();
        for (int j = 0; j < fragments.Length; j++)
        {
            // Εκτύπωση σήμανσης HTML στην κονσόλα
            Console.WriteLine(fragments[j]);
            Console.WriteLine();
        }
    }
}
```

### Δείτε επίσης

* class [Highlighter](../highlighter)
* χώρος ονομάτων [GroupDocs.Search.Highlighters](../../groupdocs.search.highlighters)
* συνέλευση [GroupDocs.Search](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
