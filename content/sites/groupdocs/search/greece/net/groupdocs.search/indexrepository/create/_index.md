---
title: Create
second_title: GroupDocs.Αναζήτηση για αναφορά API .NET
description: Δημιουργεί ένα νέο ευρετήριο στη μνήμη.
type: docs
weight: 50
url: /el/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Δημιουργεί ένα νέο ευρετήριο στη μνήμη.

```csharp
public Index Create()
```

### Επιστρεφόμενη Αξία

Το ευρετήριο που δημιουργήθηκε.

### Παραδείγματα

Το παράδειγμα δείχνει πώς να δημιουργήσετε ένα ευρετήριο στη μνήμη μέσω του αποθετηρίου ευρετηρίου.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Δείτε επίσης

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* χώρος ονομάτων [GroupDocs.Search](../../indexrepository)
* συνέλευση [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Δημιουργεί ένα νέο ευρετήριο στη μνήμη.

```csharp
public Index Create(IndexSettings settings)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| settings | IndexSettings | Οι ρυθμίσεις ευρετηρίου. |

### Επιστρεφόμενη Αξία

Το ευρετήριο που δημιουργήθηκε.

### Παραδείγματα

Το παράδειγμα δείχνει πώς να δημιουργήσετε ένα ευρετήριο στη μνήμη μέσω του αποθετηρίου ευρετηρίου.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Απενεργοποίηση χρήσης λέξεων τερματισμού κατά τη δημιουργία ευρετηρίου

Index index = indexRepository.Create(settings);
```

### Δείτε επίσης

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* χώρος ονομάτων [GroupDocs.Search](../../indexrepository)
* συνέλευση [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Δημιουργεί ένα νέο ευρετήριο στο δίσκο. Ο φάκελος ευρετηρίου θα καθαριστεί πριν από τη δημιουργία ευρετηρίου.

```csharp
public Index Create(string indexFolder)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| indexFolder | String | Ο φάκελος ευρετηρίου. |

### Επιστρεφόμενη Αξία

Το ευρετήριο που δημιουργήθηκε.

### Παραδείγματα

Το παράδειγμα δείχνει πώς να δημιουργήσετε ένα ευρετήριο στο δίσκο μέσω του αποθετηρίου ευρετηρίου.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Δείτε επίσης

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* χώρος ονομάτων [GroupDocs.Search](../../indexrepository)
* συνέλευση [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Δημιουργεί ένα νέο ευρετήριο στο δίσκο. Ο φάκελος ευρετηρίου θα καθαριστεί πριν από τη δημιουργία ευρετηρίου.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| indexFolder | String | Ο φάκελος ευρετηρίου. |
| settings | IndexSettings | Οι ρυθμίσεις ευρετηρίου. |

### Επιστρεφόμενη Αξία

Το ευρετήριο που δημιουργήθηκε.

### Παραδείγματα

Το παράδειγμα δείχνει πώς να δημιουργήσετε ένα ευρετήριο στο δίσκο μέσω του αποθετηρίου ευρετηρίου.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Απενεργοποίηση χρήσης λέξεων τερματισμού κατά τη δημιουργία ευρετηρίου

Index index = indexRepository.Create(indexFolder, settings);
```

### Δείτε επίσης

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* χώρος ονομάτων [GroupDocs.Search](../../indexrepository)
* συνέλευση [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
