---
title: TextStorageSettings
second_title: GroupDocs.Αναζήτηση για αναφορά API .NET
description: Λαμβάνει ή ορίζει τις ρυθμίσεις αποθήκευσης κειμένου. Η προεπιλεγμένη τιμή είναιμηδενικό  που σημαίνει ότι τα κείμενα των εγγράφων δεν αποθηκεύονται.
type: docs
weight: 110
url: /el/net/groupdocs.search/indexsettings/textstoragesettings/
---
## IndexSettings.TextStorageSettings property

Λαμβάνει ή ορίζει τις ρυθμίσεις αποθήκευσης κειμένου. Η προεπιλεγμένη τιμή είναι`μηδενικό` , που σημαίνει ότι τα κείμενα των εγγράφων δεν αποθηκεύονται.

```csharp
public TextStorageSettings TextStorageSettings { get; set; }
```

### Αξία περιουσίας

Ρυθμίσεις αποθήκευσης κειμένου.

### Παραδείγματα

Το παράδειγμα δείχνει πώς να ορίσετε τις ρυθμίσεις αποθήκευσης κειμένου.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";

// Δημιουργία παράδειγμα ρυθμίσεων ευρετηρίου
IndexSettings settings = new IndexSettings();
settings.TextStorageSettings = new TextStorageSettings(Compression.High); // Ρύθμιση υψηλού επιπέδου συμπίεσης για την αποθήκευση κειμένου ευρετηρίου

// Δημιουργία ευρετηρίου στον καθορισμένο φάκελο
Index index = new Index(indexFolder, settings);

// Ευρετηρίαση εγγράφων
index.Add(documentsFolder);

// Αναζήτηση
SearchResult result = index.Search("Einstein");
```

### Δείτε επίσης

* class [TextStorageSettings](../../../groupdocs.search.options/textstoragesettings)
* class [IndexSettings](../../indexsettings)
* χώρος ονομάτων [GroupDocs.Search](../../indexsettings)
* συνέλευση [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
