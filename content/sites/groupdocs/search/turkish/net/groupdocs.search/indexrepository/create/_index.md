---
title: Create
second_title: .NET API Başvurusu için GroupDocs.Search
description: Bellekte yeni bir dizin oluşturur.
type: docs
weight: 50
url: /tr/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Bellekte yeni bir dizin oluşturur.

```csharp
public Index Create()
```

### Geri dönüş değeri

Oluşturulan dizin.

### Örnekler

Örnek, dizin deposu aracılığıyla bellekte bir dizinin nasıl oluşturulacağını gösterir.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Ayrıca bakınız

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* ad alanı [GroupDocs.Search](../../indexrepository)
* toplantı [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Bellekte yeni bir dizin oluşturur.

```csharp
public Index Create(IndexSettings settings)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| settings | IndexSettings | Dizin ayarları. |

### Geri dönüş değeri

Oluşturulan dizin.

### Örnekler

Örnek, dizin deposu aracılığıyla bellekte bir dizinin nasıl oluşturulacağını gösterir.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // İndeksleme sırasında durdurma kelimelerinin kullanımını devre dışı bırakma

Index index = indexRepository.Create(settings);
```

### Ayrıca bakınız

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* ad alanı [GroupDocs.Search](../../indexrepository)
* toplantı [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Diskte yeni bir dizin oluşturur. Dizin klasörü, dizin oluşturmadan önce temizlenecektir.

```csharp
public Index Create(string indexFolder)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| indexFolder | String | Dizin klasörü. |

### Geri dönüş değeri

Oluşturulan dizin.

### Örnekler

Örnek, dizin deposu aracılığıyla diskte bir dizinin nasıl oluşturulacağını gösterir.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Ayrıca bakınız

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* ad alanı [GroupDocs.Search](../../indexrepository)
* toplantı [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Diskte yeni bir dizin oluşturur. Dizin klasörü, dizin oluşturmadan önce temizlenecektir.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| indexFolder | String | Dizin klasörü. |
| settings | IndexSettings | Dizin ayarları. |

### Geri dönüş değeri

Oluşturulan dizin.

### Örnekler

Örnek, dizin deposu aracılığıyla diskte bir dizinin nasıl oluşturulacağını gösterir.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // İndeksleme sırasında durdurma kelimelerinin kullanımını devre dışı bırakma

Index index = indexRepository.Create(indexFolder, settings);
```

### Ayrıca bakınız

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* ad alanı [GroupDocs.Search](../../indexrepository)
* toplantı [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
