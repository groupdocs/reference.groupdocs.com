---
title: Create
second_title: GroupDocs. Zoek naar .NET API-referentie
description: Creëert een nieuwe index in het geheugen.
type: docs
weight: 50
url: /nl/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Creëert een nieuwe index in het geheugen.

```csharp
public Index Create()
```

### Winstwaarde

De aangemaakte index.

### Voorbeelden

Het voorbeeld laat zien hoe u een index in het geheugen maakt via de indexrepository.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Zie ook

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* naamruimte [GroupDocs.Search](../../indexrepository)
* montage [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Creëert een nieuwe index in het geheugen.

```csharp
public Index Create(IndexSettings settings)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| settings | IndexSettings | De indexinstellingen. |

### Winstwaarde

De aangemaakte index.

### Voorbeelden

Het voorbeeld laat zien hoe u een index in het geheugen maakt via de indexrepository.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Gebruik van stopwoorden tijdens indexering uitschakelen

Index index = indexRepository.Create(settings);
```

### Zie ook

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* naamruimte [GroupDocs.Search](../../indexrepository)
* montage [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Maakt een nieuwe index op schijf. De indexmap wordt opgeschoond voordat de index wordt gemaakt.

```csharp
public Index Create(string indexFolder)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| indexFolder | String | De indexmap. |

### Winstwaarde

De aangemaakte index.

### Voorbeelden

Het voorbeeld laat zien hoe u een index op schijf maakt via de indexrepository.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Zie ook

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* naamruimte [GroupDocs.Search](../../indexrepository)
* montage [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Maakt een nieuwe index op schijf. De indexmap wordt opgeschoond voordat de index wordt gemaakt.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| indexFolder | String | De indexmap. |
| settings | IndexSettings | De indexinstellingen. |

### Winstwaarde

De aangemaakte index.

### Voorbeelden

Het voorbeeld laat zien hoe u een index op schijf maakt via de indexrepository.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Gebruik van stopwoorden tijdens indexering uitschakelen

Index index = indexRepository.Create(indexFolder, settings);
```

### Zie ook

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* naamruimte [GroupDocs.Search](../../indexrepository)
* montage [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
