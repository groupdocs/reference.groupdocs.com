---
title: GetIndexedDocumentItems
second_title: GroupDocs. Zoek naar .NET API-referentie
description: Haalt een reeks geneste items op van het opgegeven document voor containerdocumenten zoals ZIP OST PST.
type: docs
weight: 130
url: /nl/net/groupdocs.search/index/getindexeddocumentitems/
---
## Index.GetIndexedDocumentItems method

Haalt een reeks geneste items op van het opgegeven document (voor containerdocumenten zoals ZIP, OST, PST).

```csharp
public DocumentInfo[] GetIndexedDocumentItems(DocumentInfo documentInfo)
```

| Parameter | Type | Beschrijving |
| --- | --- | --- |
| documentInfo | DocumentInfo | De documentinfo. |

### Winstwaarde

Een array van documentitems.

### Voorbeelden

Het voorbeeld laat zien hoe u een lijst met items van een geïndexeerd document kunt ophalen uit een index.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";
 
// Een index maken in de opgegeven map
Index index = new Index(indexFolder);
 
// Documenten uit de opgegeven map indexeren
index.Add(documentsFolder);
 
// Lijst met geïndexeerde documenten ophalen
DocumentInfo[] documents = index.GetIndexedDocuments();
for (int i = 0; i < documents.Length; i++)
{
    DocumentInfo document = documents[i];
    Console.WriteLine(document.FilePath);
    DocumentInfo[] items = index.GetIndexedDocumentItems(document); // Lijst met documentitems ophalen
    for (int j = 0; j < items.Length; j++)
    {
        DocumentInfo item = items[j];
        Console.WriteLine("\t" + item.InnerPath);
    }
}
```

### Zie ook

* class [DocumentInfo](../../../groupdocs.search.results/documentinfo)
* class [Index](../../index)
* naamruimte [GroupDocs.Search](../../index)
* montage [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
