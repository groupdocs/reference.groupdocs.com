---
title: FragmentHighlighter
second_title: GroupDocs. ابحث عن مرجع .NET API
description: يمثل أداة تمييز نتائج البحث التي تبرز نتائج البحث في أجزاء النص.
type: docs
weight: 640
url: /ar/net/groupdocs.search.highlighters/fragmenthighlighter/
---
## FragmentHighlighter class

يمثل أداة تمييز نتائج البحث التي تبرز نتائج البحث في أجزاء النص.

```csharp
public class FragmentHighlighter : Highlighter
```

## المنشئون

| اسم | وصف |
| --- | --- |
| [FragmentHighlighter](fragmenthighlighter)(OutputFormat) | يقوم بتهيئة مثيل جديد لملف[`FragmentHighlighter`](../fragmenthighlighter) فئة . |

## الخصائص

| اسم | وصف |
| --- | --- |
| [OutputFormat](../../groupdocs.search.common/resultbuilderfactory/outputformat) { get; } | يحصل على تنسيق الإخراج . |

## طُرق

| اسم | وصف |
| --- | --- |
| [GetResult](../../groupdocs.search.highlighters/fragmenthighlighter/getresult)() | الحصول على صفيف من حاويات الأجزاء الناتجة. |

### ملاحظات

**يتعلم أكثر**

* [إبراز نتائج البحث](https://docs.groupdocs.com/display/searchnet/Highlighting+search+results)

### أمثلة

يوضح المثال استخدامًا نموذجيًا للفئة.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";

// إنشاء فهرس
Index index = new Index(indexFolder);

// فهرسة المستندات من المجلد المحدد
index.Add(documentsFolder);

// ابحث عن كلمة "أينشتاين"
SearchResult result = index.Search("Einstein");

// تعيين خيارات الإبراز
HighlightOptions options = new HighlightOptions();
options.TermsBefore = 5;
options.TermsAfter = 5;
options.TermsTotal = 15;

// إبراز الكلمات الموجودة في نص المستند
FoundDocument document = result.GetFoundDocument(0);
FragmentHighlighter highlighter = new FragmentHighlighter(OutputFormat.Html);
index.Highlight(document, highlighter, options);

// الحصول على النتيجة
FragmentContainer[] fragmentContainers = highlighter.GetResult();
for (int i = 0; i < fragmentContainers.Length; i++)
{
    FragmentContainer container = fragmentContainers[i];
    string[] fragments = container.GetFragments();
    if (fragments.Length > 0)
    {
        Console.WriteLine(container.FieldName);
        Console.WriteLine();
        for (int j = 0; j < fragments.Length; j++)
        {
            // طباعة ترميز HTML لوحدة التحكم
            Console.WriteLine(fragments[j]);
            Console.WriteLine();
        }
    }
}
```

### أنظر أيضا

* class [Highlighter](../highlighter)
* مساحة الاسم [GroupDocs.Search.Highlighters](../../groupdocs.search.highlighters)
* المجسم [GroupDocs.Search](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
