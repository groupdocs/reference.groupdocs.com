---
title: Create
second_title: GroupDocs. ابحث عن مرجع .NET API
description: إنشاء فهرس جديد في الذاكرة .
type: docs
weight: 50
url: /ar/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

إنشاء فهرس جديد في الذاكرة .

```csharp
public Index Create()
```

### قيمة الإرجاع

الفهرس الذي تم إنشاؤه.

### أمثلة

يوضح المثال كيفية إنشاء فهرس في الذاكرة من خلال مستودع الفهرس.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### أنظر أيضا

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* مساحة الاسم [GroupDocs.Search](../../indexrepository)
* المجسم [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

إنشاء فهرس جديد في الذاكرة .

```csharp
public Index Create(IndexSettings settings)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| settings | IndexSettings | إعدادات الفهرس. |

### قيمة الإرجاع

الفهرس الذي تم إنشاؤه.

### أمثلة

يوضح المثال كيفية إنشاء فهرس في الذاكرة من خلال مستودع الفهرس.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // تعطيل استخدام كلمات التوقف أثناء الفهرسة

Index index = indexRepository.Create(settings);
```

### أنظر أيضا

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* مساحة الاسم [GroupDocs.Search](../../indexrepository)
* المجسم [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

إنشاء فهرس جديد على القرص . سيتم تنظيف مجلد الفهرس قبل إنشاء الفهرس.

```csharp
public Index Create(string indexFolder)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| indexFolder | String | مجلد الفهرس. |

### قيمة الإرجاع

الفهرس الذي تم إنشاؤه.

### أمثلة

يوضح المثال كيفية إنشاء فهرس على القرص من خلال مستودع الفهرس.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### أنظر أيضا

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* مساحة الاسم [GroupDocs.Search](../../indexrepository)
* المجسم [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

إنشاء فهرس جديد على القرص . سيتم تنظيف مجلد الفهرس قبل إنشاء الفهرس.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| indexFolder | String | مجلد الفهرس. |
| settings | IndexSettings | إعدادات الفهرس. |

### قيمة الإرجاع

الفهرس الذي تم إنشاؤه.

### أمثلة

يوضح المثال كيفية إنشاء فهرس على القرص من خلال مستودع الفهرس.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // تعطيل استخدام كلمات التوقف أثناء الفهرسة

Index index = indexRepository.Create(indexFolder, settings);
```

### أنظر أيضا

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* مساحة الاسم [GroupDocs.Search](../../indexrepository)
* المجسم [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
