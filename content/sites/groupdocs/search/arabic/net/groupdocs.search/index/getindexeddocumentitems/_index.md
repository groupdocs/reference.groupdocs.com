---
title: GetIndexedDocumentItems
second_title: GroupDocs. ابحث عن مرجع .NET API
description: الحصول على مصفوفة من العناصر المتداخلة للمستند المحدد لمستندات الحاوية مثل ZIP و OST و PST .
type: docs
weight: 130
url: /ar/net/groupdocs.search/index/getindexeddocumentitems/
---
## Index.GetIndexedDocumentItems method

الحصول على مصفوفة من العناصر المتداخلة للمستند المحدد (لمستندات الحاوية مثل ZIP و OST و PST) .

```csharp
public DocumentInfo[] GetIndexedDocumentItems(DocumentInfo documentInfo)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| documentInfo | DocumentInfo | معلومات الوثيقة. |

### قيمة الإرجاع

مصفوفة من عناصر المستند.

### أمثلة

يوضح المثال كيفية الحصول على قائمة بعناصر مستند مفهرس من فهرس.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";
 
// إنشاء فهرس في المجلد المحدد
Index index = new Index(indexFolder);
 
// فهرسة المستندات من المجلد المحدد
index.Add(documentsFolder);
 
// الحصول على قائمة الوثائق المفهرسة
DocumentInfo[] documents = index.GetIndexedDocuments();
for (int i = 0; i < documents.Length; i++)
{
    DocumentInfo document = documents[i];
    Console.WriteLine(document.FilePath);
    DocumentInfo[] items = index.GetIndexedDocumentItems(document); // الحصول على قائمة بعناصر المستند
    for (int j = 0; j < items.Length; j++)
    {
        DocumentInfo item = items[j];
        Console.WriteLine("\t" + item.InnerPath);
    }
}
```

### أنظر أيضا

* class [DocumentInfo](../../../groupdocs.search.results/documentinfo)
* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
