---
title: Index
second_title: GroupDocs. ابحث عن مرجع .NET API
description: يقوم بتهيئة مثيل جديد لملفIndexgroupdocs.search/index فئة في الذاكرة.
type: docs
weight: 10
url: /ar/net/groupdocs.search/index/index/
---
## Index() {#constructor}

يقوم بتهيئة مثيل جديد لملف[`Index`](../../index) فئة في الذاكرة.

```csharp
public Index()
```

### أمثلة

يوضح المثال كيفية إنشاء فهرس في الذاكرة بدون حفظ الملفات على القرص.

```csharp
Index index = new Index(); 
```

### أنظر أيضا

* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

---

## Index(IndexSettings) {#constructor_1}

يقوم بتهيئة مثيل جديد لملف[`Index`](../../index) فئة في الذاكرة مع إعدادات فهرس معينة.

```csharp
public Index(IndexSettings settings)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| settings | IndexSettings | كائن إعدادات الفهرس. |

### أمثلة

يوضح المثال كيفية إنشاء فهرس في الذاكرة بدون حفظ الملفات على القرص باستخدام إعدادات فهرس معينة.

```csharp
IndexSettings settings = new IndexSettings();
settings.IndexType = IndexType.CompactIndex;
Index index = new Index(settings);
```

### أنظر أيضا

* class [IndexSettings](../../indexsettings)
* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

---

## Index(string) {#constructor_2}

يقوم بتهيئة مثيل جديد لملف[`Index`](../../index) class. إنشاء فهرس جديد أو فتح فهرس موجود على القرص.

```csharp
public Index(string indexFolder)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| indexFolder | String | مسار مجلد الفهرس. |

### أمثلة

يوضح المثال كيفية إنشاء فهرس على قرص أو فتح فهرس موجود.

```csharp
string indexFolder = @"c:\MyIndex\";
Index index = new Index(indexFolder); 
```

### أنظر أيضا

* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

---

## Index(string, IndexSettings) {#constructor_4}

يقوم بتهيئة مثيل جديد لملف[`Index`](../../index) class. ينشئ فهرسًا جديدًا بإعدادات معينة أو يفتح فهرسًا موجودًا على القرص.

```csharp
public Index(string indexFolder, IndexSettings settings)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| indexFolder | String | مسار مجلد الفهرس. |
| settings | IndexSettings | كائن إعدادات الفهرس. |

### أمثلة

يوضح المثال كيفية إنشاء فهرس على قرص بإعدادات فهرس معينة.

```csharp
string indexFolder = @"c:\MyIndex\";
IndexSettings settings = new IndexSettings();
settings.IndexType = IndexType.CompactIndex;
Index index = new Index(indexFolder, settings);
```

### أنظر أيضا

* class [IndexSettings](../../indexsettings)
* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

---

## Index(string, bool) {#constructor_3}

يقوم بتهيئة مثيل جديد لملف[`Index`](../../index) class. تحميل فهرس موجود من القرص إذا*overwriteIfExists* يكون`خطأ شنيع`؛ ينشئ فهرسًا جديدًا على القرص وإلا.

```csharp
public Index(string indexFolder, bool overwriteIfExists)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| indexFolder | String | مسار مجلد الفهرس. |
| overwriteIfExists | Boolean | علم الكتابة فوق مجلد الفهرس. |

### أمثلة

يوضح المثال كيفية إنشاء فهرس جديد في مجلد يحتوي بالفعل على فهرس آخر.

```csharp
string indexFolder = @"c:\MyIndex\";
Index index = new Index(indexFolder, true);
```

### أنظر أيضا

* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

---

## Index(string, IndexSettings, bool) {#constructor_5}

يقوم بتهيئة مثيل جديد لملف[`Index`](../../index) class. تحميل فهرس موجود من القرص إذا*overwriteIfExists* يكون`خطأ شنيع` ؛ ينشئ فهرسًا جديدًا على القرص بإعدادات فهرس معينة.

```csharp
public Index(string indexFolder, IndexSettings settings, bool overwriteIfExists)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| indexFolder | String | مسار مجلد الفهرس. |
| settings | IndexSettings | كائن إعدادات الفهرس. |
| overwriteIfExists | Boolean | علم الكتابة فوق مجلد الفهرس. |

### أمثلة

يوضح المثال كيفية إنشاء فهرس على قرص بإعدادات فهرس معينة.

```csharp
string indexFolder = @"c:\MyIndex\";
IndexSettings settings = new IndexSettings();
settings.IndexType = IndexType.CompactIndex;
Index index = new Index(indexFolder, settings, true);
```

### أنظر أيضا

* class [IndexSettings](../../indexsettings)
* class [Index](../../index)
* مساحة الاسم [GroupDocs.Search](../../index)
* المجسم [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
