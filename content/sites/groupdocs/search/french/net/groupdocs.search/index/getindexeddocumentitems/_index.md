---
title: GetIndexedDocumentItems
second_title: Référence de l'API GroupDocs.Search pour .NET
description: Obtient un tableau déléments imbriqués du document spécifié pour les documents conteneurs tels que ZIP OST PST.
type: docs
weight: 130
url: /fr/net/groupdocs.search/index/getindexeddocumentitems/
---
## Index.GetIndexedDocumentItems method

Obtient un tableau d'éléments imbriqués du document spécifié (pour les documents conteneurs tels que ZIP, OST, PST).

```csharp
public DocumentInfo[] GetIndexedDocumentItems(DocumentInfo documentInfo)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| documentInfo | DocumentInfo | Les informations sur le document. |

### Return_Value

Tableau d'éléments d'un document.

### Exemples

L'exemple montre comment obtenir une liste des éléments d'un document indexé à partir d'un index.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";
 
// Création d'un index dans le dossier spécifié
Index index = new Index(indexFolder);
 
// Indexation des documents du dossier spécifié
index.Add(documentsFolder);
 
// Récupère la liste des documents indexés
DocumentInfo[] documents = index.GetIndexedDocuments();
for (int i = 0; i < documents.Length; i++)
{
    DocumentInfo document = documents[i];
    Console.WriteLine(document.FilePath);
    DocumentInfo[] items = index.GetIndexedDocumentItems(document); // Récupération de la liste des éléments du document
    for (int j = 0; j < items.Length; j++)
    {
        DocumentInfo item = items[j];
        Console.WriteLine("\t" + item.InnerPath);
    }
}
```

### Voir également

* class [DocumentInfo](../../../groupdocs.search.results/documentinfo)
* class [Index](../../index)
* espace de noms [GroupDocs.Search](../../index)
* Assemblée [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
