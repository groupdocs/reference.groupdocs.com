---
title: Create
second_title: GroupDocs.Search для справочника API .NET
description: Создает новый индекс в памяти.
type: docs
weight: 50
url: /ru/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Создает новый индекс в памяти.

```csharp
public Index Create()
```

### Возвращаемое значение

Созданный индекс.

### Примеры

В примере показано, как создать индекс в памяти через репозиторий индексов.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Смотрите также

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* пространство имен [GroupDocs.Search](../../indexrepository)
* сборка [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Создает новый индекс в памяти.

```csharp
public Index Create(IndexSettings settings)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| settings | IndexSettings | Настройки индекса. |

### Возвращаемое значение

Созданный индекс.

### Примеры

В примере показано, как создать индекс в памяти через репозиторий индексов.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Отключение использования стоп-слов при индексации

Index index = indexRepository.Create(settings);
```

### Смотрите также

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* пространство имен [GroupDocs.Search](../../indexrepository)
* сборка [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Создает новый индекс на диске. Папка индекса будет очищена перед созданием индекса.

```csharp
public Index Create(string indexFolder)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| indexFolder | String | Индексная папка. |

### Возвращаемое значение

Созданный индекс.

### Примеры

В примере показано, как создать индекс на диске через репозиторий индексов.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Смотрите также

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* пространство имен [GroupDocs.Search](../../indexrepository)
* сборка [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Создает новый индекс на диске. Папка индекса будет очищена перед созданием индекса.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| indexFolder | String | Индексная папка. |
| settings | IndexSettings | Настройки индекса. |

### Возвращаемое значение

Созданный индекс.

### Примеры

В примере показано, как создать индекс на диске через репозиторий индексов.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Отключение использования стоп-слов при индексации

Index index = indexRepository.Create(indexFolder, settings);
```

### Смотрите также

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* пространство имен [GroupDocs.Search](../../indexrepository)
* сборка [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
