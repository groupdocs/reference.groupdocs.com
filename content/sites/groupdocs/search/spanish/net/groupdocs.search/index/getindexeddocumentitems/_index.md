---
title: GetIndexedDocumentItems
second_title: GroupDocs.Buscar referencia de API de .NET
description: Obtiene una matriz de elementos anidados del documento especificado para documentos contenedores como ZIP OST PST.
type: docs
weight: 130
url: /es/net/groupdocs.search/index/getindexeddocumentitems/
---
## Index.GetIndexedDocumentItems method

Obtiene una matriz de elementos anidados del documento especificado (para documentos contenedores como ZIP, OST, PST).

```csharp
public DocumentInfo[] GetIndexedDocumentItems(DocumentInfo documentInfo)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| documentInfo | DocumentInfo | La información del documento. |

### Valor_devuelto

Una matriz de elementos de un documento.

### Ejemplos

El ejemplo muestra cómo obtener una lista de elementos de un documento indexado a partir de un índice.

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";
 
// Creando un índice en la carpeta especificada
Index index = new Index(indexFolder);
 
// Indexación de documentos de la carpeta especificada
index.Add(documentsFolder);
 
// Obtener la lista de documentos indexados
DocumentInfo[] documents = index.GetIndexedDocuments();
for (int i = 0; i < documents.Length; i++)
{
    DocumentInfo document = documents[i];
    Console.WriteLine(document.FilePath);
    DocumentInfo[] items = index.GetIndexedDocumentItems(document); // Obtener la lista de elementos del documento
    for (int j = 0; j < items.Length; j++)
    {
        DocumentInfo item = items[j];
        Console.WriteLine("\t" + item.InnerPath);
    }
}
```

### Ver también

* class [DocumentInfo](../../../groupdocs.search.results/documentinfo)
* class [Index](../../index)
* espacio de nombres [GroupDocs.Search](../../index)
* asamblea [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
