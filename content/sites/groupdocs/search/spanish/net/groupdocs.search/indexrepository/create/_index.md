---
title: Create
second_title: GroupDocs.Buscar referencia de API de .NET
description: Crea un nuevo índice en memoria.
type: docs
weight: 50
url: /es/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Crea un nuevo índice en memoria.

```csharp
public Index Create()
```

### Valor_devuelto

El índice creado.

### Ejemplos

El ejemplo demuestra cómo crear un índice en la memoria a través del repositorio de índices.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Ver también

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* espacio de nombres [GroupDocs.Search](../../indexrepository)
* asamblea [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Crea un nuevo índice en memoria.

```csharp
public Index Create(IndexSettings settings)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| settings | IndexSettings | La configuración del índice. |

### Valor_devuelto

El índice creado.

### Ejemplos

El ejemplo demuestra cómo crear un índice en la memoria a través del repositorio de índices.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Deshabilitar el uso de palabras vacías durante la indexación

Index index = indexRepository.Create(settings);
```

### Ver también

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* espacio de nombres [GroupDocs.Search](../../indexrepository)
* asamblea [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Crea un nuevo índice en el disco. La carpeta del índice se limpiará antes de la creación del índice.

```csharp
public Index Create(string indexFolder)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| indexFolder | String | La carpeta índice. |

### Valor_devuelto

El índice creado.

### Ejemplos

El ejemplo muestra cómo crear un índice en el disco a través del repositorio de índices.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Ver también

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* espacio de nombres [GroupDocs.Search](../../indexrepository)
* asamblea [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Crea un nuevo índice en el disco. La carpeta del índice se limpiará antes de la creación del índice.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| indexFolder | String | La carpeta índice. |
| settings | IndexSettings | La configuración del índice. |

### Valor_devuelto

El índice creado.

### Ejemplos

El ejemplo muestra cómo crear un índice en el disco a través del repositorio de índices.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Deshabilitar el uso de palabras vacías durante la indexación

Index index = indexRepository.Create(indexFolder, settings);
```

### Ver también

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* espacio de nombres [GroupDocs.Search](../../indexrepository)
* asamblea [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
