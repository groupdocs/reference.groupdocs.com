---
title: Create
second_title: GroupDocs.Mencari Referensi .NET API
description: Membuat indeks baru di memori.
type: docs
weight: 50
url: /id/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

Membuat indeks baru di memori.

```csharp
public Index Create()
```

### Nilai Pengembalian

Indeks yang dibuat.

### Contoh

Contoh menunjukkan cara membuat indeks di memori melalui repositori indeks.

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### Lihat juga

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* ruang nama [GroupDocs.Search](../../indexrepository)
* perakitan [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

Membuat indeks baru di memori.

```csharp
public Index Create(IndexSettings settings)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| settings | IndexSettings | Pengaturan indeks. |

### Nilai Pengembalian

Indeks yang dibuat.

### Contoh

Contoh menunjukkan cara membuat indeks di memori melalui repositori indeks.

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Menonaktifkan penggunaan stop word selama pengindeksan

Index index = indexRepository.Create(settings);
```

### Lihat juga

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* ruang nama [GroupDocs.Search](../../indexrepository)
* perakitan [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

Membuat indeks baru pada disk. Folder indeks akan dibersihkan sebelum pembuatan indeks.

```csharp
public Index Create(string indexFolder)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| indexFolder | String | Folder indeks. |

### Nilai Pengembalian

Indeks yang dibuat.

### Contoh

Contoh menunjukkan cara membuat indeks pada disk melalui repositori indeks.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### Lihat juga

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* ruang nama [GroupDocs.Search](../../indexrepository)
* perakitan [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

Membuat indeks baru pada disk. Folder indeks akan dibersihkan sebelum pembuatan indeks.

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| indexFolder | String | Folder indeks. |
| settings | IndexSettings | Pengaturan indeks. |

### Nilai Pengembalian

Indeks yang dibuat.

### Contoh

Contoh menunjukkan cara membuat indeks pada disk melalui repositori indeks.

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // Menonaktifkan penggunaan stop word selama pengindeksan

Index index = indexRepository.Create(indexFolder, settings);
```

### Lihat juga

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* ruang nama [GroupDocs.Search](../../indexrepository)
* perakitan [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
