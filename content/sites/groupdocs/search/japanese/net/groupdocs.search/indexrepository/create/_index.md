---
title: Create
second_title: GroupDocs.Search for .NET API リファレンス
description: メモリ内に新しいインデックスを作成します
type: docs
weight: 50
url: /ja/net/groupdocs.search/indexrepository/create/
---
## Create() {#create}

メモリ内に新しいインデックスを作成します。

```csharp
public Index Create()
```

### 戻り値

作成されたインデックス。

### 例

この例は、インデックス リポジトリを介してメモリ内にインデックスを作成する方法を示しています。

```csharp
IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create();
```

### 関連項目

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* 名前空間 [GroupDocs.Search](../../indexrepository)
* 組み立て [GroupDocs.Search](../../../)

---

## Create(IndexSettings) {#create_1}

メモリ内に新しいインデックスを作成します。

```csharp
public Index Create(IndexSettings settings)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| settings | IndexSettings | インデックス設定。 |

### 戻り値

作成されたインデックス。

### 例

この例は、インデックス リポジトリを介してメモリ内にインデックスを作成する方法を示しています。

```csharp
IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // インデックス作成中のストップ ワードの使用を無効にする

Index index = indexRepository.Create(settings);
```

### 関連項目

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* 名前空間 [GroupDocs.Search](../../indexrepository)
* 組み立て [GroupDocs.Search](../../../)

---

## Create(string) {#create_2}

ディスク上に新しいインデックスを作成します。 インデックス作成前にインデックス フォルダーを消去します。

```csharp
public Index Create(string indexFolder)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| indexFolder | String | インデックス フォルダー。 |

### 戻り値

作成されたインデックス。

### 例

この例は、インデックス リポジトリを使用してディスク上にインデックスを作成する方法を示しています。

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();
Index index = indexRepository.Create(indexFolder);
```

### 関連項目

* class [Index](../../index)
* class [IndexRepository](../../indexrepository)
* 名前空間 [GroupDocs.Search](../../indexrepository)
* 組み立て [GroupDocs.Search](../../../)

---

## Create(string, IndexSettings) {#create_3}

ディスク上に新しいインデックスを作成します。 インデックス作成前にインデックス フォルダーを消去します。

```csharp
public Index Create(string indexFolder, IndexSettings settings)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| indexFolder | String | インデックス フォルダー。 |
| settings | IndexSettings | インデックス設定。 |

### 戻り値

作成されたインデックス。

### 例

この例は、インデックス リポジトリを使用してディスク上にインデックスを作成する方法を示しています。

```csharp
string indexFolder = @"c:\MyIndex\";

IndexRepository indexRepository = new IndexRepository();

IndexSettings settings = new IndexSettings();
settings.UseStopWords = false; // インデックス作成中のストップ ワードの使用を無効にする

Index index = indexRepository.Create(indexFolder, settings);
```

### 関連項目

* class [Index](../../index)
* class [IndexSettings](../../indexsettings)
* class [IndexRepository](../../indexrepository)
* 名前空間 [GroupDocs.Search](../../indexrepository)
* 組み立て [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
