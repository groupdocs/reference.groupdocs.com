---
title: GetIndexedDocumentItems
second_title: GroupDocs.NET API संदर्भ के लिए खोजें
description: नर्दष्ट दस्तवेज़ के नेस्टेड आइटम क एक सरण प्रप्त करत है कंटेनर दस्तवेज़ जैसे ZIP OST PST के लए
type: docs
weight: 130
url: /hi/net/groupdocs.search/index/getindexeddocumentitems/
---
## Index.GetIndexedDocumentItems method

निर्दिष्ट दस्तावेज़ के नेस्टेड आइटम की एक सरणी प्राप्त करता है (कंटेनर दस्तावेज़ जैसे ZIP, OST, PST के लिए)।

```csharp
public DocumentInfo[] GetIndexedDocumentItems(DocumentInfo documentInfo)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| documentInfo | DocumentInfo | दस्तावेज़ की जानकारी। |

### प्रतिलाभ की मात्रा

एक दस्तावेज़ आइटम की एक सरणी।

### उदाहरण

उदाहरण दर्शाता है कि इंडेक्स से इंडेक्स किए गए दस्तावेज़ की वस्तुओं की सूची कैसे प्राप्त करें।

```csharp
string indexFolder = @"c:\MyIndex\";
string documentsFolder = @"c:\MyDocuments\";
 
// निर्दिष्ट फ़ोल्डर में एक इंडेक्स बनाना
Index index = new Index(indexFolder);
 
// निर्दिष्ट फ़ोल्डर से दस्तावेज़ अनुक्रमण
index.Add(documentsFolder);
 
// अनुक्रमित दस्तावेजों की सूची प्राप्त करना
DocumentInfo[] documents = index.GetIndexedDocuments();
for (int i = 0; i < documents.Length; i++)
{
    DocumentInfo document = documents[i];
    Console.WriteLine(document.FilePath);
    DocumentInfo[] items = index.GetIndexedDocumentItems(document); // दस्तावेज़ आइटम की सूची प्राप्त करना
    for (int j = 0; j < items.Length; j++)
    {
        DocumentInfo item = items[j];
        Console.WriteLine("\t" + item.InnerPath);
    }
}
```

### यह सभी देखें

* class [DocumentInfo](../../../groupdocs.search.results/documentinfo)
* class [Index](../../index)
* नाम स्थान [GroupDocs.Search](../../index)
* सभा [GroupDocs.Search](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Search.dll -->
