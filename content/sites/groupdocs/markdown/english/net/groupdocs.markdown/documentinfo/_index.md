---
title: DocumentInfo
second_title: GroupDocs.Markdown for .NET API Reference
description: Provides readonly metadata about a loaded document such as its format page count title and encryption status.
type: docs
weight: 70
url: /net/groupdocs.markdown/documentinfo/
---
## DocumentInfo class

Provides read-only metadata about a loaded document, such as its format, page count, title, and encryption status.

```csharp
public class DocumentInfo
```

## Properties

| Name | Description |
| --- | --- |
| [Author](../../groupdocs.markdown/documentinfo/author) { get; } | Gets the document author extracted from its metadata, or Empty if no author is available. |
| [FileFormat](../../groupdocs.markdown/documentinfo/fileformat) { get; } | Gets the detected file format of the document. |
| [IsEncrypted](../../groupdocs.markdown/documentinfo/isencrypted) { get; } | Gets a value indicating whether the document is password-protected. |
| [PageCount](../../groupdocs.markdown/documentinfo/pagecount) { get; } | Gets the number of pages in the document. For spreadsheets this returns the number of worksheets. |
| [Title](../../groupdocs.markdown/documentinfo/title) { get; } | Gets the document title extracted from its metadata, or Empty if no title is available. |

### Remarks

Use [`GetDocumentInfo`](../markdownconverter/getdocumentinfo) or [`GetInfo`](../markdownconverter/getinfo) to obtain an instance of this class. The information is extracted from the source document without performing a full conversion, so it is a lightweight way to inspect a file before converting it.

### Examples

Inspect a document before converting it:

```csharp
// Using the static helper
DocumentInfo info = MarkdownConverter.GetInfo("report.docx");
Console.WriteLine($"Format: {info.FileFormat}");
Console.WriteLine($"Pages:  {info.PageCount}");
Console.WriteLine($"Title:  {info.Title}");
Console.WriteLine($"Author: {info.Author}");

if (info.IsEncrypted)
    Console.WriteLine("The document is password-protected.");
```

Using the instance method:

```csharp
using (var converter = new MarkdownConverter("spreadsheet.xlsx"))
{
    DocumentInfo info = converter.GetDocumentInfo();
    Console.WriteLine($"Worksheets: {info.PageCount}");
}
```

### See Also

* namespace [GroupDocs.Markdown](../../groupdocs.markdown)
* assembly [GroupDocs.Markdown](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Markdown.dll -->
