---
title: MarkdownConverter
second_title: GroupDocs.Markdown for .NET API Reference
description: Converts various document formats to Markdown format.
type: docs
weight: 210
url: /net/groupdocs.markdown/markdownconverter/
---
## MarkdownConverter class

Converts various document formats to Markdown format.

```csharp
public class MarkdownConverter : IDisposable
```

## Constructors

| Name | Description |
| --- | --- |
| [MarkdownConverter](markdownconverter#constructor)(Stream) | Initializes a new instance of the [`MarkdownConverter`](../markdownconverter) class. |
| [MarkdownConverter](markdownconverter#constructor_2)(string) | Initializes a new instance of the [`MarkdownConverter`](../markdownconverter) class. |
| [MarkdownConverter](markdownconverter#constructor_1)(Stream, LoadOptions) | Initializes a new instance of the [`MarkdownConverter`](../markdownconverter) class. |
| [MarkdownConverter](markdownconverter#constructor_3)(string, LoadOptions) | Initializes a new instance of the [`MarkdownConverter`](../markdownconverter) class. |

## Methods

| Name | Description |
| --- | --- |
| [Convert](../../groupdocs.markdown/markdownconverter/convert#convert)() | Converts the source document to Markdown format and returns the result as a string. |
| [Convert](../../groupdocs.markdown/markdownconverter/convert#convert_1)(DocumentConverterOptions) | Converts the source document to Markdown format using the specified conversion options and returns the result as a string. |
| [Convert](../../groupdocs.markdown/markdownconverter/convert#convert_2)(Stream) | Converts the source document to Markdown format and writes the result to the specified output stream. |
| [Convert](../../groupdocs.markdown/markdownconverter/convert#convert_4)(string) | Converts the source document to Markdown format and saves it to the specified file path. |
| [Convert](../../groupdocs.markdown/markdownconverter/convert#convert_3)(Stream, DocumentConverterOptions) | Converts the source document to Markdown format using the specified conversion options and writes the result to the specified output stream. |
| [Convert](../../groupdocs.markdown/markdownconverter/convert#convert_5)(string, DocumentConverterOptions) | Converts the source document to Markdown format using the specified conversion options and saves it to the specified file path. |
| [Dispose](../../groupdocs.markdown/markdownconverter/dispose)() | Performs application-defined tasks associated with freeing, releasing, or resetting unmanaged resources. |

### Remarks

The MarkdownConverter class provides functionality to convert documents from various formats to Markdown. It supports conversion from popular document formats like Word (DOCX, DOC) and Excel (XLSX, XLS). The conversion process can be customized using [`DocumentConverterOptions`](../documentconverteroptions).

### Examples

The following example shows how to convert a Word document to Markdown:

```csharp
using (var converter = new MarkdownConverter("document.docx"))
{
    var result = converter.Convert();
    if (result.IsSuccess)
    {
        string markdown = result.Content;
        Console.WriteLine(markdown);
    }
}
```

The following example shows how to convert a password-protected document:

```csharp
var streamInfo = new StreamInfo
{
    Extension = ".docx",
    Password = "password123"
};

using (var converter = new MarkdownConverter("protected.docx", streamInfo))
{
    var result = converter.Convert();
    if (result.IsSuccess)
    {
        string markdown = result.Content;
        Console.WriteLine(markdown);
    }
}
```

### See Also

* namespace [GroupDocs.Markdown](../../groupdocs.markdown)
* assembly [GroupDocs.Markdown](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Markdown.dll -->
