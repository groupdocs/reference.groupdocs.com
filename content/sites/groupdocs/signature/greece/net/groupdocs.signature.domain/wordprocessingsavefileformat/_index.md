---
title: WordProcessingSaveFileFormat
second_title: GroupDocs.Signature για Αναφορά API .NET
description: Καθορίζει διαφορετικές μορφές αρχείων αποθήκευσης για έγγραφα Επεξεργασίας Word.
type: docs
weight: 1080
url: /el/net/groupdocs.signature.domain/wordprocessingsavefileformat/
---
## WordProcessingSaveFileFormat enumeration

Καθορίζει διαφορετικές μορφές αρχείων αποθήκευσης για έγγραφα Επεξεργασίας Word.

```csharp
public enum WordProcessingSaveFileFormat
```

### Αξίες

| Ονομα | αξία | Περιγραφή |
| --- | --- | --- |
| Default | `0` | Η μορφή αρχείου δεν θα αλλάξει. |
| Doc | `1` | Αποθηκεύει το έγγραφο ως έγγραφο του Microsoft Word 97-2003. |
| Dot | `2` | Αποθηκεύει το έγγραφο ως πρότυπο Microsoft Word 97-2003. |
| Docx | `3` | Αποθηκεύει το έγγραφο ως έγγραφο Microsoft Word OpenXML. |
| Docm | `4` | Αποθηκεύει το έγγραφο ως έγγραφο με δυνατότητα μακροεντολής του Microsoft Word OpenXML. |
| Dotx | `5` | Αποθηκεύει το έγγραφο ως πρότυπο Microsoft Word OpenXML. |
| Dotm | `6` | Αποθηκεύει το έγγραφο ως πρότυπο Microsoft Word OpenXML με δυνατότητα μακροεντολής. |
| FlatOpc | `24` | Αποθηκεύει το έγγραφο ως Office Open XML WordprocessingML που είναι αποθηκευμένο σε επίπεδο αρχείο XML αντί για πακέτο ZIP. |
| FlatOpcMacroEnabled | `25` | Αποθηκεύει το έγγραφο ως έγγραφο Office Open XML WordprocessingML με δυνατότητα μακροεντολής που είναι αποθηκευμένο σε επίπεδο αρχείο XML αντί για πακέτο ZIP. |
| FlatOpcTemplate | `26` | Αποθηκεύει το έγγραφο ως Πρότυπο Office Open XML WordprocessingML (χωρίς μακροεντολές) που είναι αποθηκευμένο σε επίπεδο αρχείο XML αντί για πακέτο ZIP. |
| FlatOpcTemplateMacroEnabled | `27` | Αποθηκεύει το έγγραφο ως Πρότυπο Office Open XML WordprocessingML με δυνατότητα Macro-Enabled σε ένα επίπεδο αρχείο XML αντί για πακέτο ZIP. |
| Rtf | `30` | Αποθηκεύει το έγγραφο σε μορφή RTF. Όλοι οι χαρακτήρες άνω των 7 bit έχουν διαφύγει ως δεκαεξαδικοί ή Unicode χαρακτήρες. |
| WordML | `31` | Αποθηκεύει το έγγραφο στη μορφή Microsoft Word 2003 WordprocessingML. |
| Pdf | `40` | Αποθηκεύει το έγγραφο σε μορφή PDF (Adobe Portable Document). |
| Xps | `41` | Αποθηκεύει το έγγραφο σε μορφή XPS (XML Paper Specification). |
| Ps | `47` | Αποθηκεύει το έγγραφο σε μορφή PS (PostScript). |
| Mhtml | `51` | Αποθηκεύει το έγγραφο σε μορφή MHTML (αρχείο Ιστού). |
| Epub | `52` | Αποθηκεύει το έγγραφο σε μορφή IDPF EPUB. |
| Odt | `60` | Αποθηκεύει το έγγραφο ως έγγραφο Open Office. |
| Ott | `61` | Αποθηκεύει το έγγραφο ως πρότυπο εγγράφου Open Office. |
| Text | `70` | Αποθηκεύει το έγγραφο σε μορφή απλού κειμένου. |
| Tiff | `100` | Αποδίδει μια σελίδα ή σελίδες του εγγράφου και τις αποθηκεύει σε ένα αρχείο TIFF μιας ή πολλών σελίδων. |
| Png | `101` | Αποδίδει μια σελίδα του εγγράφου και το αποθηκεύει ως αρχείο PNG. |
| Bmp | `102` | Αποδίδει μια σελίδα του εγγράφου και το αποθηκεύει ως αρχείο BMP. |
| Emf | `103` | Αποδίδει μια σελίδα του εγγράφου και το αποθηκεύει ως διανυσματικό αρχείο EMF (Enhanced Meta File). |
| Jpeg | `104` | Αποδίδει μια σελίδα του εγγράφου και το αποθηκεύει ως αρχείο JPEG. |

### Δείτε επίσης

* χώρος ονομάτων [GroupDocs.Signature.Domain](../../groupdocs.signature.domain)
* συνέλευση [GroupDocs.Signature](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Signature.dll -->
