---
title: Save
second_title: GroupDocs.Parser .NET API संदर्भ के लिए
description: छव क फ़इल में सहेजत है
type: docs
weight: 50
url: /hi/net/groupdocs.parser.data/pageimagearea/save/
---
## Save(string) {#save}

छवि को फ़ाइल में सहेजता है।

```csharp
public void Save(string filePath)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| filePath | String | फ़ाइल का पथ। |

### उदाहरण

निम्न उदाहरण दिखाता है कि छवियों को फ़ाइलों में कैसे सहेजना है:

```csharp
// पार्सर वर्ग का एक उदाहरण बनाएँ
using (Parser parser = new Parser(filePath))
{
    // दस्तावेज़ से छवियां निकालें
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // जांचें कि क्या छवि निष्कर्षण समर्थित है
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // छवियों पर पुनरावृति
    foreach (PageImageArea image in images)
    {
        // इमेज को फाइल में सेव करें
        image.Save(Guid.NewGuid().ToString() + image.FileType.Extension);
    }
}
```

### यह सभी देखें

* class [PageImageArea](../../pageimagearea)
* नाम स्थान [GroupDocs.Parser.Data](../../pageimagearea)
* सभा [GroupDocs.Parser](../../../)

---

## Save(string, ImageOptions) {#save_1}

छवि को एक अलग प्रारूप में फ़ाइल में सहेजता है।

```csharp
public void Save(string filePath, ImageOptions options)
```

| पैरामीटर | प्रकार | विवरण |
| --- | --- | --- |
| filePath | String | फ़ाइल का पथ। |
| options | ImageOptions | इमेज को सेव करने के लिए जिन विकल्पों का प्रयोग किया जाता है। |

### उदाहरण

निम्न उदाहरण दिखाता है कि छवियों को एक भिन्न प्रारूप में फ़ाइलों में कैसे सहेजना है:

```csharp
// पार्सर वर्ग का एक उदाहरण बनाएँ
using (Parser parser = new Parser(filePath))
{
    // दस्तावेज़ से छवियां निकालें
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // जांचें कि क्या छवि निष्कर्षण समर्थित है
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // पीएनजी प्रारूप में छवियों को बचाने के लिए विकल्प बनाएं
    ImageOptions options = new ImageOptions(ImageFormat.Png);
    
    // छवियों पर पुनरावृति
    foreach (PageImageArea image in images)
    {
        // इमेज को png फाइल में सेव करें
        image.Save(Guid.NewGuid().ToString() + ".png", options);
    }
}
```

### यह सभी देखें

* class [ImageOptions](../../../groupdocs.parser.options/imageoptions)
* class [PageImageArea](../../pageimagearea)
* नाम स्थान [GroupDocs.Parser.Data](../../pageimagearea)
* सभा [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
