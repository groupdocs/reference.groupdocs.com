---
title: ParseForm
second_title: GroupDocs.Parser .NET API संदर्भ के लिए
description: दस्तवेज़ फ़र्म क पर्स करत है.
type: docs
weight: 190
url: /hi/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

दस्तावेज़ फ़ॉर्म को पार्स करता है.

```csharp
public DocumentData ParseForm()
```

### प्रतिलाभ की मात्रा

का एक उदाहरण[`DocumentData`](../../../groupdocs.parser.data/documentdata) वह वर्ग जिसमें निकाला गया डेटा है; `व्यर्थ` यदि टेम्प्लेट द्वारा पार्सिंग समर्थित नहीं है.

### टिप्पणियों

**और अधिक जानें:**

* [पीडीएफ फॉर्म से डेटा निकालें](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [निकाले गए डेटा के साथ काम करना](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [पीडीएफ दस्तावेजों से डेटा पार्स करें](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### उदाहरण

निम्न उदाहरण दिखाता है कि दस्तावेज़ के एक रूप को कैसे पार्स किया जाए:

```csharp
// पार्सर वर्ग का एक उदाहरण बनाएँ
using (Parser parser = new Parser(filePath))
{
    // पीडीएफ दस्तावेज़ से डेटा निकालें
    DocumentData data = parser.ParseForm();
    // निकाले गए डेटा पर पुनरावृति करें
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### यह सभी देखें

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* नाम स्थान [GroupDocs.Parser](../../parser)
* सभा [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
