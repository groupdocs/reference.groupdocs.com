---
title: GetToc
second_title: GroupDocs.Parser .NET API संदर्भ के लिए
description: दस्तवेज़ से समग्र क तलक नकलत है
type: docs
weight: 170
url: /hi/net/groupdocs.parser/parser/gettoc/
---
## Parser.GetToc method

दस्तावेज़ से सामग्री की तालिका निकालता है।

```csharp
public IEnumerable<TocItem> GetToc()
```

### प्रतिलाभ की मात्रा

सामग्री मदों की तालिका का संग्रह; `व्यर्थ` यदि सामग्री निकालने की तालिका समर्थित नहीं है.

### टिप्पणियों

**और अधिक जानें:**

* [सामग्री की तालिका निकालें](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents)
* [सामग्री मद की तालिका द्वारा पाठ निकालें](https://docs.groupdocs.com/display/parsernet/Extract+text+by+table+of+contents+item)
* [Microsoft Office Word दस्तावेज़ों से सामग्री तालिका निकालें](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+Microsoft+Office+Word+documents)
* [EPUB eBooks से सामग्री तालिका निकालें](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+EPUB+eBooks)

### उदाहरण

निम्न उदाहरण दिखाता है कि CHM फ़ाइल से सामग्री की तालिका कैसे निकाली जाए:

```csharp
// पार्सर वर्ग का एक उदाहरण बनाएँ
using (Parser parser = new Parser(filePath))
{
    // जांचें कि टेक्स्ट निष्कर्षण समर्थित है या नहीं
    if (!parser.Features.Text)
    {
        Console.WriteLine("Text extraction isn't supported.");
        return;
    }

    // जांचें कि क्या toc निष्कर्षण समर्थित है
    if (!parser.Features.Toc)
    {
        Console.WriteLine("Toc extraction isn't supported.");
        return;
    }
 
    // सामग्री की तालिका प्राप्त करें
    IEnumerable<TocItem> toc = parser.GetToc();
    
    // वस्तुओं पर पुनरावृति
    foreach (TocItem i in toc)
    {
        // टॉक टेक्स्ट प्रिंट करें
        Console.WriteLine(i.Text);
        // जांचें कि क्या पेज इंडेक्स का कोई मूल्य है
        if (i.PageIndex == null)
        {
            continue;
        }
        // एक पेज टेक्स्ट निकालें
        using (TextReader reader = parser.GetText(i.PageIndex.Value))
        {
            Console.WriteLine(reader.ReadToEnd());
        }
    }
}
```

### यह सभी देखें

* class [TocItem](../../../groupdocs.parser.data/tocitem)
* class [Parser](../../parser)
* नाम स्थान [GroupDocs.Parser](../../parser)
* सभा [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
