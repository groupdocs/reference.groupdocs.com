---
title: TemplateTable
second_title: GroupDocs.Parser لمرجع .NET API
description: يقوم بتهيئة مثيل جديد لملفTemplateTablegroupdocs.parser.templates/templatetable فئة .
type: docs
weight: 10
url: /ar/net/groupdocs.parser.templates/templatetable/templatetable/
---
## TemplateTable(TemplateTableLayout, string, int?) {#constructor}

يقوم بتهيئة مثيل جديد لملف[`TemplateTable`](../../templatetable) فئة .

```csharp
public TemplateTable(TemplateTableLayout layout, string name, int? pageIndex)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| layout | TemplateTableLayout | تخطيط الجدول. |
| name | String | اسم الجدول. |
| pageIndex | Nullable`1 | فهرس الصفحة حيث يوجد جدول القالب ؛ `لا شيء` إذا كان جدول النموذج موجودًا في أي صفحة. |

### أمثلة

يتم تعيين جدول النموذج حسب تخطيط الجدول إذا تعذر اكتشاف الجدول تلقائيًا:

```csharp
TemplateTableLayout layout = new TemplateTableLayout(
    new double[] { 50, 95, 275 },
    new double[] { 325, 340, 365 });
 
TemplateTable table = new TemplateTable(layout, "Details", null);

// إنشاء قالب مستند
Template template = new Template(new TemplateItem[] { table });
```

### أنظر أيضا

* class [TemplateTableLayout](../../templatetablelayout)
* class [TemplateTable](../../templatetable)
* مساحة الاسم [GroupDocs.Parser.Templates](../../templatetable)
* المجسم [GroupDocs.Parser](../../../)

---

## TemplateTable(TemplateTableParameters, string, int?) {#constructor_1}

يقوم بتهيئة مثيل جديد لملف[`TemplateTable`](../../templatetable) فئة .

```csharp
public TemplateTable(TemplateTableParameters parameters, string name, int? pageIndex)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| parameters | TemplateTableParameters | المعلمات لاكتشاف الجدول في الوضع التلقائي. |
| name | String | اسم الجدول. |
| pageIndex | Nullable`1 | فهرس الصفحة حيث يوجد جدول القالب ؛ `لا شيء` إذا كان جدول النموذج موجودًا في أي صفحة. |

### أمثلة

إذا تم تعيين جدول قالب بواسطة معلمات الكاشف ، فسيتم اكتشاف الجدول تلقائيًا:

```csharp
TemplateTableParameters parameters = new TemplateTableParameters(
    new Rectangle(new Point(175, 350), new Size(400, 200)),
    new double[] { 185, 370, 425, 485, 545 });

TemplateTable table = new TemplateTable(parameters, "Details", 0);

// إنشاء قالب مستند
Template template = new Template(new TemplateItem[] { table });
```

### أنظر أيضا

* class [TemplateTableParameters](../../templatetableparameters)
* class [TemplateTable](../../templatetable)
* مساحة الاسم [GroupDocs.Parser.Templates](../../templatetable)
* المجسم [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
