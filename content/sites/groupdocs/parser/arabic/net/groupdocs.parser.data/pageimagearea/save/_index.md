---
title: Save
second_title: GroupDocs.Parser لمرجع .NET API
description: يحفظ الصورة في الملف .
type: docs
weight: 50
url: /ar/net/groupdocs.parser.data/pageimagearea/save/
---
## Save(string) {#save}

يحفظ الصورة في الملف .

```csharp
public void Save(string filePath)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| filePath | String | مسار الملف. |

### أمثلة

يوضح المثال التالي كيفية حفظ الصور في الملفات:

```csharp
// إنشاء مثيل لفئة المحلل اللغوي
using (Parser parser = new Parser(filePath))
{
    // استخراج الصور من الوثيقة
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // تحقق مما إذا كان استخراج الصور مدعومًا
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // تكرار على الصور
    foreach (PageImageArea image in images)
    {
        // احفظ الصورة في الملف
        image.Save(Guid.NewGuid().ToString() + image.FileType.Extension);
    }
}
```

### أنظر أيضا

* class [PageImageArea](../../pageimagearea)
* مساحة الاسم [GroupDocs.Parser.Data](../../pageimagearea)
* المجسم [GroupDocs.Parser](../../../)

---

## Save(string, ImageOptions) {#save_1}

يحفظ الصورة في الملف بتنسيق مختلف.

```csharp
public void Save(string filePath, ImageOptions options)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| filePath | String | مسار الملف. |
| options | ImageOptions | الخيارات المستخدمة لحفظ الصورة. |

### أمثلة

يوضح المثال التالي كيفية حفظ الصور في ملفات بتنسيق مختلف:

```csharp
// إنشاء مثيل لفئة المحلل اللغوي
using (Parser parser = new Parser(filePath))
{
    // استخراج الصور من الوثيقة
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // تحقق مما إذا كان استخراج الصور مدعومًا
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // قم بإنشاء خيارات حفظ الصور بتنسيق PNG
    ImageOptions options = new ImageOptions(ImageFormat.Png);
    
    // تكرار على الصور
    foreach (PageImageArea image in images)
    {
        // احفظ الصورة في ملف png
        image.Save(Guid.NewGuid().ToString() + ".png", options);
    }
}
```

### أنظر أيضا

* class [ImageOptions](../../../groupdocs.parser.options/imageoptions)
* class [PageImageArea](../../pageimagearea)
* مساحة الاسم [GroupDocs.Parser.Data](../../pageimagearea)
* المجسم [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
