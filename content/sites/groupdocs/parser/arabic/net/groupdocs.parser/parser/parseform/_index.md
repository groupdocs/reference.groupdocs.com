---
title: ParseForm
second_title: GroupDocs.Parser لمرجع .NET API
description: توزيع نموذج المستند.
type: docs
weight: 190
url: /ar/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

توزيع نموذج المستند.

```csharp
public DocumentData ParseForm()
```

### قيمة الإرجاع

مثيل[`DocumentData`](../../../groupdocs.parser.data/documentdata) فئة تحتوي على البيانات المستخرجة ؛ `باطل` إذا كان التحليل حسب النموذج غير مدعوم.

### ملاحظات

**يتعلم أكثر:**

* [استخراج البيانات من نماذج PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [العمل مع البيانات المستخرجة](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [تحليل البيانات من مستندات PDF](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### أمثلة

يوضح المثال التالي كيفية تحليل نموذج من المستند:

```csharp
// إنشاء مثيل لفئة المحلل اللغوي
using (Parser parser = new Parser(filePath))
{
    // استخراج البيانات من وثيقة PDF
    DocumentData data = parser.ParseForm();
    // تكرار البيانات المستخرجة
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### أنظر أيضا

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* مساحة الاسم [GroupDocs.Parser](../../parser)
* المجسم [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
