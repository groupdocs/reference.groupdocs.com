---
title: Features
second_title: Справочник по API GroupDocs.Parser для .NET
description: Получает поддерживаемые функции.
type: docs
weight: 20
url: /ru/net/groupdocs.parser/parser/features/
---
## Parser.Features property

Получает поддерживаемые функции.

```csharp
public Features Features { get; }
```

### Стоимость имущества

Экземпляр[`Features`](../../../groupdocs.parser.options/features) класс, представляющий поддерживаемые функции.

### Примечания

**Узнать больше:**

* [Получите поддерживаемые функции](https://docs.groupdocs.com/display/parsernet/Get+supported+features)

### Примеры

Если функция не поддерживается, метод возвращает`нулевой` вместо значения. Некоторые операции могут занимать значительное время. Поэтому не оптимально вызывать метод только для проверки поддержки функции. Для этого используется свойство Features:

```csharp
using(Parser parser = new Parser("doc.zip"))
{
    if(!parser.Features.Text)
    {
        Console.WriteLine("Text extraction isn't supported");
        return;
    }
 
    using(TextReader reader = parser.GetText())
    {
        Console.WriteLine(reader.ReadToEnd());
    }
}
```

### Смотрите также

* class [Features](../../../groupdocs.parser.options/features)
* class [Parser](../../parser)
* пространство имен [GroupDocs.Parser](../../parser)
* сборка [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
