---
title: ParseForm
second_title: Справочник по API GroupDocs.Parser для .NET
description: Разбирает форму документа.
type: docs
weight: 190
url: /ru/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Разбирает форму документа.

```csharp
public DocumentData ParseForm()
```

### Возвращаемое значение

Экземпляр[`DocumentData`](../../../groupdocs.parser.data/documentdata) класс, содержащий извлеченные данные; `нулевой` если разбор по шаблону не поддерживается.

### Примечания

**Учить больше:**

* [Извлечение данных из форм PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Работа с извлеченными данными](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Анализировать данные из PDF-документов](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Примеры

В следующем примере показано, как анализировать форму документа:

```csharp
// Создаем экземпляр класса Parser
using (Parser parser = new Parser(filePath))
{
    // Извлечение данных из документа PDF
    DocumentData data = parser.ParseForm();
    // Перебираем извлеченные данные
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Смотрите также

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* пространство имен [GroupDocs.Parser](../../parser)
* сборка [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
