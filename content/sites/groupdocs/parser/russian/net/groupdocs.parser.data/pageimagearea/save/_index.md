---
title: Save
second_title: Справочник по API GroupDocs.Parser для .NET
description: Сохраняет изображение в файл.
type: docs
weight: 50
url: /ru/net/groupdocs.parser.data/pageimagearea/save/
---
## Save(string) {#save}

Сохраняет изображение в файл.

```csharp
public void Save(string filePath)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| filePath | String | Путь к файлу. |

### Примеры

В следующем примере показано, как сохранять изображения в файлы:

```csharp
// Создаем экземпляр класса Parser
using (Parser parser = new Parser(filePath))
{
    // Извлечение изображений из документа
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // Проверяем, поддерживается ли извлечение изображений
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // Перебираем изображения
    foreach (PageImageArea image in images)
    {
        // Сохраняем изображение в файл
        image.Save(Guid.NewGuid().ToString() + image.FileType.Extension);
    }
}
```

### Смотрите также

* class [PageImageArea](../../pageimagearea)
* пространство имен [GroupDocs.Parser.Data](../../pageimagearea)
* сборка [GroupDocs.Parser](../../../)

---

## Save(string, ImageOptions) {#save_1}

Сохраняет изображение в файл в другом формате.

```csharp
public void Save(string filePath, ImageOptions options)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| filePath | String | Путь к файлу. |
| options | ImageOptions | Параметры, которые используются для сохранения изображения. |

### Примеры

В следующем примере показано, как сохранять изображения в файлы другого формата:

```csharp
// Создаем экземпляр класса Parser
using (Parser parser = new Parser(filePath))
{
    // Извлечение изображений из документа
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // Проверяем, поддерживается ли извлечение изображений
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // Создаем опции для сохранения изображений в формате PNG
    ImageOptions options = new ImageOptions(ImageFormat.Png);
    
    // Перебираем изображения
    foreach (PageImageArea image in images)
    {
        // Сохраняем изображение в файл png
        image.Save(Guid.NewGuid().ToString() + ".png", options);
    }
}
```

### Смотрите также

* class [ImageOptions](../../../groupdocs.parser.options/imageoptions)
* class [PageImageArea](../../pageimagearea)
* пространство имен [GroupDocs.Parser.Data](../../pageimagearea)
* сборка [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
