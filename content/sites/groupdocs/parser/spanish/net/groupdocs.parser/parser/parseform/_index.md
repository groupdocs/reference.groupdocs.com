---
title: ParseForm
second_title: Referencia de API de GroupDocs.Parser para .NET
description: Analiza el formulario del documento.
type: docs
weight: 190
url: /es/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Analiza el formulario del documento.

```csharp
public DocumentData ParseForm()
```

### Valor_devuelto

Una instancia de[`DocumentData`](../../../groupdocs.parser.data/documentdata) clase que contiene los datos extraídos; `nulo` si no se admite el análisis por plantilla.

### Observaciones

**Aprende más:**

* [Extraer datos de formularios PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Trabajar con datos extraídos](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Analizar datos de documentos PDF](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Ejemplos

El siguiente ejemplo muestra cómo analizar un formulario del documento:

```csharp
// Crea una instancia de la clase Parser
using (Parser parser = new Parser(filePath))
{
    // Extraer datos del documento PDF
    DocumentData data = parser.ParseForm();
    // Iterar sobre los datos extraídos
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Ver también

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* espacio de nombres [GroupDocs.Parser](../../parser)
* asamblea [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
