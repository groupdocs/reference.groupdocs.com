---
title: GetFieldsByName
second_title: Referencia de API de GroupDocs.Parser para .NET
description: Devuelve la colección de datos de campo donde el nombre es igual afieldName .
type: docs
weight: 50
url: /es/net/groupdocs.parser.data/documentdata/getfieldsbyname/
---
## DocumentData.GetFieldsByName method

Devuelve la colección de datos de campo donde el nombre es igual a*fieldName* .

```csharp
public IList<FieldData> GetFieldsByName(string fieldName)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| fieldName | String | El nombre del campo. |

### Valor_devuelto

Una coleccion de[`FieldData`](../../fielddata) objetos; colección vacía si no se encuentran datos de campo.

### Ejemplos

Buscar campos por un nombre de campo:

[`FieldData`](../../fielddata) clase representa datos de campo. Dependiendo del campo[`PageArea`](../../fielddata/pagearea) property puede contener cualquiera de los herederos de[`PageArea`](../../pagearea) clase. Por ejemplo,[`ParseForm`](../../../groupdocs.parser/parser/parseform) method extrae solo campos de texto.

```csharp
// Obtener todos los campos con el nombre "Dirección"
IList<FieldData> addressFields = data.GetFieldsByName("Address");
if(addressFields.Count == 0) {
    Console.WriteLine("Address not found");
}
else {
    Console.WriteLine("Address");
    // Iterar sobre la colección de campos
    for (int i = 0; i < addressFields.Count; i++) {
        PageTextArea area = addressFields[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);       
         
        // Si es un campo relacionado:
        if(addressFields[i].LinkedField != null) {
            Console.Write("Linked to ");
            PageTextArea linkedArea = addressFields[i].LinkedField.PageArea as PageTextArea;
            Console.WriteLine(area == null ? "Not a template field" : area.Text);           
        }
    }
}
```

### Ver también

* class [FieldData](../../fielddata)
* class [DocumentData](../../documentdata)
* espacio de nombres [GroupDocs.Parser.Data](../../documentdata)
* asamblea [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
