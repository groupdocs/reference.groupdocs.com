---
title: Item
second_title: .NET API 참조용 GroupDocs.Parser
description: 인덱스로 필드 데이터를 가져옵니다.
type: docs
weight: 30
url: /ko/net/groupdocs.parser.data/documentdata/item/
---
## DocumentData indexer

인덱스로 필드 데이터를 가져옵니다.

```csharp
public FieldData this[int index] { get; }
```

| 모수 | 설명 |
| --- | --- |
| index | 필드의 0부터 시작하는 인덱스입니다. |

### 반환 값

인스턴스[`FieldData`](../../fielddata) 수업.

### 예

모든 필드를 통한 반복:

[`FieldData`](../../fielddata) 클래스는 필드 데이터를 나타냅니다. 분야에 따라[`PageArea`](../../fielddata/pagearea) property 는 다음 상속자를 포함할 수 있습니다.[`PageArea`](../../pagearea) 수업. 예를 들어,[`ParseForm`](../../../groupdocs.parser/parser/parseform) method 텍스트 필드만 추출:

```csharp
for (int i = 0; i < data.Count; i++)
{
    Console.Write(data[i].Name + ": ");
    PageTextArea area = data[i].PageArea as PageTextArea;
    Console.WriteLine(area == null ? "Not a template field" : area.Text);
}
```

```csharp
// 파서 생성
using (Parser parser = new Parser(filePath))
{
    // PDF 양식에서 데이터 추출
    DocumentData data = parser.ParseForm();
    // 추출된 필드를 반복합니다.
    for (int i = 0; i < data.Count; i++)
    {
        // 추출된 필드 가져오기
        FieldData field = data[i];
        // 필드명 출력
        Console.Write(field.Name + ": ");
        // 필드 값이 텍스트인지 확인하고 출력
        PageTextArea area = field.PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### 또한보십시오

* class [FieldData](../../fielddata)
* class [DocumentData](../../documentdata)
* 네임스페이스 [GroupDocs.Parser.Data](../../documentdata)
* 집회 [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
