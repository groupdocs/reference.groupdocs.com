---
title: GetContainer
second_title: .NET API 참조용 GroupDocs.Parser
description: 첨부 파일 ZIP 아카이브 등을 포함하는 형식으로 작업하기 위해 문서에서 컨테이너 개체를 추출합니다.
type: docs
weight: 60
url: /ko/net/groupdocs.parser/parser/getcontainer/
---
## Parser.GetContainer method

첨부 파일, ZIP 아카이브 등을 포함하는 형식으로 작업하기 위해 문서에서 컨테이너 개체를 추출합니다.

```csharp
public IEnumerable<ContainerItem> GetContainer()
```

### 반환 값

컨테이너 항목 모음; `없는`컨테이너 추출이 지원되지 않는 경우.

### 비고

형식이 첨부 파일 추출을 지원하는지 확인하려면 다음을 참조하십시오.[지원되는 문서 형식](https://docs.groupdocs.com/parser/net/supported-document-formats/) (**컨테이너 및 첨부 파일 추출** 열).

**더 알아보기:**

* [첨부 파일 및 ZIP 아카이브에서 데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+data+from+attachments+and+ZIP+archives)
* [컨테이너 항목 반복](https://docs.groupdocs.com/display/parsernet/Iterate+through+container+items)
* [PDF 포트폴리오에서 첨부 파일 추출](https://docs.groupdocs.com/display/parsernet/Extract+attachments+from+PDF+portfolios)
* [이메일에서 첨부 파일 추출](https://docs.groupdocs.com/display/parsernet/Extract+attachments+from+Emails)
* [Outlook 저장소에서 이메일 추출](https://docs.groupdocs.com/display/parsernet/Extract+emails+from+Outlook+Storage)
* [ZIP 아카이브 파일에서 텍스트 추출](https://docs.groupdocs.com/display/parsernet/Extract+text+from+ZIP+archive+files)

### 예

다음 예에서는 컨테이너에서 첨부 파일을 추출하는 방법을 보여줍니다.

```csharp
// Parser 클래스의 인스턴스 생성
using(Parser parser = new Parser(filePath))
{
    // 컨테이너에서 첨부 파일 추출
    IEnumerable<ContainerItem> attachments = parser.GetContainer();
    // 컨테이너 추출 지원 여부 확인
    if(attachments == null)
    {
        Console.WriteLine("Container extraction isn't supported");
    }
 
    // 첨부 파일을 반복합니다.
    foreach(ContainerItem item in attachments)
    {
        // 항목 이름과 크기를 출력합니다.
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Size));
    }
}
```

### 또한보십시오

* class [ContainerItem](../../../groupdocs.parser.data/containeritem)
* class [Parser](../../parser)
* 네임스페이스 [GroupDocs.Parser](../../parser)
* 집회 [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
