---
title: GetFileInfo
second_title: .NET API 참조용 GroupDocs.Parser
description: 파일에 대한 일반 정보를 반환합니다.
type: docs
weight: 210
url: /ko/net/groupdocs.parser/parser/getfileinfo/
---
## GetFileInfo(string) {#getfileinfo_2}

파일에 대한 일반 정보를 반환합니다.

```csharp
public static FileInfo GetFileInfo(string filePath)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| filePath | String | 파일의 경로입니다. |

### 반환 값

인스턴스[`FileInfo`](../../../groupdocs.parser.options/fileinfo) 수업.

### 예

다음 코드는 파일이 암호로 보호되어 있는지 확인하는 방법을 보여줍니다.

```csharp
// 파일 정보 얻기
Options.FileInfo info = Parser.GetFileInfo(filePath);
// IsEncrypted 속성 확인
Console.WriteLine(info.IsEncrypted ? "Password is required" : "");
```

### 또한보십시오

* class [FileInfo](../../../groupdocs.parser.options/fileinfo)
* class [Parser](../../parser)
* 네임스페이스 [GroupDocs.Parser](../../parser)
* 집회 [GroupDocs.Parser](../../../)

---

## GetFileInfo(string, LoadOptions) {#getfileinfo_3}

파일에 대한 일반 정보를 반환합니다.

```csharp
public static FileInfo GetFileInfo(string filePath, LoadOptions loadOptions)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| filePath | String | 파일의 경로입니다. |
| loadOptions | LoadOptions | 파일을 여는 옵션입니다. |

### 반환 값

인스턴스[`FileInfo`](../../../groupdocs.parser.options/fileinfo) 수업.

### 예

다음 코드는 암호로 보호된 문서의 파일 유형을 확인하는 방법을 보여줍니다.

```csharp
// 파일 정보 얻기
Options.FileInfo info = Parser.GetFileInfo(filePath, new LoadOptions("password"));
// IsEncrypted 속성 확인
Console.WriteLine(info.IsEncrypted ? "Password is required" : "");
// 파일 형식 출력
Console.WriteLine(info.FileType.ToString());
```

### 또한보십시오

* class [FileInfo](../../../groupdocs.parser.options/fileinfo)
* class [LoadOptions](../../../groupdocs.parser.options/loadoptions)
* class [Parser](../../parser)
* 네임스페이스 [GroupDocs.Parser](../../parser)
* 집회 [GroupDocs.Parser](../../../)

---

## GetFileInfo(Stream) {#getfileinfo}

파일에 대한 일반 정보를 반환합니다.

```csharp
public static FileInfo GetFileInfo(Stream document)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| document | Stream | 소스 입력 스트림입니다. |

### 반환 값

인스턴스[`FileInfo`](../../../groupdocs.parser.options/fileinfo) 수업.

### 예

다음 코드는 파일이 암호로 보호되어 있는지 확인하는 방법을 보여줍니다.

```csharp
// 파일 정보 얻기
Options.FileInfo info = Parser.GetFileInfo(document);
// IsEncrypted 속성 확인
Console.WriteLine(info.IsEncrypted ? "Password is required" : "");
```

### 또한보십시오

* class [FileInfo](../../../groupdocs.parser.options/fileinfo)
* class [Parser](../../parser)
* 네임스페이스 [GroupDocs.Parser](../../parser)
* 집회 [GroupDocs.Parser](../../../)

---

## GetFileInfo(Stream, LoadOptions) {#getfileinfo_1}

파일에 대한 일반 정보를 반환합니다.

```csharp
public static FileInfo GetFileInfo(Stream document, LoadOptions loadOptions)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| document | Stream | 소스 입력 스트림입니다. |
| loadOptions | LoadOptions | 파일을 여는 옵션입니다. |

### 반환 값

인스턴스[`FileInfo`](../../../groupdocs.parser.options/fileinfo) 수업.

### 예

다음 코드는 암호로 보호된 문서의 파일 유형을 확인하는 방법을 보여줍니다.

```csharp
// 파일 정보 얻기
Options.FileInfo info = Parser.GetFileInfo(document);
// IsEncrypted 속성 확인
Console.WriteLine(info.IsEncrypted ? "Password is required" : "");
// 파일 형식 출력
Console.WriteLine(info.FileType.ToString());
```

### 또한보십시오

* class [FileInfo](../../../groupdocs.parser.options/fileinfo)
* class [LoadOptions](../../../groupdocs.parser.options/loadoptions)
* class [Parser](../../parser)
* 네임스페이스 [GroupDocs.Parser](../../parser)
* 집회 [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
