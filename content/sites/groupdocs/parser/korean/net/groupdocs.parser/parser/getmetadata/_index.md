---
title: GetMetadata
second_title: .NET API 참조용 GroupDocs.Parser
description: 문서에서 메타데이터를 추출합니다.
type: docs
weight: 120
url: /ko/net/groupdocs.parser/parser/getmetadata/
---
## Parser.GetMetadata method

문서에서 메타데이터를 추출합니다.

```csharp
public IEnumerable<MetadataItem> GetMetadata()
```

### 반환 값

메타데이터 항목 모음; `없는` 메타데이터 추출이 지원되지 않는 경우.

### 비고

**더 알아보기:**

* [문서에서 메타데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+documents)
* [Microsoft Office Word 문서에서 메타데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Word+documents)
* [Microsoft Office Excel 스프레드시트에서 메타데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Excel+spreadsheets)
* [Microsoft Office PowerPoint 프레젠테이션에서 메타데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+PowerPoint+presentations)
* [PDF 문서에서 메타데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+PDF+documents)
* [이메일에서 메타데이터 추출](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Emails)

### 예

다음 예에서는 문서에서 메타데이터를 추출하는 방법을 보여줍니다.

```csharp
// Parser 클래스의 인스턴스 생성
using(Parser parser = new Parser(filePath))
{
    // 문서에서 메타데이터 추출
    IEnumerable<MetadataItem> metadata = parser.GetMetadata();
    // 메타데이터 추출 지원 여부 확인
    if(metadata == null)
    {
        Console.WriteLine("Metatada extraction isn't supported");
    }
 
    // 메타데이터 항목을 반복합니다.
    foreach(MetadataItem item in metadata)
    {
        // 아이템 이름과 값 출력
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Value));
    }
}
```

### 또한보십시오

* class [MetadataItem](../../../groupdocs.parser.data/metadataitem)
* class [Parser](../../parser)
* 네임스페이스 [GroupDocs.Parser](../../parser)
* 집회 [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
