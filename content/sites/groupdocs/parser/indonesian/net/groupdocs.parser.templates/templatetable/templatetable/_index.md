---
title: TemplateTable
second_title: GroupDocs.Parser untuk Referensi .NET API
description: Menginisialisasi instance baru dariTemplateTablegroupdocs.parser.templates/templatetable kelas.
type: docs
weight: 10
url: /id/net/groupdocs.parser.templates/templatetable/templatetable/
---
## TemplateTable(TemplateTableLayout, string, int?) {#constructor}

Menginisialisasi instance baru dari[`TemplateTable`](../../templatetable) kelas.

```csharp
public TemplateTable(TemplateTableLayout layout, string name, int? pageIndex)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| layout | TemplateTableLayout | Tata letak meja. |
| name | String | Nama tabel. |
| pageIndex | Nullable`1 | Indeks halaman tempat tabel template berada; `batal` jika tabel template terletak di halaman manapun. |

### Contoh

Tabel template diatur oleh tata letak tabel jika tabel tidak dapat dideteksi secara otomatis:

```csharp
TemplateTableLayout layout = new TemplateTableLayout(
    new double[] { 50, 95, 275 },
    new double[] { 325, 340, 365 });
 
TemplateTable table = new TemplateTable(layout, "Details", null);

// Buat templat dokumen
Template template = new Template(new TemplateItem[] { table });
```

### Lihat juga

* class [TemplateTableLayout](../../templatetablelayout)
* class [TemplateTable](../../templatetable)
* ruang nama [GroupDocs.Parser.Templates](../../templatetable)
* perakitan [GroupDocs.Parser](../../../)

---

## TemplateTable(TemplateTableParameters, string, int?) {#constructor_1}

Menginisialisasi instance baru dari[`TemplateTable`](../../templatetable) kelas.

```csharp
public TemplateTable(TemplateTableParameters parameters, string name, int? pageIndex)
```

| Parameter | Jenis | Keterangan |
| --- | --- | --- |
| parameters | TemplateTableParameters | Parameter untuk mendeteksi tabel dalam mode otomatis. |
| name | String | Nama tabel. |
| pageIndex | Nullable`1 | Indeks halaman tempat tabel template berada; `batal` jika tabel template terletak di halaman manapun. |

### Contoh

Jika tabel template diatur oleh parameter detektor, tabel akan dideteksi secara otomatis:

```csharp
TemplateTableParameters parameters = new TemplateTableParameters(
    new Rectangle(new Point(175, 350), new Size(400, 200)),
    new double[] { 185, 370, 425, 485, 545 });

TemplateTable table = new TemplateTable(parameters, "Details", 0);

// Buat templat dokumen
Template template = new Template(new TemplateItem[] { table });
```

### Lihat juga

* class [TemplateTableParameters](../../templatetableparameters)
* class [TemplateTable](../../templatetable)
* ruang nama [GroupDocs.Parser.Templates](../../templatetable)
* perakitan [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
