---
title: ParseForm
second_title: GroupDocs.Parser untuk Referensi .NET API
description: Mengurai formulir dokumen.
type: docs
weight: 190
url: /id/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Mengurai formulir dokumen.

```csharp
public DocumentData ParseForm()
```

### Nilai Pengembalian

Contoh dari[`DocumentData`](../../../groupdocs.parser.data/documentdata) kelas yang berisi data yang diekstrak; `batal` jika parsing dengan template tidak didukung.

### Perkataan

**Belajarlah lagi:**

* [Ekstrak data dari formulir PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Bekerja dengan data yang diekstrak](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Mengurai data dari dokumen PDF](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Contoh

Contoh berikut menunjukkan cara mengurai formulir dokumen:

```csharp
// Buat instance dari kelas Parser
using (Parser parser = new Parser(filePath))
{
    // Ekstrak data dari dokumen PDF
    DocumentData data = parser.ParseForm();
    // Ulangi data yang diekstraksi
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Lihat juga

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* ruang nama [GroupDocs.Parser](../../parser)
* perakitan [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
