---
title: GetMetadata
second_title: GroupDocs.Parser untuk Referensi .NET API
description: Mengekstrak metadata dari dokumen.
type: docs
weight: 120
url: /id/net/groupdocs.parser/parser/getmetadata/
---
## Parser.GetMetadata method

Mengekstrak metadata dari dokumen.

```csharp
public IEnumerable<MetadataItem> GetMetadata()
```

### Nilai Pengembalian

Kumpulan item metadata; `batal` jika ekstraksi metadata tidak didukung.

### Perkataan

**Belajarlah lagi:**

* [Ekstrak metadata dari dokumen](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+documents)
* [Ekstrak metadata dari dokumen Microsoft Office Word](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Word+documents)
* [Ekstrak metadata dari spreadsheet Microsoft Office Excel](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Excel+spreadsheets)
* [Ekstrak metadata dari presentasi Microsoft Office PowerPoint](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+PowerPoint+presentations)
* [Ekstrak metadata dari dokumen PDF](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+PDF+documents)
* [Ekstrak metadata dari Email](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Emails)

### Contoh

Contoh berikut menunjukkan cara mengekstrak metadata dari dokumen:

```csharp
// Buat instance dari kelas Parser
using(Parser parser = new Parser(filePath))
{
    // Ekstrak metadata dari dokumen
    IEnumerable<MetadataItem> metadata = parser.GetMetadata();
    // Periksa apakah ekstraksi metadata didukung
    if(metadata == null)
    {
        Console.WriteLine("Metatada extraction isn't supported");
    }
 
    // Ulangi item metadata
    foreach(MetadataItem item in metadata)
    {
        // Cetak nama dan nilai item
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Value));
    }
}
```

### Lihat juga

* class [MetadataItem](../../../groupdocs.parser.data/metadataitem)
* class [Parser](../../parser)
* ruang nama [GroupDocs.Parser](../../parser)
* perakitan [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
