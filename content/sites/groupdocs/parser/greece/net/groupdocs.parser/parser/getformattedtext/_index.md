---
title: GetFormattedText
second_title: GroupDocs.Parser για Αναφορά API .NET
description: Εξάγει ένα μορφοποιημένο κείμενο από το έγγραφο.
type: docs
weight: 80
url: /el/net/groupdocs.parser/parser/getformattedtext/
---
## GetFormattedText(FormattedTextOptions) {#getformattedtext}

Εξάγει ένα μορφοποιημένο κείμενο από το έγγραφο.

```csharp
public TextReader GetFormattedText(FormattedTextOptions options)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| options | FormattedTextOptions | Οι επιλογές εξαγωγής μορφοποιημένου κειμένου. |

### Επιστρεφόμενη Αξία

Ένα στιγμιότυπο τουTextReader τάξη με το εξαγόμενο κείμενο; `μηδενικό` εάν η εξαγωγή μορφοποιημένου κειμένου δεν υποστηρίζεται.

### Παρατηρήσεις

**Μάθε περισσότερα:**

* [Εξαγωγή μορφοποιημένου κειμένου από το έγγραφο](https://docs.groupdocs.com/display/parsernet/Extract+formatted+text+from+document)
* Εξαγωγή κειμένου εγγράφου ως[HTML](https://docs.groupdocs.com/display/parsernet/HTML)
* Εξαγωγή κειμένου εγγράφου ως[Χαμήλωση τιμής](https://docs.groupdocs.com/display/parsernet/Markdown)
* Εξαγωγή κειμένου εγγράφου ως[Απλό κείμενο](https://docs.groupdocs.com/display/parsernet/Plain+text)

### Παραδείγματα

Το ακόλουθο παράδειγμα δείχνει πώς να εξαγάγετε ένα κείμενο εγγράφου ως κείμενο HTML:

```csharp
// Δημιουργία μιας παρουσίας κλάσης Parser
using (Parser parser = new Parser(filePath))
{
    // Εξαγωγή ενός μορφοποιημένου κειμένου στον αναγνώστη
    using (TextReader reader = parser.GetFormattedText(new FormattedTextOptions(FormattedTextMode.Html)))
    {
        // Εκτυπώστε ένα μορφοποιημένο κείμενο από το έγγραφο
        // Εάν η εξαγωγή μορφοποιημένου κειμένου δεν υποστηρίζεται, ο αναγνώστης είναι μηδενικός
        Console.WriteLine(reader == null ? "Formatted text extraction isn't suppported" : reader.ReadToEnd());
    }
}
```

### Δείτε επίσης

* class [FormattedTextOptions](../../../groupdocs.parser.options/formattedtextoptions)
* class [Parser](../../parser)
* χώρος ονομάτων [GroupDocs.Parser](../../parser)
* συνέλευση [GroupDocs.Parser](../../../)

---

## GetFormattedText(int, FormattedTextOptions) {#getformattedtext_1}

Εξάγει ένα μορφοποιημένο κείμενο από τη σελίδα του εγγράφου.

```csharp
public TextReader GetFormattedText(int pageIndex, FormattedTextOptions options)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| pageIndex | Int32 | Το ευρετήριο σελίδας που βασίζεται σε μηδέν. |
| options | FormattedTextOptions | Οι επιλογές εξαγωγής μορφοποιημένου κειμένου. |

### Επιστρεφόμενη Αξία

Ένα στιγμιότυπο τουTextReaderτάξη με το εξαγόμενο κείμενο. `μηδενικό` αν δεν υποστηρίζεται η εξαγωγή σελίδας μορφοποιημένου κειμένου.

### Παρατηρήσεις

**Μάθε περισσότερα:**

* [Εξαγωγή μορφοποιημένου κειμένου από τη σελίδα εγγράφου](https://docs.groupdocs.com/display/parsernet/Extract+formatted+text+from+document+page)
* Εξαγωγή κειμένου εγγράφου ως[HTML](https://docs.groupdocs.com/display/parsernet/HTML)
* Εξαγωγή κειμένου εγγράφου ως[Χαμήλωση τιμής](https://docs.groupdocs.com/display/parsernet/Markdown)
* Εξαγωγή κειμένου εγγράφου ως[Απλό κείμενο](https://docs.groupdocs.com/display/parsernet/Plain+text)

### Παραδείγματα

Το ακόλουθο παράδειγμα δείχνει πώς να εξαγάγετε ένα κείμενο σελίδας εγγράφου ως κείμενο Markdown:

```csharp
// Δημιουργία μιας παρουσίας κλάσης Parser
using (Parser parser = new Parser(filePath))
{
    // Ελέγξτε εάν το έγγραφο υποστηρίζει την εξαγωγή μορφοποιημένου κειμένου
    if (!parser.Features.FormattedText)
    {
        Console.WriteLine("Document isn't supports formatted text extraction.");
        return;
    }
    
    // Λάβετε τις πληροφορίες του εγγράφου
    IDocumentInfo documentInfo = parser.GetDocumentInfo();
    // Ελέγξτε εάν το έγγραφο έχει σελίδες
    if (documentInfo.PageCount == 0)
    {
        Console.WriteLine("Document hasn't pages.");
        return;
    }
    
    // Επανάληψη σε σελίδες
    for (int p = 0; p<documentInfo.PageCount; p++)
    {
        // Εκτύπωση αριθμού σελίδας 
        Console.WriteLine(string.Format("Page {0}/{1}", p + 1, documentInfo.PageCount));
        // Εξαγωγή ενός μορφοποιημένου κειμένου στον αναγνώστη
        using (TextReader reader = parser.GetFormattedText(p, new FormattedTextOptions(FormattedTextMode.Markdown)))
        {
            // Εκτυπώστε ένα μορφοποιημένο κείμενο από το έγγραφο
            // Αγνοούμε τον μηδενικό έλεγχο, καθώς έχουμε ελέγξει νωρίτερα την υποστήριξη της δυνατότητας εξαγωγής μορφοποιημένου κειμένου
            Console.WriteLine(reader.ReadToEnd());
        }
    }
}
```

### Δείτε επίσης

* class [FormattedTextOptions](../../../groupdocs.parser.options/formattedtextoptions)
* class [Parser](../../parser)
* χώρος ονομάτων [GroupDocs.Parser](../../parser)
* συνέλευση [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
