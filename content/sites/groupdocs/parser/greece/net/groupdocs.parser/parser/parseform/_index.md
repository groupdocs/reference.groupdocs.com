---
title: ParseForm
second_title: GroupDocs.Parser για Αναφορά API .NET
description: Αναλύει τη φόρμα εγγράφου.
type: docs
weight: 190
url: /el/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Αναλύει τη φόρμα εγγράφου.

```csharp
public DocumentData ParseForm()
```

### Επιστρεφόμενη Αξία

Ένα στιγμιότυπο του[`DocumentData`](../../../groupdocs.parser.data/documentdata) κλάση που περιέχει τα εξαγόμενα δεδομένα· `μηδενικό` εάν η ανάλυση κατά πρότυπο δεν υποστηρίζεται.

### Παρατηρήσεις

**Μάθε περισσότερα:**

* [Εξαγωγή δεδομένων από φόρμες PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Εργασία με εξαγόμενα δεδομένα](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Ανάλυση δεδομένων από έγγραφα PDF](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Παραδείγματα

Το ακόλουθο παράδειγμα δείχνει τον τρόπο ανάλυσης μιας φόρμας του εγγράφου:

```csharp
// Δημιουργία μιας παρουσίας κλάσης Parser
using (Parser parser = new Parser(filePath))
{
    // Εξαγωγή δεδομένων από έγγραφο PDF
    DocumentData data = parser.ParseForm();
    // Επανάληψη σε εξαγόμενα δεδομένα
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Δείτε επίσης

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* χώρος ονομάτων [GroupDocs.Parser](../../parser)
* συνέλευση [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
