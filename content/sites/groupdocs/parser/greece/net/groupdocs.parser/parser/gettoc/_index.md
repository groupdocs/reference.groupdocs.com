---
title: GetToc
second_title: GroupDocs.Parser για Αναφορά API .NET
description: Εξάγει έναν πίνακα περιεχομένων από το έγγραφο.
type: docs
weight: 170
url: /el/net/groupdocs.parser/parser/gettoc/
---
## Parser.GetToc method

Εξάγει έναν πίνακα περιεχομένων από το έγγραφο.

```csharp
public IEnumerable<TocItem> GetToc()
```

### Επιστρεφόμενη Αξία

Μια συλλογή στοιχείων πίνακα περιεχομένων· `μηδενικό` εάν η εξαγωγή πίνακα περιεχομένων δεν υποστηρίζεται.

### Παρατηρήσεις

**Μάθε περισσότερα:**

* [Εξαγωγή πίνακα περιεχομένων](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents)
* [Εξαγωγή κειμένου ανά στοιχείο πίνακα περιεχομένων](https://docs.groupdocs.com/display/parsernet/Extract+text+by+table+of+contents+item)
* [Εξαγωγή πίνακα περιεχομένων από έγγραφα του Microsoft Office Word](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+Microsoft+Office+Word+documents)
* [Εξαγωγή πίνακα περιεχομένων από ηλεκτρονικά βιβλία EPUB](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+EPUB+eBooks)

### Παραδείγματα

Το ακόλουθο παράδειγμα δείχνει πώς να εξαγάγετε τον πίνακα περιεχομένων από το αρχείο CHM:

```csharp
// Δημιουργία μιας παρουσίας κλάσης Parser
using (Parser parser = new Parser(filePath))
{
    // Ελέγξτε αν υποστηρίζεται η εξαγωγή κειμένου
    if (!parser.Features.Text)
    {
        Console.WriteLine("Text extraction isn't supported.");
        return;
    }

    // Ελέγξτε αν υποστηρίζεται η εξαγωγή toc
    if (!parser.Features.Toc)
    {
        Console.WriteLine("Toc extraction isn't supported.");
        return;
    }
 
    // Λήψη πίνακα περιεχομένων
    IEnumerable<TocItem> toc = parser.GetToc();
    
    // Επανάληψη σε στοιχεία
    foreach (TocItem i in toc)
    {
        // Εκτυπώστε το κείμενο Toc
        Console.WriteLine(i.Text);
        // Ελέγξτε εάν το ευρετήριο σελίδας έχει τιμή
        if (i.PageIndex == null)
        {
            continue;
        }
        // Εξαγωγή κειμένου σελίδας
        using (TextReader reader = parser.GetText(i.PageIndex.Value))
        {
            Console.WriteLine(reader.ReadToEnd());
        }
    }
}
```

### Δείτε επίσης

* class [TocItem](../../../groupdocs.parser.data/tocitem)
* class [Parser](../../parser)
* χώρος ονομάτων [GroupDocs.Parser](../../parser)
* συνέλευση [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
