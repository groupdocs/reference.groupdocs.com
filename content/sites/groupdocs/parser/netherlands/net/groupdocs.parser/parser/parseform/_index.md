---
title: ParseForm
second_title: GroupDocs.Parser voor .NET API-referentie
description: Parseert het documentformulier.
type: docs
weight: 190
url: /nl/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Parseert het documentformulier.

```csharp
public DocumentData ParseForm()
```

### Winstwaarde

Een exemplaar van[`DocumentData`](../../../groupdocs.parser.data/documentdata) klasse die de geëxtraheerde gegevens bevat; `nul` als parseren op sjabloon niet wordt ondersteund.

### Opmerkingen

**Kom meer te weten:**

* [Extraheer gegevens uit PDF-formulieren](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Werken met geëxtraheerde gegevens](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Parseer gegevens uit PDF-documenten](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Voorbeelden

Het volgende voorbeeld laat zien hoe een formulier van het document moet worden geparseerd:

```csharp
// Maak een instantie van de Parser-klasse
using (Parser parser = new Parser(filePath))
{
    // Extraheer gegevens uit PDF-document
    DocumentData data = parser.ParseForm();
    // Itereren over geëxtraheerde gegevens
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Zie ook

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* naamruimte [GroupDocs.Parser](../../parser)
* montage [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
