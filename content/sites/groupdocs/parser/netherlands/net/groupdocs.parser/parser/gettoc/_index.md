---
title: GetToc
second_title: GroupDocs.Parser voor .NET API-referentie
description: Haalt een inhoudsopgave uit het document.
type: docs
weight: 170
url: /nl/net/groupdocs.parser/parser/gettoc/
---
## Parser.GetToc method

Haalt een inhoudsopgave uit het document.

```csharp
public IEnumerable<TocItem> GetToc()
```

### Winstwaarde

Een verzameling inhoudsopgave-items; `nul` als extractie van de inhoudsopgave niet wordt ondersteund.

### Opmerkingen

**Kom meer te weten:**

* [Pak de inhoudsopgave uit](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents)
* [Extraheer tekst per inhoudsopgave-item](https://docs.groupdocs.com/display/parsernet/Extract+text+by+table+of+contents+item)
* [Pak de inhoudsopgave uit Microsoft Office Word-documenten](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+Microsoft+Office+Word+documents)
* [Pak de inhoudsopgave uit EPUB eBooks](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+EPUB+eBooks)

### Voorbeelden

Het volgende voorbeeld laat zien hoe u de inhoudsopgave uit het CHM-bestand haalt:

```csharp
// Maak een instantie van de Parser-klasse
using (Parser parser = new Parser(filePath))
{
    // Controleer of tekstextractie wordt ondersteund
    if (!parser.Features.Text)
    {
        Console.WriteLine("Text extraction isn't supported.");
        return;
    }

    // Controleer of toc-extractie wordt ondersteund
    if (!parser.Features.Toc)
    {
        Console.WriteLine("Toc extraction isn't supported.");
        return;
    }
 
    // Haal de inhoudsopgave op
    IEnumerable<TocItem> toc = parser.GetToc();
    
    // Items herhalen
    foreach (TocItem i in toc)
    {
        // Druk de Toc-tekst af
        Console.WriteLine(i.Text);
        // Controleer of de pagina-index een waarde heeft
        if (i.PageIndex == null)
        {
            continue;
        }
        // Extraheer een paginatekst
        using (TextReader reader = parser.GetText(i.PageIndex.Value))
        {
            Console.WriteLine(reader.ReadToEnd());
        }
    }
}
```

### Zie ook

* class [TocItem](../../../groupdocs.parser.data/tocitem)
* class [Parser](../../parser)
* naamruimte [GroupDocs.Parser](../../parser)
* montage [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
