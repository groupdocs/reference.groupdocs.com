---
title: ParseForm
second_title: Riferimento API GroupDocs.Parser per .NET
description: Analizza il modulo del documento.
type: docs
weight: 190
url: /it/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Analizza il modulo del documento.

```csharp
public DocumentData ParseForm()
```

### Valore di ritorno

Un'istanza di[`DocumentData`](../../../groupdocs.parser.data/documentdata) classe che contiene i dati estratti; `nullo` se l'analisi per modello non è supportata.

### Osservazioni

**Scopri di più:**

* [Estrai i dati dai moduli PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Lavorare con i dati estratti](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Analizza i dati dai documenti PDF](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Esempi

L'esempio seguente mostra come analizzare un modulo del documento:

```csharp
// Crea un'istanza della classe Parser
using (Parser parser = new Parser(filePath))
{
    // Estrai i dati dal documento PDF
    DocumentData data = parser.ParseForm();
    // Itera sui dati estratti
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Guarda anche

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* spazio dei nomi [GroupDocs.Parser](../../parser)
* assemblea [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
