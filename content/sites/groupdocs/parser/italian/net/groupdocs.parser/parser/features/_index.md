---
title: Features
second_title: Riferimento API GroupDocs.Parser per .NET
description: Ottiene le funzioni supportate.
type: docs
weight: 20
url: /it/net/groupdocs.parser/parser/features/
---
## Parser.Features property

Ottiene le funzioni supportate.

```csharp
public Features Features { get; }
```

### Valore della proprietà

Un'istanza di[`Features`](../../../groupdocs.parser.options/features) classe che rappresenta le funzionalità supportate.

### Osservazioni

**Saperne di più:**

* [Ottieni funzionalità supportate](https://docs.groupdocs.com/display/parsernet/Get+supported+features)

### Esempi

Se la funzione non è supportata, il metodo restituisce`nullo` invece del valore. Alcune operazioni potrebbero richiedere molto tempo. Quindi non è ottimale chiamare il metodo solo per verificare il supporto per la funzionalità. A tale scopo viene utilizzata la proprietà Features:

```csharp
using(Parser parser = new Parser("doc.zip"))
{
    if(!parser.Features.Text)
    {
        Console.WriteLine("Text extraction isn't supported");
        return;
    }
 
    using(TextReader reader = parser.GetText())
    {
        Console.WriteLine(reader.ReadToEnd());
    }
}
```

### Guarda anche

* class [Features](../../../groupdocs.parser.options/features)
* class [Parser](../../parser)
* spazio dei nomi [GroupDocs.Parser](../../parser)
* assemblea [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
