---
title: GetMetadata
second_title: GroupDocs.Parser für .NET-API-Referenz
description: Extrahiert Metadaten aus dem Dokument.
type: docs
weight: 120
url: /de/net/groupdocs.parser/parser/getmetadata/
---
## Parser.GetMetadata method

Extrahiert Metadaten aus dem Dokument.

```csharp
public IEnumerable<MetadataItem> GetMetadata()
```

### Rückgabewert

Eine Sammlung von Metadatenelementen; `Null` wenn die Metadatenextraktion nicht unterstützt wird.

### Bemerkungen

**Mehr erfahren:**

* [Metadaten aus Dokumenten extrahieren](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+documents)
* [Extrahieren Sie Metadaten aus Microsoft Office Word-Dokumenten](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Word+documents)
* [Extrahieren Sie Metadaten aus Microsoft Office Excel-Tabellen](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Excel+spreadsheets)
* [Extrahieren Sie Metadaten aus Microsoft Office PowerPoint-Präsentationen](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+PowerPoint+presentations)
* [Metadaten aus PDF-Dokumenten extrahieren](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+PDF+documents)
* [Metadaten aus E-Mails extrahieren](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Emails)

### Beispiele

Das folgende Beispiel zeigt, wie Metadaten aus einem Dokument extrahiert werden:

```csharp
// Erstellen Sie eine Instanz der Parser-Klasse
using(Parser parser = new Parser(filePath))
{
    // Metadaten aus dem Dokument extrahieren
    IEnumerable<MetadataItem> metadata = parser.GetMetadata();
    // Prüfen, ob Metadatenextraktion unterstützt wird
    if(metadata == null)
    {
        Console.WriteLine("Metatada extraction isn't supported");
    }
 
    // Über Metadatenelemente iterieren
    foreach(MetadataItem item in metadata)
    {
        // Einen Artikelnamen und -wert drucken
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Value));
    }
}
```

### Siehe auch

* class [MetadataItem](../../../groupdocs.parser.data/metadataitem)
* class [Parser](../../parser)
* namensraum [GroupDocs.Parser](../../parser)
* Montage [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
