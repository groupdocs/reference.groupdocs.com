---
title: GetContainer
second_title: GroupDocs.Parser für .NET-API-Referenz
description: Extrahiert ein Containerobjekt aus dem Dokument um mit Formaten zu arbeiten die Anhänge ZIPArchive usw. enthalten.
type: docs
weight: 60
url: /de/net/groupdocs.parser/parser/getcontainer/
---
## Parser.GetContainer method

Extrahiert ein Containerobjekt aus dem Dokument, um mit Formaten zu arbeiten, die Anhänge, ZIP-Archive usw. enthalten.

```csharp
public IEnumerable<ContainerItem> GetContainer()
```

### Rückgabewert

Eine Sammlung von Containerartikeln; `Null`wenn die Containerextraktion nicht unterstützt wird.

### Bemerkungen

Um zu überprüfen, ob das Format das Extrahieren von Anhängen unterstützt, siehe[Unterstützte Dokumentformate](https://docs.groupdocs.com/parser/net/supported-document-formats/) (**Container und Anhänge extrahieren** Spalte).

**Erfahren Sie mehr:**

* [Extrahieren Sie Daten aus Anhängen und ZIP-Archiven](https://docs.groupdocs.com/display/parsernet/Extract+data+from+attachments+and+ZIP+archives)
* [Containerelemente durchlaufen](https://docs.groupdocs.com/display/parsernet/Iterate+through+container+items)
* [Extrahieren Sie Anhänge aus PDF-Portfolios](https://docs.groupdocs.com/display/parsernet/Extract+attachments+from+PDF+portfolios)
* [Extrahieren Sie Anhänge aus E-Mails](https://docs.groupdocs.com/display/parsernet/Extract+attachments+from+Emails)
* [Extrahieren Sie E-Mails aus dem Outlook-Speicher](https://docs.groupdocs.com/display/parsernet/Extract+emails+from+Outlook+Storage)
* [Text aus ZIP-Archivdateien extrahieren](https://docs.groupdocs.com/display/parsernet/Extract+text+from+ZIP+archive+files)

### Beispiele

Das folgende Beispiel zeigt, wie Anhänge aus einem Container extrahiert werden:

```csharp
// Erstellen Sie eine Instanz der Parser-Klasse
using(Parser parser = new Parser(filePath))
{
    // Anhänge aus dem Container extrahieren
    IEnumerable<ContainerItem> attachments = parser.GetContainer();
    // Prüfen, ob die Container-Extraktion unterstützt wird
    if(attachments == null)
    {
        Console.WriteLine("Container extraction isn't supported");
    }
 
    // Über Anhänge iterieren
    foreach(ContainerItem item in attachments)
    {
        // Einen Artikelnamen und eine Größe drucken
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Size));
    }
}
```

### Siehe auch

* class [ContainerItem](../../../groupdocs.parser.data/containeritem)
* class [Parser](../../parser)
* namensraum [GroupDocs.Parser](../../parser)
* Montage [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
