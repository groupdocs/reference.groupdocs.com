---
title: ParseForm
second_title: GroupDocs.Parser für .NET-API-Referenz
description: Analysiert das Dokumentformular.
type: docs
weight: 190
url: /de/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Analysiert das Dokumentformular.

```csharp
public DocumentData ParseForm()
```

### Rückgabewert

Eine Instanz von[`DocumentData`](../../../groupdocs.parser.data/documentdata) Klasse, die die extrahierten Daten enthält; `Null` wenn das Parsen nach Vorlage nicht unterstützt wird.

### Bemerkungen

**Mehr erfahren:**

* [Daten aus PDF-Formularen extrahieren](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Arbeiten mit extrahierten Daten](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Analysieren Sie Daten aus PDF-Dokumenten](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Beispiele

Das folgende Beispiel zeigt, wie ein Formular des Dokuments analysiert wird:

```csharp
// Erstellen Sie eine Instanz der Parser-Klasse
using (Parser parser = new Parser(filePath))
{
    // Daten aus PDF-Dokument extrahieren
    DocumentData data = parser.ParseForm();
    // Über extrahierte Daten iterieren
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Siehe auch

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* namensraum [GroupDocs.Parser](../../parser)
* Montage [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
