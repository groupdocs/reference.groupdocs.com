---
title: Save
second_title: GroupDocs.Parser for .NET API 参考
description: 将图像保存到文件中
type: docs
weight: 50
url: /zh/net/groupdocs.parser.data/pageimagearea/save/
---
## Save(string) {#save}

将图像保存到文件中。

```csharp
public void Save(string filePath)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| filePath | String | 文件的路径。 |

### 例子

以下示例显示如何将图像保存到文件：

```csharp
// 创建 Parser 类的实例
using (Parser parser = new Parser(filePath))
{
    // 从文档中提取图像
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // 检查是否支持图像提取
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // 遍历图像
    foreach (PageImageArea image in images)
    {
        // 将图片保存到文件中
        image.Save(Guid.NewGuid().ToString() + image.FileType.Extension);
    }
}
```

### 也可以看看

* class [PageImageArea](../../pageimagearea)
* 命名空间 [GroupDocs.Parser.Data](../../pageimagearea)
* 部件 [GroupDocs.Parser](../../../)

---

## Save(string, ImageOptions) {#save_1}

以不同的格式将图像保存到文件中。

```csharp
public void Save(string filePath, ImageOptions options)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| filePath | String | 文件的路径。 |
| options | ImageOptions | 用于保存图像的选项。 |

### 例子

以下示例显示了如何将图像保存到不同格式的文件中：

```csharp
// 创建 Parser 类的实例
using (Parser parser = new Parser(filePath))
{
    // 从文档中提取图像
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // 检查是否支持图像提取
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // 创建以PNG格式保存图像的选项
    ImageOptions options = new ImageOptions(ImageFormat.Png);
    
    // 遍历图像
    foreach (PageImageArea image in images)
    {
        // 将图片保存为 png 文件
        image.Save(Guid.NewGuid().ToString() + ".png", options);
    }
}
```

### 也可以看看

* class [ImageOptions](../../../groupdocs.parser.options/imageoptions)
* class [PageImageArea](../../pageimagearea)
* 命名空间 [GroupDocs.Parser.Data](../../pageimagearea)
* 部件 [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
