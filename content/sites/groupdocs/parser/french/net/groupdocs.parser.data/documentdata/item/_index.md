---
title: Item
second_title: Référence de l'API GroupDocs.Parser pour .NET
description: Obtient les données du champ par un index.
type: docs
weight: 30
url: /fr/net/groupdocs.parser.data/documentdata/item/
---
## DocumentData indexer

Obtient les données du champ par un index.

```csharp
public FieldData this[int index] { get; }
```

| Paramètre | La description |
| --- | --- |
| index | Index de base zéro du champ. |

### Return_Value

Un exemple de[`FieldData`](../../fielddata) classer.

### Exemples

Itération via tous les champs :

[`FieldData`](../../fielddata) la classe représente les données de champ. Selon le domaine[`PageArea`](../../fielddata/pagearea) propriété peut contenir n'importe lequel des héritiers de[`PageArea`](../../pagearea) classer. Par exemple,[`ParseForm`](../../../groupdocs.parser/parser/parseform) method extrait uniquement les champs de texte :

```csharp
for (int i = 0; i < data.Count; i++)
{
    Console.Write(data[i].Name + ": ");
    PageTextArea area = data[i].PageArea as PageTextArea;
    Console.WriteLine(area == null ? "Not a template field" : area.Text);
}
```

```csharp
// Crée l'analyseur
using (Parser parser = new Parser(filePath))
{
    // Extraire les données du formulaire PDF
    DocumentData data = parser.ParseForm();
    // Itération sur les champs extraits
    for (int i = 0; i < data.Count; i++)
    {
        // Récupère le champ extrait
        FieldData field = data[i];
        // Affiche le nom du champ
        Console.Write(field.Name + ": ");
        // Vérifie si la valeur du champ est un texte et l'imprime
        PageTextArea area = field.PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Voir également

* class [FieldData](../../fielddata)
* class [DocumentData](../../documentdata)
* espace de noms [GroupDocs.Parser.Data](../../documentdata)
* Assemblée [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
