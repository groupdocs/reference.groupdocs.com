---
title: TemplateTable
second_title: Référence de l'API GroupDocs.Parser pour .NET
description: Initialise une nouvelle instance duTemplateTablegroupdocs.parser.templates/templatetable classe.
type: docs
weight: 10
url: /fr/net/groupdocs.parser.templates/templatetable/templatetable/
---
## TemplateTable(TemplateTableLayout, string, int?) {#constructor}

Initialise une nouvelle instance du[`TemplateTable`](../../templatetable) classe.

```csharp
public TemplateTable(TemplateTableLayout layout, string name, int? pageIndex)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| layout | TemplateTableLayout | La disposition du tableau. |
| name | String | Le nom du tableau. |
| pageIndex | Nullable`1 | L'index de la page où se trouve le tableau de modèles ; `nul` si le tableau de modèle se trouve sur n'importe quelle page. |

### Exemples

Le tableau de modèle est défini par la disposition du tableau si le tableau ne peut pas être détecté automatiquement :

```csharp
TemplateTableLayout layout = new TemplateTableLayout(
    new double[] { 50, 95, 275 },
    new double[] { 325, 340, 365 });
 
TemplateTable table = new TemplateTable(layout, "Details", null);

// Créer un modèle de document
Template template = new Template(new TemplateItem[] { table });
```

### Voir également

* class [TemplateTableLayout](../../templatetablelayout)
* class [TemplateTable](../../templatetable)
* espace de noms [GroupDocs.Parser.Templates](../../templatetable)
* Assemblée [GroupDocs.Parser](../../../)

---

## TemplateTable(TemplateTableParameters, string, int?) {#constructor_1}

Initialise une nouvelle instance du[`TemplateTable`](../../templatetable) classe.

```csharp
public TemplateTable(TemplateTableParameters parameters, string name, int? pageIndex)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| parameters | TemplateTableParameters | Les paramètres pour détecter la table en mode automatique. |
| name | String | Le nom du tableau. |
| pageIndex | Nullable`1 | L'index de la page où se trouve le tableau de modèles ; `nul` si le tableau de modèle se trouve sur n'importe quelle page. |

### Exemples

Si une table modèle est définie par les paramètres du détecteur, la table est détectée automatiquement :

```csharp
TemplateTableParameters parameters = new TemplateTableParameters(
    new Rectangle(new Point(175, 350), new Size(400, 200)),
    new double[] { 185, 370, 425, 485, 545 });

TemplateTable table = new TemplateTable(parameters, "Details", 0);

// Créer un modèle de document
Template template = new Template(new TemplateItem[] { table });
```

### Voir également

* class [TemplateTableParameters](../../templatetableparameters)
* class [TemplateTable](../../templatetable)
* espace de noms [GroupDocs.Parser.Templates](../../templatetable)
* Assemblée [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
