---
title: ParseForm
second_title: Référence de l'API GroupDocs.Parser pour .NET
description: Analyse le formulaire de document.
type: docs
weight: 190
url: /fr/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

Analyse le formulaire de document.

```csharp
public DocumentData ParseForm()
```

### Return_Value

Une instance de[`DocumentData`](../../../groupdocs.parser.data/documentdata) classe contenant les données extraites ; `nul` si l'analyse par modèle n'est pas prise en charge.

### Remarques

**Apprendre encore plus:**

* [Extraire des données de formulaires PDF](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [Travailler avec des données extraites](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [Analyser les données des documents PDF](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### Exemples

L'exemple suivant montre comment analyser un formulaire du document :

```csharp
// Crée une instance de la classe Parser
using (Parser parser = new Parser(filePath))
{
    // Extraire les données du document PDF
    DocumentData data = parser.ParseForm();
    // Itérer sur les données extraites
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### Voir également

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* espace de noms [GroupDocs.Parser](../../parser)
* Assemblée [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
