---
title: GetFieldsByName
second_title: GroupDocs.Parser för .NET API-referens
description: Returnerar samlingen av fältdata där namnet är lika medfieldName .
type: docs
weight: 50
url: /sv/net/groupdocs.parser.data/documentdata/getfieldsbyname/
---
## DocumentData.GetFieldsByName method

Returnerar samlingen av fältdata där namnet är lika med*fieldName* .

```csharp
public IList<FieldData> GetFieldsByName(string fieldName)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| fieldName | String | Namnet på fältet. |

### Returvärde

En samling av[`FieldData`](../../fielddata) objekt; tom samling om inga fältdata hittas.

### Exempel

Hitta fält efter ett fältnamn:

[`FieldData`](../../fielddata) klass representerar fältdata. Beroende på fält[`PageArea`](../../fielddata/pagearea) property kan innehålla vilken som helst av arvtagarna till[`PageArea`](../../pagearea) klass. Till exempel,[`ParseForm`](../../../groupdocs.parser/parser/parseform) method extraherar endast textfält.

```csharp
// Få alla fält med "Adress" namn
IList<FieldData> addressFields = data.GetFieldsByName("Address");
if(addressFields.Count == 0) {
    Console.WriteLine("Address not found");
}
else {
    Console.WriteLine("Address");
    // Iterera över fältsamlingen
    for (int i = 0; i < addressFields.Count; i++) {
        PageTextArea area = addressFields[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);       
         
        // Om det är ett relaterat fält:
        if(addressFields[i].LinkedField != null) {
            Console.Write("Linked to ");
            PageTextArea linkedArea = addressFields[i].LinkedField.PageArea as PageTextArea;
            Console.WriteLine(area == null ? "Not a template field" : area.Text);           
        }
    }
}
```

### Se även

* class [FieldData](../../fielddata)
* class [DocumentData](../../documentdata)
* namnutrymme [GroupDocs.Parser.Data](../../documentdata)
* hopsättning [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
