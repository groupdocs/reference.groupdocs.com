---
title: Save
second_title: GroupDocs.Parser för .NET API-referens
description: Sparar bilden i filen.
type: docs
weight: 50
url: /sv/net/groupdocs.parser.data/pageimagearea/save/
---
## Save(string) {#save}

Sparar bilden i filen.

```csharp
public void Save(string filePath)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| filePath | String | Sökvägen till filen. |

### Exempel

Följande exempel visar hur man sparar bilder till filer:

```csharp
// Skapa en instans av Parser-klassen
using (Parser parser = new Parser(filePath))
{
    // Extrahera bilder från dokument
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // Kontrollera om bildextraktion stöds
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // Iterera över bilder
    foreach (PageImageArea image in images)
    {
        // Spara bilden i filen
        image.Save(Guid.NewGuid().ToString() + image.FileType.Extension);
    }
}
```

### Se även

* class [PageImageArea](../../pageimagearea)
* namnutrymme [GroupDocs.Parser.Data](../../pageimagearea)
* hopsättning [GroupDocs.Parser](../../../)

---

## Save(string, ImageOptions) {#save_1}

Sparar bilden i filen i ett annat format.

```csharp
public void Save(string filePath, ImageOptions options)
```

| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| filePath | String | Sökvägen till filen. |
| options | ImageOptions | Alternativen som används för att spara bilden. |

### Exempel

Följande exempel visar hur du sparar bilder till filer i ett annat format:

```csharp
// Skapa en instans av Parser-klassen
using (Parser parser = new Parser(filePath))
{
    // Extrahera bilder från dokument
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // Kontrollera om bildextraktion stöds
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // Skapa alternativ för att spara bilder i PNG-format
    ImageOptions options = new ImageOptions(ImageFormat.Png);
    
    // Iterera över bilder
    foreach (PageImageArea image in images)
    {
        // Spara bilden till png-filen
        image.Save(Guid.NewGuid().ToString() + ".png", options);
    }
}
```

### Se även

* class [ImageOptions](../../../groupdocs.parser.options/imageoptions)
* class [PageImageArea](../../pageimagearea)
* namnutrymme [GroupDocs.Parser.Data](../../pageimagearea)
* hopsättning [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
