---
title: GetContainer
second_title: GroupDocs.Parser för .NET API-referens
description: Extraherar ett containerobjekt från dokumentet för att arbeta med format som innehåller bilagor ZIParkiv etc.
type: docs
weight: 60
url: /sv/net/groupdocs.parser/parser/getcontainer/
---
## Parser.GetContainer method

Extraherar ett containerobjekt från dokumentet för att arbeta med format som innehåller bilagor, ZIP-arkiv etc.

```csharp
public IEnumerable<ContainerItem> GetContainer()
```

### Returvärde

En samling containerobjekt; `null`om containerextraktion inte stöds.

### Anmärkningar

För att kontrollera om formatet stöder extrahering av bilagor, se[Dokumentformat som stöds](https://docs.groupdocs.com/parser/net/supported-document-formats/) (**Extrahera behållare och tillbehör** kolumn).

**Läs mer:**

* [Extrahera data från bilagor och ZIP-arkiv](https://docs.groupdocs.com/display/parsernet/Extract+data+from+attachments+and+ZIP+archives)
* [Iterera genom behållareföremål](https://docs.groupdocs.com/display/parsernet/Iterate+through+container+items)
* [Extrahera bilagor från PDF-portföljer](https://docs.groupdocs.com/display/parsernet/Extract+attachments+from+PDF+portfolios)
* [Extrahera bilagor från e-postmeddelanden](https://docs.groupdocs.com/display/parsernet/Extract+attachments+from+Emails)
* [Extrahera e-postmeddelanden från Outlook Storage](https://docs.groupdocs.com/display/parsernet/Extract+emails+from+Outlook+Storage)
* [Extrahera text från ZIP-arkivfiler](https://docs.groupdocs.com/display/parsernet/Extract+text+from+ZIP+archive+files)

### Exempel

Följande exempel visar hur man extraherar bilagor från en behållare:

```csharp
// Skapa en instans av Parser-klassen
using(Parser parser = new Parser(filePath))
{
    // Extrahera bilagor från behållaren
    IEnumerable<ContainerItem> attachments = parser.GetContainer();
    // Kontrollera om containerextraktion stöds
    if(attachments == null)
    {
        Console.WriteLine("Container extraction isn't supported");
    }
 
    // Iterera över bilagor
    foreach(ContainerItem item in attachments)
    {
        // Skriv ut ett objekts namn och storlek
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Size));
    }
}
```

### Se även

* class [ContainerItem](../../../groupdocs.parser.data/containeritem)
* class [Parser](../../parser)
* namnutrymme [GroupDocs.Parser](../../parser)
* hopsättning [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
