---
title: GetToc
second_title: GroupDocs.Parser för .NET API-referens
description: Extraherar en innehållsförteckning från dokumentet.
type: docs
weight: 170
url: /sv/net/groupdocs.parser/parser/gettoc/
---
## Parser.GetToc method

Extraherar en innehållsförteckning från dokumentet.

```csharp
public IEnumerable<TocItem> GetToc()
```

### Returvärde

En samling innehållsförteckningsobjekt; `null` om innehållsförteckningsextraktion inte stöds.

### Anmärkningar

**Läs mer:**

* [Extrahera innehållsförteckning](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents)
* [Extrahera text efter innehållsförteckning](https://docs.groupdocs.com/display/parsernet/Extract+text+by+table+of+contents+item)
* [Extrahera innehållsförteckning från Microsoft Office Word-dokument](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+Microsoft+Office+Word+documents)
* [Extrahera innehållsförteckning från EPUB e-böcker](https://docs.groupdocs.com/display/parsernet/Extract+table+of+contents+from+EPUB+eBooks)

### Exempel

Följande exempel visar hur man extraherar innehållsförteckningen från CHM-filen:

```csharp
// Skapa en instans av Parser-klassen
using (Parser parser = new Parser(filePath))
{
    // Kontrollera om textextraktion stöds
    if (!parser.Features.Text)
    {
        Console.WriteLine("Text extraction isn't supported.");
        return;
    }

    // Kontrollera om toc-extraktion stöds
    if (!parser.Features.Toc)
    {
        Console.WriteLine("Toc extraction isn't supported.");
        return;
    }
 
    // Få innehållsförteckning
    IEnumerable<TocItem> toc = parser.GetToc();
    
    // Iterera över objekt
    foreach (TocItem i in toc)
    {
        // Skriv ut Toc-texten
        Console.WriteLine(i.Text);
        // Kontrollera om sidindex har ett värde
        if (i.PageIndex == null)
        {
            continue;
        }
        // Extrahera en sidtext
        using (TextReader reader = parser.GetText(i.PageIndex.Value))
        {
            Console.WriteLine(reader.ReadToEnd());
        }
    }
}
```

### Se även

* class [TocItem](../../../groupdocs.parser.data/tocitem)
* class [Parser](../../parser)
* namnutrymme [GroupDocs.Parser](../../parser)
* hopsättning [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
