---
title: ParseForm
second_title: GroupDocs.Parser for .NET API リファレンス
description: ドキュメント フォームを解析します
type: docs
weight: 190
url: /ja/net/groupdocs.parser/parser/parseform/
---
## Parser.ParseForm method

ドキュメント フォームを解析します。

```csharp
public DocumentData ParseForm()
```

### 戻り値

のインスタンス[`DocumentData`](../../../groupdocs.parser.data/documentdata)抽出されたデータを含むクラス; `ヌル`テンプレートによる解析がサポートされていない場合.

### 備考

**もっと詳しく知る：**

* [PDF フォームからデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+data+from+PDF+forms)
* [抽出されたデータの操作](https://docs.groupdocs.com/display/parsernet/Working+with+data+extracted+by+template)
* [PDF ドキュメントからデータを解析する](https://docs.groupdocs.com/display/parsernet/Parse+data+from+PDF+documents)

### 例

次の例は、ドキュメントのフォームを解析する方法を示しています。

```csharp
// Parser クラスのインスタンスを作成します
using (Parser parser = new Parser(filePath))
{
    // PDF ドキュメントからデータを抽出します
    DocumentData data = parser.ParseForm();
    // 抽出されたデータを反復処理します
    for (int i = 0; i<data.Count; i++)
    {
        Console.Write(data[i].Name + ": ");
        PageTextArea area = data[i].PageArea as PageTextArea;
        Console.WriteLine(area == null ? "Not a template field" : area.Text);
    }
}
```

### 関連項目

* class [DocumentData](../../../groupdocs.parser.data/documentdata)
* class [Parser](../../parser)
* 名前空間 [GroupDocs.Parser](../../parser)
* 組み立て [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
