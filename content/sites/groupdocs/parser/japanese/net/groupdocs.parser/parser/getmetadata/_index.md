---
title: GetMetadata
second_title: GroupDocs.Parser for .NET API リファレンス
description: ドキュメントからメタデータを抽出します
type: docs
weight: 120
url: /ja/net/groupdocs.parser/parser/getmetadata/
---
## Parser.GetMetadata method

ドキュメントからメタデータを抽出します。

```csharp
public IEnumerable<MetadataItem> GetMetadata()
```

### 戻り値

メタデータ項目のコレクション; `ヌル`メタデータ抽出がサポートされていない場合.

### 備考

**もっと詳しく知る：**

* [ドキュメントからメタデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+documents)
* [Microsoft Office Word ドキュメントからメタデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Word+documents)
* [Microsoft Office Excel スプレッドシートからメタデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+Excel+spreadsheets)
* [Microsoft Office PowerPoint プレゼンテーションからメタデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Microsoft+Office+PowerPoint+presentations)
* [PDF ドキュメントからメタデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+PDF+documents)
* [メールからメタデータを抽出する](https://docs.groupdocs.com/display/parsernet/Extract+metadata+from+Emails)

### 例

次の例は、ドキュメントからメタデータを抽出する方法を示しています。

```csharp
// Parser クラスのインスタンスを作成します
using(Parser parser = new Parser(filePath))
{
    // ドキュメントからメタデータを抽出します
    IEnumerable<MetadataItem> metadata = parser.GetMetadata();
    // メタデータ抽出がサポートされているかどうかを確認します
    if(metadata == null)
    {
        Console.WriteLine("Metatada extraction isn't supported");
    }
 
    // メタデータ項目を繰り返します
    foreach(MetadataItem item in metadata)
    {
        // アイテム名と値を出力
        Console.WriteLine(string.Format("{0}: {1}", item.Name, item.Value));
    }
}
```

### 関連項目

* class [MetadataItem](../../../groupdocs.parser.data/metadataitem)
* class [Parser](../../parser)
* 名前空間 [GroupDocs.Parser](../../parser)
* 組み立て [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
