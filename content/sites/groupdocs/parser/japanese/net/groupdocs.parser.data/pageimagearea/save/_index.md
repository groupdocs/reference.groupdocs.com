---
title: Save
second_title: GroupDocs.Parser for .NET API リファレンス
description: 画像をファイルに保存します
type: docs
weight: 50
url: /ja/net/groupdocs.parser.data/pageimagearea/save/
---
## Save(string) {#save}

画像をファイルに保存します。

```csharp
public void Save(string filePath)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| filePath | String | ファイルへのパス。 |

### 例

次の例は、イメージをファイルに保存する方法を示しています。

```csharp
// Parser クラスのインスタンスを作成します
using (Parser parser = new Parser(filePath))
{
    // ドキュメントから画像を抽出
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // 画像抽出がサポートされているかどうかを確認します
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // 画像を繰り返す
    foreach (PageImageArea image in images)
    {
        // 画像をファイルに保存
        image.Save(Guid.NewGuid().ToString() + image.FileType.Extension);
    }
}
```

### 関連項目

* class [PageImageArea](../../pageimagearea)
* 名前空間 [GroupDocs.Parser.Data](../../pageimagearea)
* 組み立て [GroupDocs.Parser](../../../)

---

## Save(string, ImageOptions) {#save_1}

画像を別の形式でファイルに保存します。

```csharp
public void Save(string filePath, ImageOptions options)
```

| パラメータ | タイプ | 説明 |
| --- | --- | --- |
| filePath | String | ファイルへのパス。 |
| options | ImageOptions | 画像の保存に使用されるオプション。 |

### 例

次の例は、画像を別の形式でファイルに保存する方法を示しています。

```csharp
// Parser クラスのインスタンスを作成します
using (Parser parser = new Parser(filePath))
{
    // ドキュメントから画像を抽出
    IEnumerable<PageImageArea> images = parser.GetImages();
    
    // 画像抽出がサポートされているかどうかを確認します
    if (images == null)
    {
        Console.WriteLine("Page images extraction isn't supported");
        return;
    }

    // 画像を PNG 形式で保存するためのオプションを作成します
    ImageOptions options = new ImageOptions(ImageFormat.Png);
    
    // 画像を繰り返す
    foreach (PageImageArea image in images)
    {
        //画像をpngファイルに保存
        image.Save(Guid.NewGuid().ToString() + ".png", options);
    }
}
```

### 関連項目

* class [ImageOptions](../../../groupdocs.parser.options/imageoptions)
* class [PageImageArea](../../pageimagearea)
* 名前空間 [GroupDocs.Parser.Data](../../pageimagearea)
* 組み立て [GroupDocs.Parser](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Parser.dll -->
