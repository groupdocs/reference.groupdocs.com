---
title: Edit
second_title: Справочник по API GroupDocs.Editor для .NET
description: Открывает ранее загруженный документ для редактирования с использованием указанных параметров специфичных для формата путем создания и возврата экземпляра EditableDocumentgroupdocs.editor/editabledocument класс который в свою очередь содержит методы для создания HTMLразметки и связанных ресурсов.
type: docs
weight: 50
url: /ru/net/groupdocs.editor/editor/edit/
---
## Edit(IEditOptions) {#edit_1}

Открывает ранее загруженный документ для редактирования с использованием указанных параметров, специфичных для формата, путем создания и возврата экземпляра '[`EditableDocument`](../../editabledocument) класс, который, в свою очередь, содержит методы для создания HTML-разметки и связанных ресурсов.

```csharp
public EditableDocument Edit(IEditOptions editOptions)
```

| Параметр | Тип | Описание |
| --- | --- | --- |
| editOptions | IEditOptions | Параметры документа для конкретного формата, что позволяет настроить процесс конвертации. Может быть NULL — в этом случае GroupDocs.Editor определяет формат ранее загруженного документа и применяет параметры, установленные по умолчанию для этого формата. Не должно конфликтовать с ранее примененными параметрами загрузки. |

### Возвращаемое значение

Экземпляр '[`EditableDocument`](../../editabledocument) класс, который инкапсулирует весь входной документ со всеми его ресурсами в промежуточном формате. Этот метод в случае успешного завершения никогда не возвращает NULL.

### Примечания

Когда входной исходный документ загружается в экземпляр «Редактор» через конструктор, этот метод позволяет открыть документ для редактирования, преобразовав его в промежуточный формат, который инкапсулируется в экземпляре класса «EditableDocument». '[`EditableDocument`](../../editabledocument) , возвращаемый из этого метода, содержит все необходимые методы и свойства для создания HTML-разметки и соответствующих ресурсов (таких как изображения, шрифты и таблицы стилей) во всех необходимых конфигурациях для последующей передачи их в любой HTML-редактор WYSIWYG. Эта перегрузка получает параметры редактирования, характерные для форматов семейства. **Учить больше**

* Подробнее о редактировании документов с помощью GroupDocs.Editor: [Как редактировать документ с помощью GroupDocs.Editor](https://docs.groupdocs.com/display/editornet/Edit+document)

### Смотрите также

* class [EditableDocument](../../editabledocument)
* interface [IEditOptions](../../../groupdocs.editor.options/ieditoptions)
* class [Editor](../../editor)
* пространство имен [GroupDocs.Editor](../../editor)
* сборка [GroupDocs.Editor](../../../)

---

## Edit() {#edit}

Открывает ранее загруженный документ для редактирования с параметрами по умолчанию, создавая и возвращая экземпляр '[`EditableDocument`](../../editabledocument) класс, который, в свою очередь, содержит методы для создания HTML-разметки и связанных ресурсов.

```csharp
public EditableDocument Edit()
```

### Возвращаемое значение

Экземпляр '[`EditableDocument`](../../editabledocument) класс, который инкапсулирует весь входной документ со всеми его ресурсами в промежуточном формате. Этот метод в случае успешного завершения никогда не возвращает NULL.

### Примечания

Когда входной исходный документ загружается в экземпляр «Редактор» через конструктор, этот метод позволяет открыть документ для редактирования путем преобразования его в промежуточный формат, который инкапсулируется в экземпляре «[`EditableDocument`](../../editabledocument) учебный класс. '[`EditableDocument`](../../editabledocument) , возвращаемый из этого метода, содержит все необходимые методы и свойства для создания HTML-разметки и соответствующих ресурсов (таких как изображения, шрифты и таблицы стилей) во всех необходимых конфигурациях для последующей передачи их в любой HTML-редактор WYSIWYG. Эта перегрузка применяет параметры редактирования, которые установлены по умолчанию для формата, к которому принадлежит входной документ. **Учить больше**

* Подробнее о редактировании документов с помощью GroupDocs.Editor: [Как редактировать документ с помощью GroupDocs.Editor](https://docs.groupdocs.com/display/editornet/Edit+document)

### Смотрите также

* class [EditableDocument](../../editabledocument)
* class [Editor](../../editor)
* пространство имен [GroupDocs.Editor](../../editor)
* сборка [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
