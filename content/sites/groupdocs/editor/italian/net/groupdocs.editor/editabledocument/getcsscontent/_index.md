---
title: GetCssContent
second_title: GroupDocs.Editor per Riferimento API .NET
description: Restituisce il contenuto di tutti i fogli di stile esterni come un elenco di stringhe dove una stringa rappresenta un foglio di stile. Restituisce un elenco vuoto se non sono presenti CSS per questo documento.
type: docs
weight: 140
url: /it/net/groupdocs.editor/editabledocument/getcsscontent/
---
## GetCssContent() {#getcsscontent}

Restituisce il contenuto di tutti i fogli di stile esterni come un elenco di stringhe, dove una stringa rappresenta un foglio di stile. Restituisce un elenco vuoto, se non sono presenti CSS per questo documento.

```csharp
public List<string> GetCssContent()
```

### Valore di ritorno

Un elenco di stringhe, in cui ogni stringa contiene un contenuto di un documento CSS

### Guarda anche

* class [EditableDocument](../../editabledocument)
* spazio dei nomi [GroupDocs.Editor](../../editabledocument)
* assemblea [GroupDocs.Editor](../../../)

---

## GetCssContent(string, string) {#getcsscontent_1}

Restituisce il contenuto di tutti i fogli di stile esterni come un elenco di stringhe, dove una stringa rappresenta un foglio di stile. Il prefisso specificato verrà applicato a ogni collegamento alla risorsa esterna in ogni foglio di stile risultante. Restituisce un elenco vuoto, se non esiste un CSS per questo documento.

```csharp
public List<string> GetCssContent(string externalImagesPrefix, string externalFontsPrefix)
```

| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| externalImagesPrefix | String | Attraverso questo parametro utilizzato è possibile specificare un prefisso, che verrà aggiunto ai link a tutte le immagini esterne, che saranno presenti nelle dichiarazioni CSS nelle stringhe CSS risultanti. Se NULL o vuoto, i prefissi non verranno aggiunti. |
| externalFontsPrefix | String | Attraverso questo parametro utilizzato è possibile specificare un prefisso, che verrà aggiunto a links a tutti i font esterni in @font-face at-rules nelle stringhe CSS risultanti. Se NULL o vuoto, i prefissi non verranno aggiunti. |

### Valore di ritorno

Un elenco di stringhe, in cui ogni stringa contiene un contenuto di un documento CSS

### Guarda anche

* class [EditableDocument](../../editabledocument)
* spazio dei nomi [GroupDocs.Editor](../../editabledocument)
* assemblea [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
