---
title: WorksheetNumber
second_title: Referencia de API de GroupDocs.Editor para .NET
description: Permite insertar la hoja de cálculo editada en una copia de la hoja de cálculo existente en lugar de crear una nueva hoja de cálculo de una sola hoja de cálculo comportamiento predeterminado. WorksheetNumber es un número basado en 1 de una hoja de cálculo en la hoja de cálculo cargada en la clase Editor. Si es 0 valor predeterminado la nueva hoja de cálculo se creará con una sola hoja de trabajo editada. Si es mayor o menor que cero y hay una hoja de cálculo válida cargada en la clase Editor la hoja de trabajo editada que está representada por instancia de EditableDocument de entrada se insertará en esta hoja de cálculo.
type: docs
weight: 50
url: /es/net/groupdocs.editor.options/spreadsheetsaveoptions/worksheetnumber/
---
## SpreadsheetSaveOptions.WorksheetNumber property

Permite insertar la hoja de cálculo editada en una copia de la hoja de cálculo existente en lugar de crear una nueva hoja de cálculo de una sola hoja de cálculo (comportamiento predeterminado). WorksheetNumber es un número basado en 1 de una hoja de cálculo en la hoja de cálculo, cargada en la clase Editor. Si es 0 (valor predeterminado), la nueva hoja de cálculo se creará con una sola hoja de trabajo editada. Si es mayor o menor que cero, y hay una hoja de cálculo válida, cargada en la clase Editor, la hoja de trabajo editada, que está representada por instancia de EditableDocument de entrada, se insertará en esta hoja de cálculo.

```csharp
public int WorksheetNumber { get; set; }
```

### Observaciones

Número de hoja de trabajopropiedad entera, si no está en el estado predeterminado (valor reservado '0'), representa un número de hoja de trabajo, por lo que comienza desde 1, no desde cero, y su valor máximo es la cantidad de todas las diapositivas existentes en una presentación. Sin embargo, si el valor especificado es mayor que la cantidad de todas las diapositivas, GroupDocs.Editor lo ajustará para marcar la última hoja de trabajo. También se permiten valores negativos y se cuentan las hojas de trabajo desde el final. Por ejemplo, "-1" implica la última hoja de trabajo en una hoja de cálculo, "-2" — penúltimo, etc. Al igual que con los valores positivos, cuando el número de hoja de trabajo negativo excede el recuento total de hojas de trabajo en la hoja de cálculo dada, se ajustará a la primera hoja de cálculo. La[`InsertAsNewWorksheet`](../insertasnewworksheet) La propiedad booleana está estrechamente relacionada con esta.

### Ejemplos

La hoja de cálculo dada tiene 5 hojas de trabajo: WorksheetNumber = 0; — ignorar la hoja de cálculo dada, crear una nueva hoja de cálculo y colocar la hoja de trabajo editada en ella. WorksheetNumber = 1; — reemplace la primera hoja de trabajo con edited WorksheetNumber = 2; — reemplace la segunda hoja de trabajo con edited WorksheetNumber = 5; — reemplace la última (5ta) hoja de trabajo con edited WorksheetNumber = 6; — reemplace la última (5.ª) hoja de trabajo con editada, porque 6 es mayor que 5 y, por lo tanto, se ajusta WorksheetNumber = -1; — reemplace la última (5ta) hoja de trabajo con editada, porque "-1" significa "última existente" WorksheetNumber = -2; — reemplace la cuarta hoja de trabajo con edited WorksheetNumber = -3; — reemplace la tercera hoja de trabajo con edited WorksheetNumber = -4; — reemplace la segunda hoja de trabajo con edited WorksheetNumber = -5; — reemplace la primera hoja de trabajo con edited WorksheetNumber = -6; — reemplace la primera hoja de trabajo con editada, porque "-6" es mayor que 5 y, por lo tanto, se ajusta

### Ver también

* class [SpreadsheetSaveOptions](../../spreadsheetsaveoptions)
* espacio de nombres [GroupDocs.Editor.Options](../../spreadsheetsaveoptions)
* asamblea [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
