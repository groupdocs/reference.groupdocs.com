---
title: InsertAsNewWorksheet
second_title: Referencia de API de GroupDocs.Editor para .NET
description: Indicador booleano que especifica si la hoja de trabajo editada debe reemplazar la hoja de trabajo existente en la hoja de cálculo original en la posición especificada por elWorksheetNumbergroupdocs.editor.options/spreadsheetsaveoptions/worksheetnumber property o debe insertarse entre la hoja de trabajo existente y la anterior sin reemplazar su contenido. Por defecto es falso se reemplazará la hoja de trabajo existente. Esta propiedad se ignora si el valor deWorksheetNumbergroupdocs.editor.options/spreadsheetsaveoptions/worksheetnumber la propiedad se establece en 0.
type: docs
weight: 20
url: /es/net/groupdocs.editor.options/spreadsheetsaveoptions/insertasnewworksheet/
---
## SpreadsheetSaveOptions.InsertAsNewWorksheet property

Indicador booleano, que especifica si la hoja de trabajo editada debe reemplazar la hoja de trabajo existente en la hoja de cálculo original en la posición, especificada por el[`WorksheetNumber`](../worksheetnumber) property, o debe insertarse entre la hoja de trabajo existente y la anterior, sin reemplazar su contenido. Por defecto es falso: se reemplazará la hoja de trabajo existente. Esta propiedad se ignora si el valor de[`WorksheetNumber`](../worksheetnumber) la propiedad se establece en '0'.

```csharp
public bool InsertAsNewWorksheet { get; set; }
```

### Observaciones

Por defecto, se reemplaza la hoja de cálculo. Esto significa que si la hoja de cálculo dada tiene 5 hojas de trabajo, y[`WorksheetNumber`](../worksheetnumber) =4, la cuarta hoja de trabajo se reemplazará con la nueva hoja de trabajo editada, mientras que la cantidad total de hojas de trabajo en la hoja de cálculo (5) permanecerá intacta. Sin embargo, si el valor de esta propiedad se establece enverdaderola nueva hoja de cálculo editada se inyectará como la cuarta hoja de cálculo y todas las hojas de cálculo posteriores se desplazarán al final: la cuarta hoja de cálculo "antigua" se convierte en la quinta, y la quinta se convierte en la sexta, y la cantidad total de hojas de cálculo en la hoja de cálculo se incrementará en uno e igual a 6.

### Ver también

* class [SpreadsheetSaveOptions](../../spreadsheetsaveoptions)
* espacio de nombres [GroupDocs.Editor.Options](../../spreadsheetsaveoptions)
* asamblea [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
