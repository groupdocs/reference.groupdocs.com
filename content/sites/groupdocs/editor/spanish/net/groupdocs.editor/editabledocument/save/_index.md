---
title: Save
second_title: Referencia de API de GroupDocs.Editor para .NET
description: Guarda este documento HTML en el archivo de la ruta especificada donde se almacenará el marcado HTML y en la carpeta adjunta con recursos.
type: docs
weight: 160
url: /es/net/groupdocs.editor/editabledocument/save/
---
## Save(string) {#save}

Guarda este documento HTML en el archivo de la ruta especificada, donde se almacenará el marcado HTML, y en la carpeta adjunta con recursos.

```csharp
public void Save(string htmlFilePath)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| htmlFilePath | String | Ruta completa al archivo, donde se almacenará el marcado HTML. El archivo se creará o se sobrescribirá, si existe. La carpeta de recursos adjunta se creará en la misma carpeta, donde existe el archivo HTML. |

### Ver también

* class [EditableDocument](../../editabledocument)
* espacio de nombres [GroupDocs.Editor](../../editabledocument)
* asamblea [GroupDocs.Editor](../../../)

---

## Save(string, string) {#save_1}

Guarda este documento HTML en el archivo en la ruta especificada, donde se almacenará el marcado HTML, y en la carpeta adjunta con recursos, que se encuentra en la ruta especificada.

```csharp
public void Save(string htmlFilePath, string resourcesFolderPath)
```

| Parámetro | Escribe | Descripción |
| --- | --- | --- |
| htmlFilePath | String | Ruta completa al archivo, donde se almacenará el marcado HTML. No puede ser NULL ni estar vacío. El archivo se creará o se sobrescribirá, si existe. |
| resourcesFolderPath | String | Ruta completa a la carpeta adjunta, donde se almacenarán todos los recursos relacionados. Si es NULL o está vacío, la carpeta se creará automáticamente en el mismo directorio, donde se encuentra el archivo *.html. Si se especifica y no existe, se creará. |

### Ver también

* class [EditableDocument](../../editabledocument)
* espacio de nombres [GroupDocs.Editor](../../editabledocument)
* asamblea [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
