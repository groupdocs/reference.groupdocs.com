---
title: Equals
second_title: .NET API 참조용 GroupDocs.Editor
description: 이 인스턴스가 다른 지정된 이메일 instance 와 동일한지 여부를 결정합니다.
type: docs
weight: 170
url: /ko/net/groupdocs.editor.formats/emailformats/equals/
---
## Equals(EmailFormats) {#equals}

이 인스턴스가 다른 지정된 이메일 instance 와 동일한지 여부를 결정합니다.

```csharp
public bool Equals(EmailFormats other)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| other | EmailFormats | 이 항목과 같은지 확인해야 하는 다른 이메일 인스턴스 |

### 반환 값

같으면 참, 같지 않으면 거짓

### 또한보십시오

* struct [EmailFormats](../../emailformats)
* 네임스페이스 [GroupDocs.Editor.Formats](../../emailformats)
* 집회 [GroupDocs.Editor](../../../)

---

## Equals(IDocumentFormat) {#equals_1}

이 인스턴스가 지정된 다른 IDocumentFormat instance 와 같은지 여부를 결정합니다.

```csharp
public bool Equals(IDocumentFormat other)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| other | IDocumentFormat | 다른 IDocumentFormat 인스턴스. 이메일이 아닌 경우 메서드는 'false'를 반환합니다. |

### 반환 값

같으면 참, 같지 않으면 거짓

### 또한보십시오

* interface [IDocumentFormat](../../idocumentformat)
* struct [EmailFormats](../../emailformats)
* 네임스페이스 [GroupDocs.Editor.Formats](../../emailformats)
* 집회 [GroupDocs.Editor](../../../)

---

## Equals(object) {#equals_2}

이 인스턴스가 다른 지정된 개체, 즉 boxed Email 와 같은지 여부를 결정합니다.

```csharp
public override bool Equals(object obj)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| obj | Object | 기타 박스형 이메일 인스턴스 |

### 반환 값

같으면 참, 같지 않으면 거짓

### 또한보십시오

* struct [EmailFormats](../../emailformats)
* 네임스페이스 [GroupDocs.Editor.Formats](../../emailformats)
* 집회 [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
