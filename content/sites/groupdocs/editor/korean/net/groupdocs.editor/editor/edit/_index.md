---
title: Edit
second_title: .NET API 참조용 GroupDocs.Editor
description: 의 인스턴스를 생성하고 반환하여 지정된 형식별 옵션을 사용하여 편집하기 위해 이전에 로드된 문서를 엽니다.EditableDocumentgroupdocs.editor/editabledocument 이 클래스에는 HTML 마크업 및 관련 리소스를 생성하는 메서드가 포함되어 있습니다.
type: docs
weight: 50
url: /ko/net/groupdocs.editor/editor/edit/
---
## Edit(IEditOptions) {#edit_1}

'의 인스턴스를 생성하고 반환하여 지정된 형식별 옵션을 사용하여 편집하기 위해 이전에 로드된 문서를 엽니다.[`EditableDocument`](../../editabledocument) 이 클래스에는 HTML 마크업 및 관련 리소스를 생성하는 메서드가 포함되어 있습니다.

```csharp
public EditableDocument Edit(IEditOptions editOptions)
```

| 모수 | 유형 | 설명 |
| --- | --- | --- |
| editOptions | IEditOptions | 변환 프로세스를 조정할 수 있는 형식별 문서 옵션. NULL일 수 있음 — 이 경우 GroupDocs.Editor는 이전에 로드된 문서의 형식을 감지하고 이 형식의 기본값인 옵션을 적용합니다. 이전에 적용된 로드 옵션과 충돌하지 않아야 합니다. |

### 반환 값

'의 인스턴스[`EditableDocument`](../../editabledocument) 전체 입력 문서를 중간 형식의 모든 리소스와 함께 캡슐화하는 클래스입니다. 이 메서드는 성공적으로 완료되면 NULL을 반환하지 않습니다.

### 비고

입력된 원본 문서가 생성자를 통해 'Editor' 인스턴스에 로드되면 이 메서드는 문서를 'EditableDocument' 클래스의 인스턴스 내에 캡슐화되는 중간 형식으로 변환하여 편집용으로 문서를 열 수 있습니다. '[`EditableDocument`](../../editabledocument)이 메서드에서 반환된 에는 나중에 WYSIWYG HTML 편집기로 전달하기 위해 필요한 모든 구성에서 HTML 마크업 및 해당 리소스(예: 이미지, 글꼴 및 스타일시트)를 생성하는 데 필요한 모든 메서드와 속성이 포함되어 있습니다. 이 오버로드는 패밀리 형식에 특정한 편집 옵션을 가져옵니다. **더 알아보기**

* GroupDocs.Editor: 를 사용한 문서 편집에 대한 추가 정보[GroupDocs.Editor를 사용하여 문서를 편집하는 방법](https://docs.groupdocs.com/display/editornet/Edit+document)

### 또한보십시오

* class [EditableDocument](../../editabledocument)
* interface [IEditOptions](../../../groupdocs.editor.options/ieditoptions)
* class [Editor](../../editor)
* 네임스페이스 [GroupDocs.Editor](../../editor)
* 집회 [GroupDocs.Editor](../../../)

---

## Edit() {#edit}

'의 인스턴스를 생성하고 반환하여 기본 옵션을 사용하여 편집하기 위해 이전에 로드된 문서를 엽니다.[`EditableDocument`](../../editabledocument) 이 클래스에는 HTML 마크업 및 관련 리소스를 생성하는 메서드가 포함되어 있습니다.

```csharp
public EditableDocument Edit()
```

### 반환 값

'의 인스턴스[`EditableDocument`](../../editabledocument) 전체 입력 문서를 중간 형식의 모든 리소스와 함께 캡슐화하는 클래스입니다. 이 메서드는 성공적으로 완료되면 NULL을 반환하지 않습니다.

### 비고

입력된 원본 문서가 생성자를 통해 'Editor' 인스턴스에 로드되면 이 메서드는 문서를 중간 형식으로 변환하여 편집용으로 열 수 있으며, 이 메서드는 ' 인스턴스 내에 캡슐화됩니다.[`EditableDocument`](../../editabledocument) 수업. '[`EditableDocument`](../../editabledocument)이 메서드에서 반환된 에는 이후에 WYSIWYG HTML 편집기로 전달하기 위해 필요한 모든 구성에서 HTML 마크업 및 해당 리소스(예: 이미지, 글꼴 및 스타일시트)를 생성하는 데 필요한 모든 메서드와 속성이 포함되어 있습니다. 이 오버로드는 입력 문서가 속한 형식의 기본값인 편집 옵션을 적용합니다. **더 알아보기**

* GroupDocs.Editor: 를 사용한 문서 편집에 대한 추가 정보[GroupDocs.Editor를 사용하여 문서를 편집하는 방법](https://docs.groupdocs.com/display/editornet/Edit+document)

### 또한보십시오

* class [EditableDocument](../../editabledocument)
* class [Editor](../../editor)
* 네임스페이스 [GroupDocs.Editor](../../editor)
* 집회 [GroupDocs.Editor](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Editor.dll -->
