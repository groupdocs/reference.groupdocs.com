---
title: JsonDataSource
second_title: GroupDocs.Assembly voor .NET API-referentie
description: Biedt toegang tot gegevens van een JSONbestand of stream die kan worden gebruikt tijdens het samenstellen van een document.
type: docs
weight: 150
url: /nl/net/groupdocs.assembly.data/jsondatasource/
---
## JsonDataSource class

Biedt toegang tot gegevens van een JSON-bestand of -stream die kan worden gebruikt tijdens het samenstellen van een document.

```csharp
public class JsonDataSource
```

## Constructeurs

| Naam | Beschrijving |
| --- | --- |
| [JsonDataSource](jsondatasource#constructor)(Stream) | Maakt een nieuwe gegevensbron met gegevens uit een JSON-stream met behulp van standaardopties voor het parseren van JSON-gegevens. |
| [JsonDataSource](jsondatasource#constructor_2)(string) | Maakt een nieuwe gegevensbron met gegevens uit een JSON-bestand met behulp van standaardopties voor het parseren van JSON-gegevens. |
| [JsonDataSource](jsondatasource#constructor_1)(Stream, JsonDataLoadOptions) | Maakt een nieuwe gegevensbron met gegevens uit een JSON-stream met behulp van de opgegeven opties voor het parseren van JSON-gegevens. |
| [JsonDataSource](jsondatasource#constructor_3)(string, JsonDataLoadOptions) | Maakt een nieuwe gegevensbron met gegevens uit een JSON-bestand met behulp van de opgegeven opties voor het parseren van JSON-gegevens. |

### Opmerkingen

Om toegang te krijgen tot gegevens van het overeenkomstige bestand of de corresponderende stream tijdens het samenstellen van een document, geeft u een instantie van deze klasse door als een gegevensbron aan een van[`DocumentAssembler`](../../groupdocs.assembly/documentassembler) .AssembleDocument overbelasting.

In sjabloondocumenten, als een JSON-element op het hoogste niveau een array is, a[`JsonDataSource`](../jsondatasource) instantie moet worden op dezelfde manier worden behandeld alsof het eenDataTable voorbeeld. Als een JSON-element op het hoogste niveau een object is, a[`JsonDataSource`](../jsondatasource) instantie moet op dezelfde manier worden behandeld alsof het wasDataRowvoorbeeld. Zie voor meer informatie sjabloonsyntaxisreferentie (https://docs.groupdocs.com/display/assemblynet/Template+Syntax+-+Part+1+of+2#TemplateSyntax-Part1of2-UsingDataSources).

In sjabloondocumenten kunt u werken met getypte waarden van JSON-elementen. Voor het gemak vervangt de engine de set van eenvoudige JSON-typen door de volgende:

* `lang?`
* `dubbele?`
* `bool?`
* `Datum Tijd?`
* `snaar`

De engine herkent automatisch waarden van de extra typen op basis van hun JSON-representaties.

Om het standaardgedrag van het laden van JSON-gegevens te overschrijven, initialiseert en passeert u een[`JsonDataLoadOptions`](../jsondataloadoptions)instance naar een constructor van deze klasse.

### Zie ook

* naamruimte [GroupDocs.Assembly.Data](../../groupdocs.assembly.data)
* montage [GroupDocs.Assembly](../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Assembly.dll -->
