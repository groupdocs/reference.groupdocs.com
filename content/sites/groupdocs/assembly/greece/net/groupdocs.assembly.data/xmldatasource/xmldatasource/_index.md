---
title: XmlDataSource
second_title: GroupDocs.Assembly για Αναφορά API .NET
description: Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από ένα αρχείο XML χρησιμοποιώντας προεπιλεγμένες επιλογές για τη φόρτωση δεδομένων XML.
type: docs
weight: 10
url: /el/net/groupdocs.assembly.data/xmldatasource/xmldatasource/
---
## XmlDataSource(string) {#constructor_4}

Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από ένα αρχείο XML χρησιμοποιώντας προεπιλεγμένες επιλογές για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(string xmlPath)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlPath | String | Η διαδρομή προς το αρχείο XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |

### Δείτε επίσης

* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(Stream) {#constructor}

Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από μια ροή XML χρησιμοποιώντας προεπιλεγμένες επιλογές για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(Stream xmlStream)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlStream | Stream | Η ροή δεδομένων XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |

### Δείτε επίσης

* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(string, string) {#constructor_6}

Δημιουργεί μια νέα προέλευση δεδομένων με δεδομένα από ένα αρχείο XML χρησιμοποιώντας ένα αρχείο ορισμού σχήματος XML. Οι προεπιλεγμένες επιλογές χρησιμοποιούνται για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(string xmlPath, string xmlSchemaPath)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlPath | String | Η διαδρομή προς το αρχείο XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |
| xmlSchemaPath | String | Η διαδρομή προς το αρχείο ορισμού σχήματος XML που παρέχει σχήμα για το αρχείο XML . |

### Δείτε επίσης

* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(Stream, Stream) {#constructor_2}

Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από μια ροή XML χρησιμοποιώντας μια ροή ορισμού σχήματος XML. Οι προεπιλεγμένες επιλογές χρησιμοποιούνται για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(Stream xmlStream, Stream xmlSchemaStream)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlStream | Stream | Η ροή δεδομένων XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |
| xmlSchemaStream | Stream | Η ροή του ορισμού σχήματος XML που παρέχει σχήμα για τα δεδομένα XML. |

### Δείτε επίσης

* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(string, XmlDataLoadOptions) {#constructor_5}

Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από ένα αρχείο XML χρησιμοποιώντας τις καθορισμένες επιλογές για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(string xmlPath, XmlDataLoadOptions options)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlPath | String | Η διαδρομή προς το αρχείο XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |
| options | XmlDataLoadOptions | Επιλογές για φόρτωση δεδομένων XML. |

### Δείτε επίσης

* class [XmlDataLoadOptions](../../xmldataloadoptions)
* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(Stream, XmlDataLoadOptions) {#constructor_1}

Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από μια ροή XML χρησιμοποιώντας τις καθορισμένες επιλογές για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(Stream xmlStream, XmlDataLoadOptions options)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlStream | Stream | Η ροή δεδομένων XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |
| options | XmlDataLoadOptions | Επιλογές για φόρτωση δεδομένων XML. |

### Δείτε επίσης

* class [XmlDataLoadOptions](../../xmldataloadoptions)
* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(string, string, XmlDataLoadOptions) {#constructor_7}

Δημιουργεί μια νέα προέλευση δεδομένων με δεδομένα από ένα αρχείο XML χρησιμοποιώντας ένα αρχείο ορισμού σχήματος XML. Οι επιλογές specified χρησιμοποιούνται για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(string xmlPath, string xmlSchemaPath, XmlDataLoadOptions options)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlPath | String | Η διαδρομή προς το αρχείο XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |
| xmlSchemaPath | String | Η διαδρομή προς το αρχείο ορισμού σχήματος XML που παρέχει σχήμα για το αρχείο XML . |
| options | XmlDataLoadOptions | Επιλογές για φόρτωση δεδομένων XML. |

### Δείτε επίσης

* class [XmlDataLoadOptions](../../xmldataloadoptions)
* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

---

## XmlDataSource(Stream, Stream, XmlDataLoadOptions) {#constructor_3}

Δημιουργεί μια νέα πηγή δεδομένων με δεδομένα από μια ροή XML χρησιμοποιώντας μια ροή ορισμού σχήματος XML. Οι επιλογές specified χρησιμοποιούνται για τη φόρτωση δεδομένων XML.

```csharp
public XmlDataSource(Stream xmlStream, Stream xmlSchemaStream, XmlDataLoadOptions options)
```

| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| xmlStream | Stream | Η ροή δεδομένων XML που θα χρησιμοποιηθεί ως πηγή δεδομένων. |
| xmlSchemaStream | Stream | Η ροή του ορισμού σχήματος XML που παρέχει σχήμα για τα δεδομένα XML. |
| options | XmlDataLoadOptions | Επιλογές για φόρτωση δεδομένων XML. |

### Δείτε επίσης

* class [XmlDataLoadOptions](../../xmldataloadoptions)
* class [XmlDataSource](../../xmldatasource)
* χώρος ονομάτων [GroupDocs.Assembly.Data](../../xmldatasource)
* συνέλευση [GroupDocs.Assembly](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Assembly.dll -->
