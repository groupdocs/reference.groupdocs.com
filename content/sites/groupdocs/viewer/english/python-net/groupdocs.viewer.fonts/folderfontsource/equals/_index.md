---
title: equals method
second_title: GroupDocs.Viewer for Python via .NET API References
description: "Determines whether the current FolderFontSource is the same as the specified FolderFontSource object."
type: docs
url: /python-net/groupdocs.viewer.fonts/folderfontsource/equals/
is_root: false
weight: 1010
---


## equals {#other}

Determines whether the current FolderFontSource is the same as the specified FolderFontSource object.

```python
def equals(self, other):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| other | `FolderFontSource` | The object to compare with the current FolderFontSource. |

**Returns:** bool: True if both FolderFontSource objects are the same; otherwise, False.

## equals {#obj}

Determines whether the current FolderFontSource is the same as the specified object.

```python
def equals(self, obj):
    ...
```

| Parameter | Type | Description |
| :- | :- | :- |
| obj | `Any` | The object to compare with the current FolderFontSource. |

**Returns:** bool: True if `obj` is a FolderFontSource and is the same as the current FolderFontSource; otherwise False.

### See Also
* class [`FolderFontSource`](/viewer/python-net/groupdocs.viewer.fonts/folderfontsource/)
