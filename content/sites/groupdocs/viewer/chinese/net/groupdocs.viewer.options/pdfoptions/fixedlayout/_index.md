---
title: FixedLayout
second_title: GroupDocs.Viewer for .NET API 参考
description: PDF 是一种固定格式因此所有元素在页面上都有特定位置 为确保输出 HTML 看起来与源 PDF 相同默认情况下文档以固定布局呈现为 HTML 呈现时固定布局 输出 HTML 具有固定大小因此无论窗口大小如何元素都将保留在同一位置 要使用流体布局呈现 HTML请将此属性设置为错误的.
type: docs
weight: 50
url: /zh/net/groupdocs.viewer.options/pdfoptions/fixedlayout/
---
## PdfOptions.FixedLayout property

PDF 是一种固定格式，因此所有元素在页面上都有特定位置。 为确保输出 HTML 看起来与源 PDF 相同，默认情况下文档以固定布局呈现为 HTML。 呈现时固定布局 输出 HTML 具有固定大小，因此无论窗口大小如何，元素都将保留在同一位置。 要使用流体布局呈现 HTML，请将此属性设置为`错误的`.

```csharp
public bool FixedLayout { get; set; }
```

### 评论

默认值为`真的`.

呈现为 HTML 时支持此选项。

当呈现为流体布局时图像被跳过。 在呈现包含文本内容的 PDF 文档时使用流体布局。

### 例子

此示例展示了如何使用流体布局将 PDF 文档呈现为 HTML。

```csharp
using GroupDocs.Viewer;
using GroupDocs.Viewer.Options;
//..

using (var viewer = new Viewer("resume.pdf"))
{
   var viewOptions = HtmlViewOptions.ForEmbeddedResources("page_{0}.html");
   viewOptions.FixedLayout = false;
   viewer.View(viewOptions);
}        
```

### 也可以看看

* class [PdfOptions](../../pdfoptions)
* 命名空间 [GroupDocs.Viewer.Options](../../pdfoptions)
* 部件 [GroupDocs.Viewer](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for GroupDocs.Viewer.dll -->
